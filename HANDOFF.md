# 프로젝트 인수인계 — 문제집 추출기

새 대화를 시작할 때 이 문서를 붙여넣어 맥락을 이어간다.

---

## 0. 응답 규칙

- **명령마다 실행 위치를 먼저 표시할 것.** `【내 PC — PowerShell】` 또는 `【서버 — SSH】`
- 구어체로 쓰고, 한 문장이 끝나면 줄바꿈할 것.
- 결론 먼저. 개발 용어는 괄호로 역할 설명을 덧붙일 것.
- **진단과 수정을 한 턴에 묶을 것.** "이거 고칠게" 하고 턴을 끝내지 말 것.
  말한 것은 그 턴에서 고쳐서 저장소까지 올릴 것.
- 허락을 묻지 말 것. 명령은 붙여넣기 한 번으로 끝나게 묶을 것.
  **다만 한 줄이 너무 길면 붙여넣다 잘린다.** 짧게 나눠서 줄 것.
- 사용자는 최종 결과물에 대한 피드백을 하는 사람이다. 백엔드를 이해시키려 하지 말 것.
- 추측한 수치·경로는 "확인 필요"로 표시할 것. 임의로 지어내지 말 것.
- "정답지"는 해설지를 뜻할 때만. 채점 기준 파일은 **"테스트 기준 파일"**.
- **"실측" 이라는 말은 쓰지 말 것.** "실전 테스트" 로 부른다.
- 압박성 격려, 반복 사과 금지.

**진단할 때 주의** — 이 프로젝트에서 원인 진단이 세 번 연달아 틀렸다.
추측으로 고치지 말고 `probe.py` / `why_missed.py` 로 원본 줄을 먼저 볼 것.

---

## 0-2. 작업 지침 — 이 프로젝트에서 검증된 방법 (2026-08-09 사용자 지시로 성문화)

앞으로 모든 작업은 이 지침을 따른다. 각 항목은 이 프로젝트의 실제 사건에서 나왔다.

1. **실물이 유일한 심판이다.** 추측 진단은 3연속 실패했다. 고치기 전에 원본 줄(probe)·
   원본 쪽 그림·렌더 스크린샷을 직접 보고, 고친 뒤에도 눈으로 확인한 것만 "됐다"고 한다.
2. **잣대부터 세운다.** 수정을 숫자로 검증할 방법(채점기·검수판·기준점)이 없으면
   잣대 만들기가 첫 작업이다. (동아: 기준 확보 → 첫 채점 → 다듬기 순서)
3. **모든 수정은 회귀 검증과 함께.** 다른 책·다른 지표가 망가지지 않았는지 같이 본다.
   좋아진 숫자만 보고하지 않는다.
4. **같은 방법이 두 번 어긋나면 구조를 바꾼다.** 세 번 다듬지 않는다 — 룰이 안 되면
   LLM(luna), 계산이 안 되면 실측, 어림이 안 되면 역산(측정판). 시스템이 답을 알고
   있으면(한/글의 재계산처럼) 시스템에게 묻는 실험을 설계한다.
5. **사용자 왕복은 비싸다 — 한 번에 많이 묻는다.** 확인 요청은 실험 여러 개를 한 파일에
   담고(보정판 120개, 여유 4단계), 증상은 어느 화면에서 났는지부터 특정한다 (5일 오진).
6. **오차에는 방향이 있다.** 겹침/빈틈, 오검출/미검출처럼 실패의 아픔이 비대칭이면
   어느 쪽을 감수할지 먼저 정하고 여유를 그쪽에 둔다.
7. **돈 나가는 길은 스위치와 캐시로 감싼다.** 과금 경로는 기본 꺼짐 + 지문(sha) 캐시.
   시험 과금은 금액을 먼저 밝히고 진행한다.
8. **한 턴 원칙: 진단→수정→검증→배포→기록.** 말한 것은 그 턴에 두 저장소(GitHub)까지
   올린다. 서버에만 있는 코드는 배포 순간 사라진다.
9. **실패도 기록이 이긴다.** 안 된 방법은 왜 안 됐는지까지 HANDOFF 에 남겨
   재시도를 막는다 (pyhwp 변환, 글자 크기 잣대, UNMARKED 룰).
10. **막히면 판을 키우지 말고 사례를 좁힌다.** "케이스가 너무 많다"고 느껴지면
    전수조사로 유형을 세고 가장 큰 유형 하나만 잡는다 — 케이스는 출판사 단위로 수렴한다.

---

## 1. 만들려는 것

PDF 문제집·교과서를 업로드하면 **문제만 골라내 좌우 2단으로 조판한 편집 가능 문서**를 돌려주는 웹 서비스.

| 항목 | 결정 |
|---|---|
| 대상 고객 | **학원** (배포용 문제집). 학교 결재·양식 제약 없음 |
| OCR | Mathpix `v3/pdf` + SSE 스트리밍 |
| 산출물 (1차) | **웹 편집기 + PDF 내보내기만** (2026-08-05 확정) |
| 산출물 (후속) | HWPX (한/글 표준, 조사 완료 — §11). DOCX 는 재워 둠 |
| 조판 | 좌우 2단 |
| 서버 | Vultr 2vCPU / 4GB / 80GB, Ubuntu 24.04, 서울 (158.247.240.59) |
| 처리 방식 | 비동기 (큐 + 워커) |
| 대상 | 수학. 이후 국어·영어 확장 |
| 판매가 | 페이지당 30원 (미검증 가설), 크레딧 묶음 판매 |
| 원가 | Mathpix $0.005/p = 7.5원 (환율 1,500원) |
| PG 수수료 | 3% |

**무료 체험 (확정안)**

| 단계 | 대상 | 제공 | 다운로드 |
|---|---|---|---|
| 1. 미리보기 | 비가입자 | 앞 5페이지 처리, 화면에서 결과 전체 확인 | ✕ |
| 2. 가입 크레딧 | 이메일 인증 | 1회성 10페이지 | ○ |
| 3. 결제 | 유료 | 페이지당 | ○ |

**남용 방어** — 1~3순위만 적용. 해시 중복 차단 / 다운로드 게이트 / Turnstile.

**판정 목표와 현황**

| 지표 | 목표 | 기하 | 확통 |
|---|---|---|---|
| 오검출 | 5% 미만 | **1.7%** ✅ | **0.0%** ✅ |
| 미탐지 누락 | 0% | 20.3% | 33.9% |
| 그림 오첨부 | 0% | 8.5% | 12.8% |

그림 오첨부는 측정했다. 자세한 내역은 §9.

---

## 2. 확정된 사실

**텍스트레이어는 쓸 수 없다.** 글자가 있어도 수식이 깨지고 읽는 순서가 붕괴된다. 전량 OCR 대상.

**Mathpix 는 딱 한 번 돌렸다.** 14개 파일 263페이지, $1.03.
그 결과가 `stage0_out/runs/*/result.lines.json` 에 있고, 이후 모든 작업은 이 파일만 다시 읽는다.
**지금까지 20번 넘게 돌렸지만 추가 비용은 0원이다.** `run.sh` 만 과금이다.

| 필드 | 뜻 |
|---|---|
| `type` | 줄의 종류 (본문·수식·도형·머리말) |
| `subtype` | `continues_line_*` = 윗줄에서 이어짐 (아래 주의) |
| `region` | 위치와 크기 (`top_left_x/y`, `width`, `height`) |
| `font_size` | 글자 크기 |
| `id` / `parent_id` | 줄 사이 상하위 연결 |
| `conversion_output` | 최종 문서 포함 여부 (False면 중복) |

**Mathpix 의 줄 구조에서 배운 것 — 다 값비싼 것들이다**

1. 발문을 `column` / `list_item` 같은 **빈 껍데기로 감싸고** 글자는 자식 줄에 넣는다.
   타입 이름은 판마다 달라지므로 **'글자가 없는 줄'** 인지로 판단해야 한다.
2. **`continues_line_*` 에는 두 종류가 섞여 있다.**
   진짜 이어짐(윗줄과 바짝 붙음)과 다음 덩어리의 첫 줄(멀리 떨어짐)이 같은 표시를 달고 온다.
   **세로 간격으로 가르는 게 유일한 방법이다.** 이걸 몰라서 문제 둘이 하나로 붙어 있었다.
3. 교과서는 소단원이 바뀌면 문제 번호가 1로 돌아간다. 번호를 억지로 키우면 안 된다.
4. OCR 이 `증명`을 `정명`, `탐구`를 `밤구` 로 읽는다. 표기 목록에 오독 형태를 넣어야 한다.
5. **수식에서도 오독한다.** 평행 기호 `∥` 를 빗금 두 개 `/ /` 로 준다.
   기하 교재에서 22곳 전부 평행이고 '두 번 나누기' 는 한 곳도 없어서 되돌려 놓았다.
6. **중복순열·조합의 앞첨자를 빈 껍데기로 준다.** `₄Π₂` 를 `{ }_{4} \Pi_{2}` 로 준다.
   `{ }` 를 그대로 옮기면 워드 수식에 **빈 칸**이 생긴다 (아래 참고).

**기준 파일도 수식을 잃는다.** 한/글 → PDF 편집본은 **수식·숫자·영문 변수가 통째로 사라진다.**
그래서 채점은 **한글 뼈대만 비교**한다. 숫자·수식 정확도는 이 방식으로 잴 수 없다.

**서버 pypdf 는 앞 표시번호를 버린다.** 편집본의 `107.107)` 을 `107)` 로만 뽑는다.

**미해결:** SSE 스트리밍 이벤트 0건 (진행률용, 후순위). 샘플 115 DPI.
단, 미검출 중 '원본에없음' 은 0개라 **해상도 문제는 아니다.**

---

## 3. 작업 환경

**서버 작업 폴더:** `~/mathocr/` (측정·추출 연구용)

**웹 서비스는 따로 있다:** `/opt/math-workbook-backend/` — 도커 스택(api·worker·redis·caddy).
저장소는 `math-workbook-backend`, 배포는 `bash deploy.sh` (origin/main 을 받아 재빌드).
mathocr 는 여기에 **통합돼 있다** (§12). 서버에 git 자격증명이 저장돼 있어 두 저장소 모두 직접 push 가 된다.

```
.env                  API 키 (커밋 안 됨)
pull.sh               코드 동기화 (scripts/ 폴더 통째로 받음)
scripts/
  stage0.py           Mathpix 측정 + 리포트   (과금)
  extract.py          문제 추출 (v8)
  parse_ref.py        테스트 기준 파일 파서 (v3)
  grade.py            자동 채점기 (v3, 그림 귀속 채점 포함)
  why_missed.py       미검출 원인을 네 갈래로 분류
  orphan_figs.py      어느 문제에도 안 붙은 그림의 정체를 분류
  probe.py            원본 줄 들여다보기
  latex2omml.py       LaTeX -> 워드 수식(OMML) / 브라우저 수식(MathML) 변환
  make_docx.py        좌우 2단 DOCX 생성 + 같은 조판 HTML 미리보기 (--html)
  img_probe.py        DOCX 그림이 뷰어마다 안 보이는 원인 가르기용 시험 파일 생성
  preview.py          2단 조판 HTML 미리보기
  analyze.py          lines.json 구조 분석
  inventory.py        자료 폴더 형식 판별
  run*.sh
refs/                 테스트 기준 파일 (신사고 기하·확통 편집본 PDF 2개)
samples/테스트자료_스캔본/        14개
stage0_out/runs/<파일명>/result.lines.json
grade_out/            채점 결과·점수 이력
```

**run 폴더 이름** — 필터는 `기하` / `확률과통계` 두 개면 된다.

```
[신사고 고등 기하] 01_이차곡선 / 03_벡터의 연산 / 05_공간도형
[신사고 고등 확률과통계] 01_순열 / 03_확률의 뜻과 활용 / 04_조건부확률 / 06_통계적 추정
```

**표준 명령 — 붙여넣기 한 번으로 끝나는 형태로 줄 것**

```
cd ~/mathocr && bash pull.sh > /dev/null 2>&1 && bash scripts/run_grade.sh "refs/[기하][신사고][문제편집].pdf" "기하" 기하 "메모" > /dev/null 2>&1; bash scripts/run_grade.sh "refs/[확률과통계][신사고][문제편집].pdf" "확률과통계" 확통 "메모" > /dev/null 2>&1; for t in 기하 확통; do echo "######## $t"; sed -n '/## 1. 점수/,/직전 실행/p' grade_out/GRADE_$t.md; done
```

**서버 파이썬 패키지:** requests / pypdf / pillow. pdfplumber 안 쓴다.
DOCX 를 눈으로 볼 때 쓰는 LibreOffice 도 깔려 있다 (§9).

`raw.githubusercontent.com` 은 CDN 캐시 때문에 옛 파일을 준다. codeload 압축본을 쓸 것.

**내 PC 로 결과물 받기** — 한 줄이 길면 붙여넣다 잘린다. 한 줄씩 짧게 줄 것.

```
scp root@158.247.240.59:'~/mathocr/out/*.docx' $HOME\Downloads\
```

---

## 4. 추출기 현황 (v8)

**판정 로직**

1. `page_info` 제거, `conversion_output=False` 제거, 페이지 폭 75% 넘는 그림 제거
2. 시작 판정: `문제/예제/유제/탐구/유형/0N` + 글자크기 60 이하
   - 부모가 **빈 껍데기면 통과** (부모를 못 찾아도 통과)
   - `탐구 (1)` 처럼 괄호 붙은 것은 소문항이므로 제외
3. 파일별 x 최빈 구간 학습 후 ±90 밖 제외
4. 끝 판정 — 명령형 어미 이후 첫 서술문에서 끊는다. 예제도 같다.
   - 이어짐 표시는 **간격이 좁으면 계속, 넓으면 끊는다**
   - `(단, ...)` 단서 절은 어미 뒤에 와도 발문의 일부로 본다
5. 종료 사유: 파일경계 / 단원제목 / 풀이시작 / 간격 / 페이지초과 / 60줄 / 본문종료
6. 번호는 원본 그대로. 억지로 키우지 않는다.

**점수 이력**

| 버전 | 기하 정확도 | 기하 오검출률 | 바꾼 것 |
|---|---|---|---|
| v3 | 0.319 | 0.902 | 기준점 |
| v4 | 0.425 | 0.203 | 빈 껍데기 부모 통과 |
| v4b | 0.507 | 0.076 | 예제 풀이에서 끊기 |
| v5 | 0.575 | 0.067 | 껍데기를 '글자 없는 줄' 로 |
| v6 | 0.616 | 0.050 | 예제도 발문에서 끊기 |
| v7 | 0.703 | 0.017 | 이어짐 줄에서 어미 확인 |
| v8 | 0.730 | 0.017 | 이어짐을 간격으로 구분, 단서 절 보존 |
| v9 | 0.730 | 0.016 | 타 출판사 표기 (번호점·번호), 신사고 무손상 |
| **v9+luna (현재)** | 완전일치 54→**60** | **0.027** | LLM 분류기로 표기 없는 서술형 회수 |

**확통 (v9):** 완전일치 37·오검출 0 은 v8 그대로. 잘림 +2·부분일치 +1 은
이전에 미검출이던 표기 없는 문제(기준 13 등)를 반쪽이나마 새로 잡은 것이다.

**v9 타 출판사 표기 보강 (2026-08-06)** — 캐기(마이닝)로 근거를 만들고 넣었다.
측정해 둔 타 출판사 7개 + 실전 테스트한 「원의 방정식」에서 **122 → 145문제 (+23)**.

| 추가 표기 | 예 | 안전장치 |
|---|---|---|
| 번호점 `1.` | `1. 다음 그림에서 …` | 약한 표기 공통 장치 |
| 번호 `1 ` | `1 다음 식을 간단히 하시오` | 아래 네 겹 전부 |

약한 표기 네 겹 안전장치 — ① 이어짐 줄 제외 ② 단위·조사 뒤따르면 제외
(`5 개이다`, `1 부터`, `12 명`) ③ 몇 줄 안에 명령형 어미 필수 ④ `…일까/할까` 개념
꼭지 제외 + 바로 위가 `…알아보자`(활동 도입)면 소문항으로 보고 제외.
`생각 열기`·`생각 톡` 류는 활동 자료라 넣지 않았다.

교훈: 안전장치 없이 넣었더니 기하 오검출이 1→12 로 튀었다 (개념 꼭지 `1 타원이란
무엇일까`, 활동 소문항 `2 두 벡터…증명해 보자`). 장치를 겹겹이 걸어 v8 수준으로 되돌렸다.

**경계 판정은 사실상 끝났다.** 추출한 것 중 95%가 경계까지 정확하다.

| | 기하 | 확통 |
|---|---|---|
| 완전일치 | 54 | 37 |
| 잘림 / 넘침 / 부분일치 | 1 / 2 / 2 | 2 / 0 / 0 |

---

## 5. 남은 미검출 35건의 정체

`why_missed.py` 로 분류한 결과다.

| 원인 | 기하 | 확통 | 룰로 가능? |
|---|---|---|---|
| 표기없음 (지문으로 시작) | 9 | 19 | **어려움** |
| 표기있음 | 4 | 1 | 가능하나 분류 신뢰도 낮음 |
| 흡수됨 | 2 | 0 | 가능 |
| 원본에없음 | 0 | 0 | — |

**28건이 표기 없는 서술형이다.** `문제 N` 같은 표기 없이 지문으로 시작한다.
예: `축구 경기를 시작하기 전 심판은…`, `통일 신라 시대의 유물인 목제 주령구는…`

⚠️ **`표기있음` 분류는 믿지 말 것.** 한글 뼈대만으로 찾기 때문에
수식만 다른 같은 문장들이 같은 자리를 가리킨다. (기하 2/5/8번이 전부 '닮음 1.0')
실제로는 표기없음에 가까울 가능성이 크다.

---

## 6. 실패한 시도 — 다시 하지 말 것

**표기 없이 지문으로 시작하는 문제 찾기** (`UNMARKED`, 현재 꺼짐)

네 조건(x 대역 / 글자 크기 / 위쪽 여백 / 명령형 어미)을 모두 걸었는데도 오검출률이
기하 0.05 → 0.11, 확통 0.00 → 0.13 으로 튀었다.

**이유:** 개념 설명 문단과 발문은 네 조건이 전부 같다. 둘 다 본문 대역에 있고,
문단이 바뀌고, 개념 설명 안에도 `~해 보자` 같은 어미가 나온다.
**위치·모양 룰로는 구분할 수 없다.** 여기가 LLM 분류기가 필요한 지점이다.

기능은 코드에 남아 있다. `python3 scripts/extract.py ... --unmarked` 로 시험 가능.

**채점기의 rescue 단계** — 순서가 어긋난 짝을 건지는 기능을 넣었으나 실측에서 0건이었다.
문제가 순서가 아니었다. 해롭진 않으니 둘 것.

---

## 7. 테스트 기준 파일

`교과서 모음_문제풀이` 폴더에 수기 편집본 123개. 이 중 2개를 서버 `refs/` 에 올렸다.

| 파일 | 결과 |
|---|---|
| 신사고 기하 77쪽 | 문제 301개, 결손 0, 해설 52쪽부터 |
| 신사고 확통 75쪽 | 문제 303개, 해설 52쪽부터 (표기 없음 → 번호재시작으로 판정) |
| 교학사 확통 68쪽 | 문제 224개, 결손 0 |
| 천재(류) 경우의 수 17쪽 | 문제 65개 |

**출판사별로 어긋나는 부분** (parse_ref 가 처리)

| 어긋남 | 처리 |
|---|---|
| 머리말에 쪽번호가 박혀 쪽마다 문구가 다름 | 숫자를 `#`로 가린 뒤 반복 빈도 판정 |
| 꼬리말이 `- 12 -` 형태 | 쪽번호 패턴에 추가 |
| 해설 시작 표기가 없음 | 번호가 다시 1부터 시작하는 쪽을 해설 시작으로 |
| 소단원 번호 `N-N-N.` 이 없음 | 쪽 머리 2줄에서 제목 추정 |
| 앞 표시번호가 사라짐 | 뒤 번호만으로 찾고 1씩 커지는 가장 긴 사슬만 인정 |

---

## 8. 자동 채점기 (`scripts/grade.py`)

**판정 기준** — `담김`(기준이 추출에 얼마나 들어갔나)과 `군더더기없음`(추출이 기준 안에 들어가나), 경계 0.85.

완전일치 / 잘림 / 넘침 / 부분일치 / 미검출 / 오검출 로 집계하고 `history.jsonl` 에 쌓는다.

**주의 1** — 같은 태그로 두 번 돌리면 `직전 대비` 가 자기 자신과 비교되어 전부 0 으로 찍힌다.

**주의 2** — 채점 범위는 자동으로 축소된다. 새 문제를 많이 찾으면 분모가 커져
정확도가 오히려 떨어질 수 있다. 숫자를 그대로 믿지 말 것.

---

## 9. 바로 다음 할 일

**이미 끝난 것 두 가지** (예전 판 문서에는 "아직 없다"고 적혀 있었다)

- **DOCX 2단 조판 생성기는 있다.** `scripts/make_docx.py` + `scripts/run_docx.sh`.
  Mathpix 재호출 없이 `lines.json` 만 다시 읽으므로 비용 0원.
  `out/기하_문제집.docx` 가 실제로 나왔다 (2026-08-02).
  `preview.py` 는 HTML 미리보기용으로 그대로 둔다.
- **그림 귀속은 측정했다.** `grade.py` 가 v3 이고 채점 항목에 들어가 있다
  (GRADE 리포트 `## 7. 그림 귀속 채점`).
  기준 파일에는 그림 위치가 없으므로, **기준 발문이 그림을 가리키는지**를 정답으로 쓴다.
  `오른쪽 그림과 같이` 가 있으면 그 문제엔 그림이 있어야 한다.

**그림 귀속 실측** (2026-08-02 측정, 2026-08-04 재실행에서 같은 값 확인)

| | 기하 | 확통 |
|---|---|---|
| 판정 대상 (짝지어진 문제) | 59 | 39 |
| 그림이 필요한 문제 | 23 | 4 |
| 오첨부 | **5 / 59 = 0.085** | **5 / 39 = 0.128** |
| └ 위치로 짐작해 붙인 것 | 0 / 59 = 0.000 | 0 / 39 = 0.000 |
| 그림 누락 | 0 / 23 = 0.000 | 1 / 4 = 0.250 |

⚠️ **분모를 잊지 말 것.** 기준 파일에는 기하 **301문제**, 확통 **303문제** 가 들어 있는데
그림 채점이 실제로 보는 것은 **기하 59개 · 확통 39개** (추출과 짝이 지어진 것) 뿐이다.
8.5% 는 문제집 전체 수치가 아니라 이 59개 안에서의 비율이다.

오첨부는 전부 **본문 안에 있던 그림**이다. 짐작해서 붙인 것은 0건이라
'애매하면 안 붙인다' 는 방침은 지켜지고 있다. 목표 0% 까지는 아직 남았다.
기하 오첨부 5건 중 4건이 35~38쪽 공간도형에 몰려 있다. 도형 그림이 빽빽한 단원이다.
확통 누락 1건은 기준 102번(15쪽)이다. 분모가 4라 0.25 로 찍힐 뿐 **실제로는 1건이다.**

**어느 문제에도 안 붙은 그림 214장 — 갈라 봤다 (2026-08-04, `scripts/orphan_figs.py`)**

반대 방향은 깨끗하다. **발문이 그림을 가리키는데 그림이 없는 문제는 양쪽 다 0개다.**
붙어야 할 것이 안 붙은 게 아니라, 어디에도 안 붙은 그림이 214장 남아 있는 것이다.

| | 기하 | 확통 |
|---|---|---|
| 그림 전체 | 165장 | 90장 |
| └ 문제에 붙은 것 | 33장 | 8장 |
| └ **미귀속** | **132장** | **82장** |

미귀속 그림이 '줄 번호로 어디에 남았나' 로 가른 결과다.

| 갈래 | 기하 | 확통 | 합 | 정체 |
|---|---|---|---|---|
| 문제없는쪽 | 85 | 60 | **145 (68%)** | 그 쪽에서 문제를 하나도 안 뽑았다 |
| 문제사이 | 45 | 21 | **66 (31%)** | 대부분 예제 풀이 그림 |
| 문제뒤 | 2 | 1 | 3 (1%) | |
| 문제앞 | 0 | 0 | 0 | |

**짐작이 틀렸다 — 채점 범위 밖 단원의 그림이 아니다.** 전부 OCR 한 쪽에서 나온 것이다.
쪽 자체가 문제를 안 담은 개념 설명 쪽인 것이다.
기하는 63쪽 중 29쪽, 확통은 68쪽 중 21쪽에서만 문제가 나왔다.

**흘린 건 없다.** `문제사이` 66장 중 앞 문제에 200px 안으로 붙은 것은 기하 6장뿐이고,
그 6장을 원본 줄로 확인해 보니 **전부 예제 풀이 그림**이었다.
바로 앞 글줄이 `또 그래프는 오른쪽 그림과 같다` / `답 풀이 참조` 이고
바로 뒷 글줄이 다음 `문제 N` 이다. 안 넣는 게 맞다.
확통은 200px 안이 0장이다 (중앙값 576px).

⚠️ **단, 145장을 전부 개념 그림으로 단정하면 안 된다.**
문제를 못 뽑은 쪽에는 §5 의 **미검출 28건(표기 없는 서술형)** 도 섞여 있다.
그 28건을 나중에 잡으면 145장 중 일부는 문제 그림으로 옮겨간다.

**결론 — DOCX 는 문제 그림을 잃고 있지 않다.** 지금 빠지는 214장은 개념 설명·예제 풀이 그림이다.
미검출 28건을 잡기 전에는 이 숫자를 더 좁힐 수 없다.

**1차 출시 방향 — 2026-08-05 사용자가 확정했다**

- 대상은 **학원** (배포용 문제집). 학교 선생님용이 아니다.
- 1차는 최소 버전: **웹 편집기 + PDF 내보내기만.** 편집하려면 우리 사이트에 들어온다.
- **DOCX 는 재운다.** 코드는 지우지 않는다. 수식 파서(latex2omml)는 셋이 같이 쓴다.
- 후속 내보내기는 **HWPX** (한/글 표준 형식). 조사 결과는 §11.
- "PDF 여도 편집되면 문제 없다" — 사용자가 확인함.

**남은 할 일 — 순서는 사용자가 정한다**

1. **누락 28건** — 표기 없는 서술형. 룰로는 한계가 드러났으므로
   LLM 분류기(문제/비문제 이진 판정)를 붙일지 결정해야 한다. 모델·단가 확인 필요.
   편집기가 생기면 1차에서는 '누락은 사용자가 직접 추가'로 넘길 수도 있다 (문제 추가 기능 필요).
2. **그림 오첨부 0% 로 좁히기** — 본문 그림이 문제에 딸려 들어가는 것을 어떻게 가를지.
   이것도 편집기의 '그림 떼기'로 1차에서는 수동 보정 가능.
3. ~~DOCX 결과물 눈으로 검수~~ — 완료. 수식 편집까지 확인됐다 (§10).
   DOCX 의 그림 표시 문제(한/글 전체·워드 일부 안 보임)는 **DOCX 를 재우면서 함께 보류.**
   원인 가르기용 `out/그림시험.docx` 와 `scripts/img_probe.py` 는 남겨 둬다.

**수식 편집 가능성 — 2026-08-04 에 OMML 로 확정했다**

| 방식 | 수식 | 조판 제어 | 비용 | 상태 |
|---|---|---|---|---|
| Mathpix DOCX 받아서 후처리 | Mathpix 가 해결 | 낮음 | 재호출 과금 | 안 씀 |
| **python-docx + LaTeX→OMML** | **편집 가능** | 완전 | 0원 | **현재 방식** |
| 수식을 이미지로 박기 | 편집 불가 | 완전 | 0원 | 못 옮기는 줄만 |

`scripts/latex2omml.py` 를 직접 만들었다. 외부 패키지 없음, Mathpix 재호출 없음.
교재에 나오는 문법을 세어 보니 스무 가지 남짓이면 다 덮여서 그것만 정확히 옮긴다.

**모르는 문법을 만나면 그 줄만 그림으로 되돌린다.** 틀린 수식을 내보내느니 그림이 낫다.
실측 — 수식 436줄 중 **433줄(99.3%) 변환**, 못 옮긴 3줄은 `\begin{aligned}` 여러 줄 수식이다.

| | 기하 | 확통 |
|---|---|---|
| 편집 가능한 수식 | 168개 | 61개 |
| 남은 그림 | 33장 | 8장 |

기하는 그림이 158장에서 33장으로 줄었다. 줄어든 125장이 수식이었다.
같은 파서에서 워드용(OMML)과 브라우저용(MathML)을 함께 뽑으므로 미리보기와 DOCX 가 어긋나지 않는다.

**확인 완료 (2026-08-05):** 수식은 편집 가능한 상태로 열린다. 이 프로젝트 최대 미확인 항목이 통과됐다.
단 DOCX 의 그림이 한/글에서는 전부, 워드에서는 일부 안 보인다. 파일 자체는 정상이라
(PNG 8bit RGB / 크기·ID·연결 전부 검증) 뷰어별 마크업 해석 문제로 보인다. DOCX 보류로 함께 잠들었다.

**DOCX 실물 검수 — 2026-08-04. 사용자 지적 두 가지를 고쳤다**

서버에 LibreOffice 를 깔아 DOCX 를 PDF 로 그려 놓고 눈으로 확인했다.
추측하지 말고 이 방법을 쓸 것.

```
soffice --headless --convert-to pdf --outdir <폴더> out/기하_문제집.docx
```

| 지적 | 진짜 원인 | 처리 |
|---|---|---|
| 문장 맨 앞에 글머리기호 같은 특수문자 | **워드 수식의 빈 칸** | 빈 칸을 아예 안 만든다 |
| 문제 사이 간격이 없다 | 여백이 머리글 앞 10pt 뿐 | 3줄(36pt)로, 앞 문제 **뒤**에 |

**빈 칸이 상자로 보이는 것이다.** `₄Π₂` 를 옮길 때 밑이 빈 첨자가 만들어졌고
워드·한/글·LibreOffice 모두 빈 칸을 **네모 상자**로 그린다. 그게 `❑₄Π₂` 로 보였다.
앞첨자 전용 구조(`m:sPre`)로 접고, 남는 칸은 보이지 않는 글자로 메웠다.
절댓값 `|AC|` 도 낱글자로 두면 뷰어가 나눗셈으로 잘못 읽어 `¿AC∨¿` 가 나왔다. 괄호 구조로 바꿨다.

이제 **어떤 칸이든 비면 그 줄은 그림으로 되돌린다.** 상자를 내보내느니 그림이 낫다.
이 안전장치를 걸어도 변환 수는 그대로다 (기하 168개 / 확통 61개, 그림도 33·8장 그대로).

**문제 사이 여백은 앞 문제 뒤에 붙인다.** 다음 문제 앞에 붙이면 그 문제가 다음 단으로
넘어갈 때 여백이 단 꼭대기에 남아 단마다 시작 높이가 달라진다. 뒤에 붙이면 단 끝에서 사라진다.
문제가 단 경계에서 쪼개지지 않게 문단마다 붙임 표시를 다시 계산한다 (`keepNext`/`keepLines`).
HTML 미리보기도 같은 값(`PROBLEM_GAP_PT`)을 쓴다.

**끊긴 문장 잇기 (merge_lines)**

원본은 한 쪽을 두 단으로 짜 놓아 문장이 중간에서 끊긴다.
그 끊김을 그대로 옮기면 우리 단 너비와 안 맞아 문장이 엉뚱한 데서 잘린다.
Mathpix 가 줄마다 주는 이어짐 표시(`continues_line_space` / `_no_space`)로 문장을 다시 붙인다.
`continues_line_newline` 은 소문항 (1) (2) 이므로 문단을 나눈다.

⚠️ **이음매 띄어쓰기는 Mathpix 표시를 그대로 따르는데 그게 가끔 틀린다.**
`지수를 사` + `용하여` 를 띄어 이으라고 준다. Mathpix 자체 출력(`result.mmd`)도 똑같이 틀리므로
더 나은 근거가 없다. 조사 한 글자로 시작하는 이음매(`변화` + `를 보이는`)만 규칙으로 바로잡았다 — 7건.

**덧붙였던 선은 다 뺐다.** 문제 사이 구분선과 답 쓸 빈 줄(밑줄)은 우리가 넣은 것이라 제거했다.
`--answer-lines` 옵션도 같이 없앴다.

---

## 10. 확인 필요

- ~~한/글이 워드 수식(OMML)을 편집 가능한 상태로 여는가~~ — **확인됨. 편집된다** (2026-08-05).
- LLM 모델·단가 (룰로 안 되는 부분에만 쓸 계획)
- SSE 스트리밍 미작동 원인 (진행률 표시에 필요 — 웹 서비스 만들 때 같이)
- 페이지당 실제 처리 시간 (워커 개수 산정)
- Vultr 월 요금
- 저작권 정책·이용약관 — 학원 배포용이라 더 중요해졌다
- 서버 SSH 보안 설정 (서비스 직전 일괄 점검)
- **새 PDF 실전 OCR 경로** — 지금까지는 한 번 받아 둔 lines.json 만 다시 읽었다.
  업로드 → Mathpix 호출 → 저장까지 자동으로 이어지는 경로는 아직 서비스 형태로 검증 안 됐다.

---

## 11. HWPX 조사 결과 (2026-08-05) — 후속 내보내기용

**`.hwp` 직접 생성은 하지 않는다. `.hwpx` 를 만든다.**

| | `.hwp` (5.0) | `.hwpx` |
|---|---|---|
| 속 구조 | 이진 + 압축 | **ZIP + XML** (OWPML, 국가표준 KS X 6101) |
| 난이도 | 매우 어려움 | DOCX 만들 때와 같은 계열 |
| 열리는 버전 | — | 한/글 2010 SE+ 열림, **2014 VP 이상 안정** |

- 구조: `mimetype` / `META-INF/` / `Contents/header.xml` / `Contents/section0.xml` / `BinData/`(그림).
  DOCX 와 판박이라 make_docx 번대를 옮겨 쓸 수 있다.
- **수식은 한/글 자체 스크립트다.** 한컴 공식 스펙(수식 rev 1.3)을 확보했다.
  `over` `sqrt` `^` `_` `vec` `bar` `LEFT( RIGHT)` `LSUB`(앞첨자 ₄P₂) `times` `neq` `LEQ` 등.
  latex2omml 파서에 **출력구 하나만 더 달면 된다** (OMML/MathML 처럼).
- 라이브러리 판단: 파이썬 것들(python-hwpx 등)은 알파 단계에 그림·수식 지원이 얕다.
  자바 hwpxlib(Apache-2.0)는 검증됐지만 JVM 이 필요하다. **직접 XML 생성이 맞다.**
- HWPX 는 표준 스키마 검증이 가능해서 구조 오류를 서버에서 걸러낼 수 있다. DOCX 보다 왕복이 적다.
- ⚠️ **문구 의무** — 한컴 공개 문서를 참고해 개발하면 제품·설명서·소스에 이 문장을 넣어야 한다:
  "본 제품은 한글과컴퓨터의 글 문서 파일(.hwp) 공개 문서를 참고하여 개발하였습니다."
- 시작할 때: 한/글에서 2단+수식+그림 든 문서를 `.hwpx` 로 저장한 **견본 파일**을 받아 뚯어보는 게 제일 빠르다.

---

## 12. 웹 통합 — 기존 백엔드에 붙였다 (2026-08-05, v1.40.0)

**서버에 이미 웹 서비스가 있었다.** `/opt/math-workbook-backend` 도커 스택.
텍스트레이어 좌표로 문제를 찾아 **그림으로 오려내는** 제품이 업로드→큐→크롭 수정→A4 PDF 까지
돌아가고 있었다 (자체 화면 + 게스트 세션, https://158-247-240-59.sslip.io).
사용자 결정: mathocr(편집 가능 추출)를 **이 스택에 통합**한다. 두 경로가 한 서버에 공존한다.

| | 기존 크롭 경로 | mathocr 경로 (신규) |
|---|---|---|
| 대상 | 텍스트레이어 있는 PDF | 스캔본 |
| 산출물 | 문제 그림 조각 → A4 PDF | **편집 가능한 글·수식** → 2단 미리보기 HTML |
| 주소 | `/v1/jobs`, `/` | `/v1/mathocr/jobs`, **`/mathocr`** |

**코드 위치** — 원본은 계속 이 저장소(math-workbook-api)다.
백엔드의 `app/mathocr/vendor/` 는 extract.py·latex2omml.py·make_docx.py 의 **무수정 복사본**.
고칠 것이 있으면 여기서 고쳐 채점기로 검증한 뒤 vendor 로 복사한다. vendor 에서 직접 고치지 말 것.
글루 코드는 `app/mathocr/` (mathpix 클라이언트 / service / tasks / routes / 화면).

**비용 규칙 (구현됨)** — ① 같은 파일(sha256)을 전에 읽었으면 재사용 ② 측정 자료(runs/, 이름+쪽수 일치)
재사용 ③ 둘 다 아니면 `MATHOCR_LIVE_OCR_ENABLED=true` 일 때만 Mathpix 호출. **기본 꺼짐.**
Mathpix 키는 백엔드 .env 에 넣어 뒀다. 측정 자료는 compose 가
`/root/mathocr/stage0_out/runs` 를 읽기 전용으로 마운트한다.

**끝까지 시험했다 (2026-08-05)** — 게스트 세션 → 기하 01 샘플 업로드 → 3초 만에 완료
(캐시 재사용, 문제 24 / 수식 53 / 그림 1) → 미리보기 HTML 정상. 낯선 PDF 는
"유료 OCR 꺼짐" 안내와 함께 거절되는 것도 확인.

**편집기 1차 — 완료 (2026-08-05, v1.41.0)**

추출 결과가 **편집 문서(mathocr-doc.json)** 로 저장되고, 편집기·미리보기가 전부 이 문서에서 나온다.
나중의 PDF 내보내기도 같은 문서를 쓴다. 문서 규칙은 백엔드 `app/mathocr/document.py` 맨 위 설명.

- 화면: `/mathocr/edit#<작업번호>` — 지우기(복원 가능)·순서(끌기/버튼)·글 고치기(원문 편집,
  수식은 \( \) 그대로, 즉시 재렌더)·그림 떼기·**그림 창고에서 붙이기**(미귀속 그림 47장도
  오려 둔다)·문제/단락 추가·자동저장(1.2초 디바운스).
- 미리보기는 항상 편집 문서에서 새로 그린다. 편집 전후 조판 CSS 가 같다.
- 서버 실측: 문서 받기 2.6MB(그림 데이터 포함) / 저장·재렌더 즉시 / 삭제·수정·붙이기·추가 반영 확인.

**PDF 내보내기 — 완료 (2026-08-05, v1.42.3)**

편집 문서를 **미리보기와 같은 HTML 그대로 크로미엄이 인쇄**한다 (`POST /v1/mathocr/jobs/{id}/pdf`,
편집기의 「PDF 받기」). A4·2단·문제 간격·수식·그림까지 화면과 동일함을 래스터로 확인했다.
크로미엄은 도커 이미지에 있고 메모리 때문에 한 번에 한 건만 돈다(잠금). 한 단원 변환 2초.

이 과정에서 잡은 수식 표시 결함 세 가지 — **전부 브라우저 미리보기에도 적용되는 것들이다**:

1. **결합 문자 화살표는 브라우저가 못 그린다.** `\vec{a}` 의 U+20D7 을 MathML 에 그대로 얹으면
   빈 칸이 된다. 화면용(MathML)은 보통 화살표(→)로 바꿔 얹는다. 워드용(OMML)은 결합 문자가 규격.
2. **수학 이탤릭 글자는 수학 글꼴이 있어야 보인다.** 𝑎·𝑥 는 전용 유니코드 영역(U+1D400대)을 쓰는데
   Noto CJK 에 글리프가 없어 통째로 사라졌다. 서버에 fonts-lmodern·fonts-stix 를 넣어 해결.
   **사용자 PC 브라우저에는 보통 있던 글꼴이라 미리보기에서는 안 드러났던 문제다.**
3. `~` 는 LaTeX 에서 공백인데 물결로 찍히고 있었다 (`10~m/s`). 공백으로 되돌림.
   덤: 본문 속 분수를 교과서처럼 크게 펴는 CSS(`math-style:normal`) 추가.

**유료 OCR 실전 시험 — 완료 (2026-08-05)**

스위치를 잠깐 켜고, 캐시에 없는 「3-3 원의 방정식」(12쪽, 다른 출판사 수학상)을 실제 과금으로 돌렸다.

| 항목 | 결과 |
|---|---|
| 경로 | 업로드 → Mathpix 제출 → 폴링 → 추출 → 편집 문서 → PDF 까지 **끝까지 통과** |
| 시간 | 전체 15초 (OCR 11초). 12쪽이므로 **페이지당 약 1초** (작은 파일 기준, 확인 필요: 큰 파일) |
| 비용 | 12쪽 × 7.5원 = 약 90원. **같은 파일 재업로드는 6초·0원** (sha 캐시 동작 확인) |
| 스위치 | 시험 후 다시 껐다. 끈 뒤: 낯선 파일 거절 / 읽어 둔 파일은 계속 무료 처리 — 둘 다 확인 |

**낯선 책 품질 — 예상대로 표기가 다르면 적게 잡힌다.** 이 책은 문제 번호가 `01 02…` 형태라
두자리 패턴으로 5개만 추출됐다(전부 경계·수식 깨끗). 신사고 밖 교재는 표기 목록 보강이나
LLM 분류기 없이는 회수율이 낮다. 잡은 것의 품질은 좋았다.

**다듬을 것 (소소)** — ① 추출 후 발문 앞에 `. ` 부스러기가 남는 경우 제거
② 화면에 보이는 표기 이름 `두자리` 를 `문제` 로 순화.

**Mathpix 재과금 — 실전 테스트로 확정 (2026-08-05)**

같은 파일을 다시 제출하면 **전액 다시 과금된다.** 사용량 API(`GET /v3/ocr-usage`)로 전후를 쟀다:
12쪽 파일 재제출 후 오늘 사용량 12 → 24쪽. 중복 제거 없음.
반대로 **결과 다시 내려받기·상태 조회는 무료다** (수십 번 조회에도 숫자 불변).
→ 그래서 '품질 나쁘면 자동 재요청' 은 하지 않는다. 대신 **받은 직후 품질 검사**
(쪽수 일치 확인)를 넣었고, 불량이면 결과를 치워 두고 실패로 알린다 (자동 재시도 없음).

**계정·크레딧 — 완료 (2026-08-05, v1.43.0)**

무료 체험 확정안 그대로다. 크레딧 단위는 '쪽'.

| 신분 | 규칙 |
|---|---|
| 게스트 | 앞 5쪽만 처리(화면 확인용), PDF 내려받기 불가. 이미 읽어 둔 파일은 전체 표시(비용 0원이라) |
| 회원 | 가입 시 10쪽 무료(1회), 만들 때 쪽수만큼 차감, **같은 파일 재작업 0쪽**, 실패 자동 환불 |
| 관리자 | `POST /v1/admin/credits` + X-Admin-Key(.env ADMIN_CREDIT_KEY)로 수동 충전 — PG 전 단계 |

계정은 sqlite(`/data/jobs/_accounts.db`, 볼륨)에 산다. IP 당 하루 가입 3회 제한.
서버에서 9개 시나리오(402/충전/차감/재작업 0원/환불 준비/게스트 잘림/PDF 403) 전부 통과.

**이메일 인증 — 완료 (2026-08-05, v1.44.0)**

- 가입 → 6자리 코드(10분 유효, 시도 5회, 재발송 1분 1회) → 인증하면 **그때 10쪽 지급**.
  가입 반복으로 무료 쪽수를 긁는 구멍이 막혔다. PDF 내려받기도 인증한 회원부터.
- **메일 발송(SMTP)은 설정만 비어 있다.** Gmail 앱 비밀번호를 받아 .env 에
  `SMTP_HOST=smtp.gmail.com` `SMTP_USER` `SMTP_PASSWORD` `SMTP_FROM` 넣고 재배포하면 자동 발송.
  그전까지는 관리자가 코드 조회(`GET /v1/admin/verification-code`)로 수동 안내한다.
- 서버 시나리오 8종(미지급→조회→오답→인증→10쪽→중복지급 차단→재발송 차단) 통과.

**아직 없는 것 (외부 계약·계정 필요)** — ① SMTP 계정(앱 비밀번호 한 번 만들면 끝)
② Turnstile(클라우드플레어) ③ **PG 실연동** — 절차는 사용자에게 안내함(사업자등록·통신판매업 신고 필요).
그전까지 판매는 입금 확인 후 수동 충전.

**품질 점검 — 2026-08-06, 15개 파일 전수 (`scripts/quality_check.py`, 결과 grade_out/QUALITY.md)**

LLM 분류기 결정 전에 지금 결과물 상태를 수치와 눈으로 쟀다. 총 248문제 기준:

| 지표 | 값 | 판단 |
|---|---|---|
| 수식 변환 | 458/463 (98.9%), 그림 대체 5 | 좋음 |
| 오독 위험(저신뢰<0.6 줄) | 9줄 — 대부분 표(tabular)·긴 발문 | 낮음 |
| 발문 부스러기 | 0 (청소 로직 추가로 해결) | 해결 |
| 빈·한줄 문제 | 20 — 라벨만 남은 카드 | **빈 문제는 '지운 상태'로 시작하게 처리** (편집기에서 복원 가능) |
| 화면 표기 이름 | `두자리 1` 노출 | `문제 1` 로 순화 (display_name) |

타 출판사 3권을 조판 렌더로 직접 검수 — 전반 판매 가능 수준. 남은 흠:
빈칸(□) 수열 조각 흩어짐(등차 문제1, 원본이 표 조판), 탐구 만화가 문제에 딸려 들어간 것
(지수 4쪽, 알려진 오첨부 유형), 탐구/활동이 앞 문제에 흡수되는 경계 사례.

**LLM 분류기 모델·단가 조사 — 2026-08-06 (웹 시세, 공식 페이지 재확인 필요)**

판정량 실측: 문제 못 찾은 쪽이 263쪽 중 151쪽(57%), 60쪽 책 환산 **판정 대상 ≈34쪽·4만 자
≈ 입력 5만 토큰/권** (지시문 포함), 출력 소량. 권당 원가:

| 모델 | 입력/출력 ($/1M) | 권당(60쪽) | 비고 |
|---|---|---|---|
| Gemini 2.5 Flash-Lite | 0.10/0.40 | ≈8원 | 10월 퇴역 예정 — 제외 |
| **GPT-5.6 Luna** | 0.20/1.20 | **≈18원** | **1순위 — 백엔드에 OpenAI 키·클라이언트 이미 있음** |
| GPT-5.4 nano | 0.20/1.25 | ≈18원 | |
| Haiku 4.5 | 1/5 | ≈86원 | Anthropic 키 신규 필요 |
| Gemini 3.6 Flash | 1.50/7.50 | ≈129원 | |
| Sonnet 5 | 2/10 (9월부터 3/15) | ≈173원 | |

판매가 1,800원/권(60쪽×30원) 대비 Luna 1%, Sonnet 급도 10% — **원가는 결정 요인이 아니다.**
정확도가 결정 요인이고, 우리는 잴 수 있다: 신사고 미검출 28건(정답=문제) + 개념 문단(정답=아님)으로
시험셋을 만들어 모델 2~3개를 몇백 원에 실전 테스트 → 그 결과로 확정.

**분류기 정확도 실전 테스트 — 2026-08-06 (`scripts/classifier_bench.py`) → 모델 확정: gpt-5.6-luna**

시험셋 62건을 기준 파일로 자동 라벨했다. 양성 = 표기 문제 16 + 표기 없는 서술형 22
(트라이그램 포함률 0.55 이상으로 기준과 대조), 음성 24 = 예제 풀이 블록·'~라고 한다' 정의 문단
(매칭에 기대지 않고 정의상 확실한 것만 — 매칭 기반 음성 라벨은 우연 겹침으로 오염돼서 버렸다).

| 모델 | 정확도 | 표기 없는 서술형 | 비고 |
|---|---|---|---|
| **gpt-5.6-luna** | **60/62 (96.8%)** | **22/22 만점** | 최저가 티어. 틀린 2건은 시험셋 라벨 흠(음성 블록에 다음 문제 머리 혼입)이라 사실상 만점 |
| gpt-5.4-nano | 57/62 | 18/22 | '축구 경기…' 등 핵심 미검출을 놓침 |
| gpt-5.6-terra | 56/62 | 18/22 | 더 큰 모델인데 더 나쁨. 출력만 2배 |

실측 비용: 62건 판정 = 입력 14k 토큰 ≈ **5원**. 권당 추산(18원)과 일치.
결론 — **luna 확정.** 다음 연동 때: 판정 대상은 '문제 못 찾은 쪽'의 문단, UNMARKED 후보 생성
+ luna 판정 조합. 기대 효과는 미검출 28건 대부분 회수 (채점기로 확정할 것).

**luna 분류기 연동 — 완료 (2026-08-06, 백엔드 v1.45.0)**

구조: 느슨한 후보 생성(UNMARKED 룰) → **묻기 전 룰 배제**(탐구·공학 활동, 문장 조각)
→ luna 판정 → 통과만 채택. `scripts/llm_filter.py` (판정문은 벤치와 공유 — 고치면 재벤치).
채점기 스위치 `GRADE_LLM=1` 로 언제든 효과 재측정 가능.

| 채점 실측 | v9 | **v9+luna** |
|---|---|---|
| 완전일치 (기하/확통) | 54 / 37 | **60 / 44 (+13)** |
| 오검출률 | 1.6% / 0% | 2.7% / 2.0% (목표 5% 안) |
| 남은 오검출 | — | 기하 2(빈 예제·혼합 블록), 확통 1(오독 코너) |

첫 시도에서 오검출이 9건으로 튀어(탐구 소문항 `밤구 (1)`, 공학 활동, 문장 조각)
**룰 사전 배제 + 판정문에 활동 명시**로 잡았다. 판정문 수정 뒤 벤치 재확인(95.2%).
비용: 판정은 '지문' 후보만(표기 문제는 안 물음), 권당 약 10~20원. 실패 후보는 버린다.
서비스 확인: 확통 03 이 12→18문제, 진행률에 "AI 가 문제 후보를 가리는 중 (i/n)".
§5 의 미검출 28건 문제는 이것으로 대부분 회수됐다 (완전일치 +13, 나머지는 반쪽 회수=잘림).

**HWPX 내보내기 — 구현 완료, 한/글 실물 확인 대기 (2026-08-06, 백엔드 v1.46.0)**

§11 계획대로 **직접 XML 조립**으로 만들었다 (`app/mathocr/hwpxexport.py`).
- 밑판: hwpxlib(Apache-2.0)의 빈 문서. 수식(hp:equation)·그림(hp:pic)·2단(colPr) 요소는
  한/글이 실제로 저장한 견본 파일에서 구조를 채집해 그대로 본떴다.
- 수식: latex2omml 에 **세 번째 출력구 `to_hwp_script()`** 를 달았다 (over·sqrt·^ _·vec·
  bar·LEFT RIGHT·LSUB). 미리보기·PDF 와 같은 나무라 어긋나지 않는다.
- 못 옮기는 수식은 원문 글자로 남긴다 (한/글에서 고칠 수 있게).
- 검증: mimetype 규격(무압축 선두), 저장 전 XML 자체 검사, python-hwpx 재독
  (웹에서 받은 파일 205KB, 문단 145개 정상). **한/글(2014 VP+)에서 여는 실물 확인은 사용자 몫.**
- 편집기 「한글(HWPX) 받기」(인증 회원). 한컴 공개 문서 참고 문구를 화면·문서·소스에 게시.
- 덤: 가입 IP 제한(하루 3회)이 내 시험 계정 생성을 막았다 — 남용 방어 실동작 확인.

**한/글 2016 실물 확인 1차 — 실패, 원인 잡고 재배포 (v1.46.2)**

증상: 미리보기 글자 깨짐 / 본문 글자 전무 / 그림 외 전부 검은 배경.
원인: 밑판으로 쓴 '빈 문서'가 한/글이 아니라 도구(hwpxlib)가 만든 것이었다
(version.xml 이 자백). **밑판은 반드시 한/글이 실제 저장한 파일이어야 한다** —
SimpleEquation 견본에서 내용만 비운 것으로 통째 교체했고, 미리보기용
Preview/PrvText.txt(UTF-16LE)도 우리 내용으로 새로 쓴다. 사용자 재확인 대기.

**한/글 2016 실물 확인 2차 — 두 원인 더 잡음 (v1.46.3)**

| 증상 | 원인 (진품 대조로 확정) | 처리 |
|---|---|---|
| 글자가 전부 겹쳐 보임 | 진품은 모든 문단(127/127)에 줄 배치 정보(linesegarray)가 있는데 우리 문단엔 없었다 | 전 문단에 삽입 |
| 미리보기 전부 깨짐 | PrvText.txt 진품 인코딩은 **UTF-8** (UTF-16 으로 쓰고 있었다) | UTF-8 로 |

**3차 확인 — 바코드 증상 → 원인 확정 (v1.46.4)**: 진품은 문단의 '화면 줄 수만큼'
lineseg 를 갖는다 (실측: 81자 문단 → 2개, 둘째 줄 textpos=44, flags 첫줄 1441792/다음 393216).
문단당 1개만 넣으면 긴 발문이 한 줄 폭에 짓눌린다. **줄바꿈을 계산해 줄마다 lineseg** 를
만들도록 재작성 (전각 1000·반각 500 hwpunit 대략치, 개체는 글자 1개로 셈).

HWPX 교훈 넷: ① 밑판은 한/글이 저장한 진품이어야 한다 ② lineseg 는 문단당이 아니라
**줄당** ③ PrvText 는 UTF-16LE+BOM 이 신구 호환 (최신 한/글은 UTF-8 로 쓰지만 2016 은 UTF-16 세대)
④ 증상은 실물이 아니면 못 잡는다 — 한/글 확인 왕복이 유일한 길.

**4차 확인 — '손상/변조' 경고 + 그림 실종 + 미리보기 CP949 물증 (v1.46.5 대응)**
- 미리보기 깨짐 '怨좊벑 湲고븯' = UTF-8 을 CP949 로 읽은 전형 → **PrvText 는 CP949** 로.
- 경고가 뜨면 한/글이 보안 모드로 열어 그림 같은 개체를 막는다 — 그림 실종은 부수 효과 추정.
- 압축 항목 순서를 진품 실측 순서와 동일하게 맞춤 (손상 판정 용의점 제거).
- 다음 왕복에서 물을 것: 경고가 이전 판에도 있었는지 / '보안 낮음'으로 열면 어떻게 보이는지.

**5차 — 대조 실험으로 결판 (2026-08-05)**
진품 hwpx 2개(한/글 9.x·11.x 가 직접 저장) + 5방식 실험 파일을 보냈다.
**셋 다 사용자 한/글 2016 에서 비정상** → 우리 조립이 아니라 **2016 의 hwpx 읽기
한계로 확정**. 한컴 FAQ 의 '2014 VP 이상 안정'은 실물과 다르다(패치 상태 영향 추정).
서비스 안내는 **한/글 2020 이상 권장**으로 고침(v1.46.6).

**6차 — 한/글 2020 에서 성공 확정 (2026-08-05, v1.47.0)**
진품·실험·변환 파일 모두 정상, **수식 더블클릭 → 수식 편집기 열림 확인**. HWPX 완성.
마무리 손질: ① 글자 서체 함초롬바탕(charPr 1, 전 버전 기본 내장) ② 수식 든 문단은
모든 줄 높이를 가장 큰 개체에 맞춰 '줄겹침 종종' 제거 ③ 미리보기 깨짐 재보고는
**이전에 받아 둔 파일** 탓이었음(서버 최신 파일은 CP949 정상 확인) — 새로 받으면 해결.
결론: **HWPX 는 한/글 2020 이상 지원.** 2016 이하는 hwpx 읽기 자체가 불안정(대조 실험으로 확정).

**7차 — 두 오진의 전모 (v1.47.1). '크롬이 중국어 번체로 인식' 한마디가 다 풀었다**
- 닷새 내내 쫓던 '미리보기 깨짐'의 진짜 범인은 한/글 파일이 아니라 **웹 미리보기 HTML 의
  인코딩 선언 누락**이었다. 크롬이 인코딩을 추측하다 한국어 UTF-8 을 중국어 계열로 읽으면
  정확히 '怨좊벑' 깨짐 + 번체 번역 제안이 나온다. `<meta charset>` 추가로 종결.
- 그 오진 때문에 PrvText 를 UTF-16→CP949 로 바꿨었는데, **한/글 2020 은 UTF-8 이 아닌
  PrvText 를 '손상/변조'로 판정**한다 (경고가 그 버전들에서만 떴다는 증언과 정확히 일치).
  UTF-8 복원으로 손상 경고 종결.
- 교훈: **증상이 어느 화면에서 났는지부터 물을 것.** '미리보기'라는 말이 웹 화면과
  한/글 파일 미리보기 둘 다를 가리켜 닷새를 돌았다.

**8차 — 성분 분해로 종결 (2026-08-05)**
같은 문제집을 성분별 4개(A 글만/B 글+수식/C 글+그림/D 전체)로 나눠 한/글 2020 확인:
**A·C 만 팝업, B·D 는 정상.** D = 지금 서비스가 만드는 구성 그대로 → **실서비스 산출물은
정상 확정** (PrvText UTF-8 복원이 유효했고, '여전히 손상' 보고는 이전에 받아 둔 구판 파일).
잔여 수수께끼: **수식이 하나도 없는 문서**(A·C 조합)에서만 팝업 — 수학 문제집 실사용에는
해당 없음. 원인 미상(수식 요소가 있으면 통과하는 상관만 확인). 실사례가 나타나면 그때 판다.
확인용 명령: 분해 세트 생성 스크립트는 이 대화 기록 참고, 진품 대조 파일은 hwpxlib testFile.

**다음 단계** — ① 사용자: 새로 받은 .hwpx 재확인 (겹침 해소·미리보기·수식 더블클릭)
② PG 계약 진행(사용자) 후 결제창 연동 ③ 잘림(반쪽 회수)의 경계 다듬기.

### 2026-08-05 — 표 노출·손상 팝업 마무리 (backend 1.48.0)
- **표(`\begin{tabular}`)는 무조건 그림으로.** 표 뼈대는 수식 구분자 밖에 있어
  변환 가능 검사를 통과해 버렸고, 미리보기에 LaTeX 원문이 노출됐다
  (실물: 신사고 확통 06 확률분포표). make_docx 원본과 backend document.py 에 같은 규칙 적용.
- **hwpx 줄 배치 정보(linesegarray) 전면 제거.** 2016 겹침 대응용 손계산 값이었는데
  지원 기준이 한/글 2020+ 로 확정됐고, 2020 은 배치 정보 없이 완벽히 그린다는 것을
  실물(시험3 방식3)로 확인했다. 파일에서 가장 진품답지 않은 부분 = 손상 판정 용의자라
  제거가 곧 팝업 대응이기도 하다. → 사장님 한/글 2020 확인 대기.
- 이미 만들어진 문서(기존 작업)는 옛 규칙이라, 같은 파일을 다시 올려야 새 규칙이 적용된다
  (읽어 둔 파일이라 무료).

### 2026-08-05 — 수식 상자 크기·옛 문서 표 원문 (backend 1.48.1)
- 손상 팝업은 linesegarray 제거로 잡혔다고 사장님 확인.
- 수식 상자(hp:sz)를 스크립트 글자 수로 어림하던 것을 '눈에 보이는 글자 수' 기준으로
  바꿨다 (명령어 제외, 분수·근호는 높이 가산). 옛 방식은 P(X=x)를 절반 폭으로 잡아
  이웃 글자와 겹쳤다 (실물 확인). 상자는 과대치가 안전하다 — 편집하면 한/글이 다시 잰다.
- 표 원문이 남아 보인 것은 재업로드가 아니라 **기존 작업을 다시 연 것**이 원인
  (옛 규칙으로 만든 문서가 그대로). 대응 둘: ① 렌더 양쪽(미리보기·hwpx)에 마지막 그물
  — 표 원문은 안내문으로 치환, ② 사장님의 확통 작업(8f4ccb43)은 서버에서 새 규칙으로
  제자리 재생성해 둠 (표 원문 0, 소유권 10001 복원).

### 2026-08-06 — 수식 상자 나무 계산·문제 표시 서식 (backend 1.48.2)
- 수식 상자: 사용자 실물 기준(수식 편집기 여닫으면 딱 맞춰짐)에 맞춰 hwpxexport 에
  _measure() 추가 — L.parse 나무를 걸으며 분수는 위아래 쌓기(폭은 큰 쪽), 첨자 0.65배,
  루트/괄호/윗기호 가산. 연산자 780, 라틴 520, 전각 1000 hwpunit (10pt 기준 어림).
- 문제 표시(문제 N (원본 N쪽)): 밑판 charPr 1번을 복제한 8번(9pt 굵게 #2F5D8A,
  미리보기 .label 과 동일색)을 header.xml 에 주입, 라벨 문단만 charPrIDRef=8.
- 접속 주소는 https://158-247-240-59.sslip.io/mathocr (뿌리 주소는 옛 화면).

### 2026-08-06 — 수식 상자는 크로미엄 실측으로 (backend 1.48.3)
- 시험 파일(eqbox.hwpx, 방법 1·2·3) 실물 확인 결과로 확정:
  **한/글은 수식 상자(hp:sz)를 불러올 때 다시 재지 않는다.**
  크기를 빼면 기본값이라 너무 크고(방법1), 0이나 계산값이 실물보다 작으면
  수식이 이웃 글자를 덮는다(방법2·3). 정확한 크기를 적어 주는 수밖에 없다.
- 손계산(_measure)은 세 번 다 어긋났으므로 접었다(크로미엄 없을 때 예비로만).
  **미리보기와 같은 MathML 을 크로미엄으로 실제 그려 픽셀 크기를 잰다**
  (pdfexport.measure_math_sizes, --dump-dom + document.fonts.ready).
  잴 때 math 요소 자체를 재야 한다 — 감싼 글상자를 재면 글꼴 줄높이가 섞여
  한 글자 수식도 한 줄 높이로 나온다 (실측). px→hwpunit 은 ×75 (96dpi),
  여유 8% + 150 (HYhwpEQ 가 더 넓게 그릴 수 있어 살짝 크게 — 남으면 빈틈,
  모자라면 겹침). 문서의 수식을 모아 한 번에 재므로 내보내기당 +1초.
- 진품 견본(hwpxlib SimpleEquation)의 크기값은 내용 대비 너무 작아 믿을 수 없다
  — 도구가 스크립트만 바꿔 넣은 파일이라 한/글이 다시 잰 값이 아닌 듯. 보정 기준으로 쓰지 말 것.
- 시험 파일을 새 방식으로 다시 만들어 같은 주소에 뒀다:
  https://158-247-240-59.sslip.io/mathocr/testfile/eqbox.hwpx (확인 1·2 두 문단).
  → **사용자 확인 대기: 겹침이 사라졌는지, 상자가 과하게 크지 않은지.**

### 2026-08-06 — 수식 상자 여유 축소, 4단계 시험 파일 (backend 1.48.4)
- 실물 확인: **겹침은 크로미엄 실측으로 해결됐다.** 남은 지적은 '상자가 수식보다 크다'
  — 한/글 수식 글꼴(HYhwpEQ)이 브라우저 수학 글꼴보다 촘촘하게 그려서 여유분이 빈틈으로 보인다.
- 기본값을 여유 8%+150 → **0%+80 (실측 그대로)** 로 줄여 배포했다.
- 얼마나 더 줄일지는 눈으로만 정할 수 있어서, 같은 문단을 **여유 4단계**(8% / 0% / -8% / -15%)로
  넣은 시험 파일을 같은 주소(eqbox.hwpx)에 뒀다. 생성 스크립트는 판마다 여유 상수를 바꿔
  4번 조립한 뒤 문단 블록을 한 파일로 합친다 (문단 id 는 판마다 1000씩 옮겨 중복 제거).
  → **사용자 확인 대기: 넷 중 가장 좋은 방식 번호.** 답을 받으면 그 값으로 기본값 확정.

### 2026-08-06 — 수식 상자, 한/글의 일괄 재계산 기능으로 보정하기로
- 사용자 관찰이 맞았다: **수식 편집기를 닫으면 한/글이 상자를 딱 맞게 다시 잰다.**
  같은 동작을 문서 전체에 한 번에 시키는 기능이 한/글에 이미 있다 —
  수식 우클릭 → 개체 속성 → 글자 크기 지정 + **적용 범위 '문서 전체'** → 설정.
  (수식 크기 일괄 변경 용도의 기본 기능. 어떤 파일이든 10초면 전부 재계산된다.)
- 파일 안에 '다시 재라'고 표시하는 방법은 없다 — 한/글은 적힌 크기를 그대로 믿는다
  (크기 없으면 기본값 크게, 작으면 겹침 — eqbox 실험으로 확정).
  자동화(pyhwpx 등)는 윈도우 전용이라 서버(리눅스)에서 못 쓴다.
- 그래서 **보정판**으로 계산식 자체를 맞춘다: 서버가 아는 수식 120개(작업 문서 전부 + 문법 견본)를
  11pt(baseUnit 1100)로 심은 calib.hwpx 를 만들었다. 사용자가 10을 지정하면 값이 반드시
  바뀌므로 전부 다시 그려진다. 저장해 돌려받으면 수식마다 한/글의 진짜 크기를 얻는다.
  → 브라우저 실측(px)과 짝지어 변환식을 맞추고 배포. 그다음부터는 처음부터 딱 맞는다.
- 내려받기: /mathocr/testfile/calib.hwpx. 순서 목록은 서버 /data/jobs/_시험/calib_latex.json.
  돌아온 파일은 ~/mathocr/refs/calib.hwpx 로 받기로 함. **사용자 왕복 대기.**

### 2026-08-06 — 보정판 왕복 완료, 수식 상자 회귀 적합 (backend 1.48.5)
- 사용자가 calib.hwpx 를 한/글에서 재계산·저장해 돌려줬다 (첫 회는 원본 그대로가
  와서 해시 대조로 잡아냄 — **돌아온 파일은 반드시 해시부터 대조할 것**).
- 한/글 실측 120개로 회귀 적합: 폭 = 0.56×브라우저px×75 + 특징 항
  (숫자/대문자/소문자/연산자/⊥∥ 1378/벡터화살표 -308/윗줄 -259/절댓값 막대/그리스/
  cos·sin 류 780…) + 여유 500. 높이 = 958 + 분수 1234 + 근호·첨자·악센트 소액
  + 0.035×px + 여유 170. **기준선(baseLine)도 실측이 가르쳐 줌**: 분수 있으면 67,
  없으면 87 (그동안 진품 견본의 61 고정을 쓰고 있었다).
- 참고: 한/글 재저장은 hp:script 에 xml:space="preserve" 를 붙인다 (파싱 주의).
  hwpxlib 견본의 sz 값은 내용 대비 엉터리 — 보정 기준으로 쓰지 말 것.
- 검증(같은 120개): 폭 모자람 최대 106 (0.4pt, outMargin 112 가 흡수), 높이 모자람 0건,
  빈틈 중앙값 537 (약 0.7mm). 시험 파일 eqbox.hwpx 를 보정식으로 다시 만들어 둠.
  → **사용자 확인 대기.** 통과하면 수식 상자 건은 종결.

### 2026-08-06 — '여전히 헐렁' 지적 → 두 갈래 새 방향 (backend 1.48.6)
- 웹 조사: hwpx 를 만드는 공개 도구들(HwpForge 러스트/hwpx-skill 파이썬)도 수식 크기는
  **고정 기본값**(8779×2600)이다. 파일 안에 '크기 다시 재라' 표시는 없다 — 재확인.
- **갈래 1 (배포됨)**: 보정판 실측 120개를 표(app/mathocr/data/eq_sizes_hangul.json)로 싣고,
  표에 있는 수식은 예측 없이 **한/글이 재 준 값 그대로** 쓴다. 크로미엄 측정도 건너뛴다.
  현재 문제집 수식은 대부분 덮인다. eqbox.hwpx 를 실측표 수식만으로 다시 만들어 둠
  ("수식 상자 확인 4") — 이건 오차 0 이어야 한다. **사용자 확인 대기.**
- **갈래 2 (준비됨)**: 새 수식용 조판 규칙 역산. 낱글자('1','a','=')와 겹글자('11'),
  구조 조각(분수·근호·첨자) 프로브 223개를 calib2.hwpx 로 만들어 둠 (11pt 심음).
  같은 왕복(글자 크기 10 문서 전체 → 저장 → scp)이 오면: 겹글자-낱글자 차이로
  낱글자 폭을 직접 얻고(w(cc)-w(c)), 구조 상수를 역산해 회귀를 대체한다.
  순서 목록: /data/jobs/_시험/calib2_latex.json.

### 2026-08-06 — 측정판 왕복 완료: 한/글 조판 규칙 역산 계산기 (backend 1.48.7)
- **확인 4 통과** (실측표 수식 = 오차 0, 사용자 확인). 측정판(calib2)도 돌아왔다.
- 프로브 분석 결과 — 한/글 수식 조판은 완전히 규칙적이다:
  상자 여백 정확히 0 / 낱글자 폭은 75 단위 정수 (숫자 525, 이탤릭 소문자 300~900,
  대문자 375~1050, ⊥ 2025, ≤≥≠∥ 1050) / 분수 = max(분자,분모)+500, 높이 2250,
  기준선 66 / 근호 = 내용+1220, 높이 1123 / 첨자 5/7 크기 (위 1163·아래 1177·둘 다 1365)
  / bar·vec = 내용+105, 높이 1170 / 늘어나는 괄호 = 내용+750 / 관계 기호 앞뒤 +420,
  이항 +− +280 (단항이면 0, 직전 글자로 판정) / 수식 안 공백은 폭 0.
- **주의: \mathrm 은 나무에서 style sty="p" 로 온다** (rm 아님). 로만체 글자 폭은
  이탤릭과 다르다 — 실전판 120개에서 최소제곱으로 역산해 넣었다 (어림 섞임).
- hwpxexport 에 _eq_layout() 계산기를 넣고 **크로미엄 측정·회귀 모형·옛 손계산을 전부
  제거**. 우선순위: 실측표(335개, calib1+calib2 병합) → 계산기 → (파싱 실패면 수식
  자체가 글자로 남으므로 상자 불필요). 내보내기 속도도 +1초 → 즉시로 복귀.
- 검증(실전판 120, 계산기 경로 강제): 중앙값 +150(여유), 범위 [-300,+1200], 모자람 11건
  (최대 300 = 1pt, outMargin 112 가 일부 흡수). 표 명중 시 오차 0.
- 확인 5 (eqbox.hwpx): 일부러 실측표에 없는 새 수식 9개로 만들어 계산기를 사용자 눈으로
  시험 중. → **사용자 확인 대기.** 통과하면 종결. 미세 이탈 보고되면 로만체 폭 프로브
  (\mathrm{AA} 류) 왕복 한 번으로 마지막 어림을 실측으로 바꿀 수 있다.

### 2026-08-06 — 확인 5 '살짝 안 맞음' + 사용자 발견: 값 안 바꿔도 재계산된다
- 확인 5(계산기 경로, 새 수식 9개)는 "살짝 안 맞음" — 예상 범위 (로만체 어림 때문).
- **사용자 발견**: 수식 개체 속성에서 **값을 바꾸지 않고** 적용 범위 '문서 전체' → 설정만
  눌러도 전부 딱 맞게 재계산된다. (이전 가정 '같은 값이면 재계산 안 될 수 있다'는 틀림.
  덕분에 측정판에 11pt 심기 꼼수도 불필요해졌다.) 최종 사용자용 10초 응급 처치로도 안내 가능.
- 남은 어림을 실측으로 바꿀 측정판 3(calib3.hwpx, 프로브 137개)을 만들어 둠:
  로만체(\mathrm) a-z·A-Z 낱/겹글자 104개 + 맥락 띄움(⊥·∥ 관계 위치, =-1 단항,
  vec±vec, 쉼표, ∘′∠△≡, cos류, f(x)) 33개. → **왕복 대기.** 돌아오면
  _EQ_ADV_ROMAN 을 실측으로 교체하고 띄움 규칙 미세 수정, 여유(150) 축소 검토.

### 2026-08-06 — 측정판 3 반영: 조판 계산기 완성판 (backend 1.48.8)
- 로만체(\mathrm·함수 이름) a-z A-Z 폭 52개 전량 실측 교체. **함수 이름(cos 등)은
  나무에서 감싸개 없이 run 자체에 sty="p"** — 이걸 반영하니 cos θ 가 자릿수까지 맞았다
  (함수 뒤 별도 간격은 없다. 이전의 -150 은 로만체 미적용 탓이었다).
- 띄움 규칙 확정: 관계 기호(=<>≤≥≠≡) 피연산자 뒤 +420 / 윗줄 개체·분수 뒤 +210 /
  그 외(첫머리 등) 0. 이항 +−×± 피연산자 뒤 +280 / 첫머리·여는 괄호·윗줄 개체 뒤 +140.
  ⊥∥ 는 띄움 없음(폭 2025/1050 에 포함). 늘어나는 괄호는 여닫이 글자 폭 그대로
  (절댓값 막대 525·중괄호 635). 프라임 315 (첨자에서 실효 225), △1050, ≡910, ∠1050.
- ⚠ 파이썬 함정: **빈 문자열은 어느 문자열에나 `in` 으로 참** (`"" in ")]|"` == True).
  직전 글자 판정이 이것 때문에 조용히 틀리고 있었다. 튜플 비교로 수정.
- 검증 — 실전판 120(계산기 강제): 여유 100 포함 [-210,+500], 표준편차 ~80.
  실측표는 466개 (calib1+2+3 병합, 명중 시 오차 0).
- 확인 6 (eqbox.hwpx): 표에 없는 새 수식 9개 (f′(2)·cos75°·△DEF·분수부등식·
  벡터절댓값·P(0.5≤Z≤1.5)). → **사용자 확인 대기. 통과하면 수식 상자 건 종결.**
- 남는 후보 작업(선택): 편집기 안내문에 '수식 개체 속성 → 문서 전체 → 설정' 10초
  재계산 팁 게시.

### 2026-08-06 — 수식 상자 건 종결 (사용자 확정)
- 확인 6 결과 "거의 변화 없음, 충분함" — **종결.** 최종 상태 = backend 1.48.8:
  실측표 466개(오차 0) + 조판 계산기(새 수식, 대부분 ±1pt).
- 완벽히 딱 맞아야 하는 사용자는 한/글에서 10초 재계산(수식 개체 속성 →
  적용 범위 '문서 전체' → 설정)을 쓰면 된다 — 값 안 바꿔도 재계산된다 (사용자 발견).
- 다음 할 일은 이전 목록 그대로: ① PG 계약(사용자) 후 결제창 연동
  ② 잘림(반쪽 회수) 경계 다듬기 ③ SMTP 계정 연결 ④ Turnstile.

### 2026-08-06 — 잘림(반쪽 회수) 다듬기 완료 (backend 1.48.9)
- 잘림 11건을 원본 줄·기준 전문 대조로 전수 분석. **진짜 추출 잘림은 1건뿐** —
  나머지는 기준 파서(parse_ref)가 만든 허상이었다:
  ① 앞 문장 마침표에 붙은 'N.N)' 을 소수점 방어가 막아 **문제 10쌍이 한 덩어리**
  (확통 303→313 개로 분리. 쪽 배정이 안 틀어지게 마침표→공백, 글자 수 유지)
  ② [공학적 도구]·[수학 역량 플러스] 배지 ③ '비상 수학1' 출처 표기
  ④ 표 조각 — 표 속 숫자는 내장 글꼴 사용자 영역(U+E0xx)으로 나온다 (실측)
  ⑤ 쪽 머리말 조각 '기하 내신대비신사고 교과서…' + 짧은 단원명 꼬리.
  전부 문장 끝(마침표) 뒤에서만 자르는 규칙로 parse_ref 에 넣었다.
- extract.py: 어미 뒤에 오는 **보기 상자(보기 / ㄱ.ㄴ.ㄷ. 항목)** 를 발문의 일부로
  계속 담는다. 항목 머리는 호환 자모(ㄱ)와 첫소리 자모(ᄀ)가 섞여 온다 (OCR 실측).
  vendor 로 복사·배포 (1.48.9).
- 채점 (직전 v9+luna 대비): **기하 완전일치 60→65, 잘림 6→2 / 확통 44→49, 잘림 5→1.**
  오검출률 기하 2.7%·확통 1.9% 유지. 확통 채점 범위 115→102 는 붙은 문제 분리로
  쪽·단원 배정이 정확해진 결과 (허수 미검출 13건 정리).
- **남은 잘림 3건은 같은 부류** — 표기 없는 지문형의 다단 구성 (도입 문단+지시 문단,
  소문항 분리). luna 후보의 시작·끝을 LLM 에게 물어야 하는 영역. 미검출 잔여와 함께
  다음 과제로. luna 판정은 실행마다 ±1건 흔들린다 (이번에 확통 110 반쪽 회수가 새로 등장).

### 2026-08-07 — 지문형 경계를 luna 에게 2차 질의 (backend 1.49.0) — 잘림 양쪽 0
- 남았던 잘림의 정체: 여러 문단짜리 문제(도입+지시+소문항)에서 룰 후보가 문단
  하나만 뜨는 것. 기하 기준 31은 '찾아보기' 활동 상자의 **소문항 2만** 약한 표기
  '번호 2' 로 잡힌 사례 (지문 후보조차 아니었다 — 원본 줄로 확인).
- llm_filter 에 **refine_boundaries** 추가: 채택된 지문·약한 표기(번호·번호점)
  문제마다 앞뒤 이웃 문단(각 최대 2개, 다른 문제에 속하면 제외)을 [1]…[k] 로
  보여 주고 ★ 문단이 속한 '문제 하나'의 범위를 고르게 한다. ★ 를 포함해
  이어진 선택만 인정, 확실치 않으면 ★ 만 고르라고 지시 (넘침 방어).
  이웃이 없으면 질의 자체가 안 생겨 보통 문제엔 비용 0. 권당 +10원쯤.
- 채점 (작업 시작 시점 v9+luna 대비):
  **기하 완전일치 60→66, 잘림 6→0, 오검출률 1.4% / 확통 44→51, 잘림 5→0, 오검출 0.**
  luna 판정은 실행마다 ±1~2건 흔들린다 (temperature) — 점수 비교 시 감안할 것.
- 채점기 자동 적용 (GRADE_LLM=1), 백엔드 service 에 연결 (진행률 문구
  "AI 가 문제 경계를 다듬는 중"), vendor 동기화, 1.49.0 배포.
- 주의: 경계를 넓혀도 그림 귀속은 다시 안 돈다 — 넓힌 문단의 그림은 그림 창고에서
  수동으로 붙여야 한다 (기존 편집기 기능으로 가능). 필요해지면 후속 과제.

### 2026-08-07 — SMTP 연결 완료: 인증 메일 자동 발송 시작
- Gmail 앱 비밀번호를 받아 서버 .env 에 SMTP 4줄(HOST/USER/PASSWORD/FROM) 기록,
  api·worker 재기동. 값은 .env 에만 있다 (커밋 안 됨, .env.bak 백업 있음).
- 실제 발송 시험: send_verification_mail() 직접 호출 → True (Gmail 접수 확인).
  이제 가입하면 6자리 코드가 자동으로 간다. 관리자 수동 조회
  (GET /v1/admin/verification-code)는 예비로 남아 있다.
- 남은 외부 계약: ① PG (사업자등록·통신판매업 신고 후) ② Turnstile.

### 2026-08-07 — Turnstile 가입 봇 방어 구현 (backend 1.49.1) — 키 대기
- 가입(/v1/auth/signup)에만 적용. 서버가 클라우드플레어 siteverify 로 토큰 검증,
  화면은 GET /v1/auth/config 의 공개 키가 있을 때만 위젯을 그린다.
- .env 두 줄(TURNSTILE_SITE_KEY / TURNSTILE_SECRET_KEY)이 비어 있으면 완전 무동작
  (현재 상태). 확인 서버 장애 시 가입을 막지 않는다 (redis 제한과 같은 fail-open).
- 공식 시험 키 3종(없음/항상통과 1x…AA/항상실패 2x…AA)으로 검증 완료.
- **사용자 작업 대기**: 클라우드플레어 계정 → Turnstile 위젯 생성
  (도메인 158-247-240-59.sslip.io) → Site Key·Secret Key 두 개를 받아 오면
  .env 에 넣고 재기동으로 켠다.
- 사용자 결정 (2026-08-07): **Turnstile 실키는 서비스 직전에 넣는다.** 코드는 배포돼
  있으므로 .env 두 줄 + 재기동이면 끝. 그때까지 가입은 봇 확인 없이 (IP 제한만).

### 2026-08-07 — 사용자 검수 반영: 조판·서체·괄호·번호·그림 (backend 1.50.0)
- **2x2 조판**: 한 쪽에 문제 4개 (미리보기·PDF 공통, 사용자 확정). 칸 높이 122mm
  고정 — 넘치는 문제는 잘려 보이므로 편집으로 줄여야 한다 (드묾).
  ⚠ 인쇄 함정 실측 2개: ① 시트 min-height 297mm 를 인쇄에 두면 쪽마다 빈 쪽이
  낀다 (인쇄 영역은 269mm) ② 제목 있는 첫 쪽 기준으로 칸 높이를 정할 것.
- **서체**: 본문 한글 은바탕(KoTeX 기본, fonts-unfonts-core), 수식 Latin Modern
  Math(CM 계열). 브라우저엔 /mathocr/fonts/ 로 내려주고(@font-face local 우선),
  PDF 컨테이너엔 설치. 도커 이미지 재빌드 필요했음.
- **괄호 짝 불일치**: 브라우저 MathML 은 줄 맨 앞 여는 괄호만 세로로 늘인다
  (여닫이 비대칭의 정체). 일반 괄호는 stretchy=false 로 고정, \left\right 만 늘임.
- **문제 번호**: 순번(문제 1, 2, …)으로 통일 — 원본 번호는 소단원마다 재시작이라
  뒤죽박죽으로 보였다. 빈 문제(자동 삭제)는 건너뛰고 매기며, 편집 저장 때
  자동 재번호 (사용자가 고친 이름은 보존). 원본 위치는 '(원본 N쪽)' 이 담당.
- **그림 귀속**: 언급 검사를 머리 90자 → 본문 전체로, 문제 세로 구간 안의 그림은
  무언급도 귀속 (오른쪽 열 그림은 줄 순서만 뒤로 밀린다). 기하 01 기준 1→4장.
  채점: 오첨부율 기하 11.3%·확통 2.0%, 누락 기하 1·확통 2 — 지켜볼 것.
  이차곡선처럼 발문이 그림을 안 가리키는 단원은 원래 붙을 그림이 적다 (창고 42장
  대부분 개념·예제 그림 — 정상).
- 사용자 시험 작업(a8c65756, 기하 01)은 서버에서 새 규칙으로 제자리 재생성해 둠.
  화면·PDF 래스터로 검증: 2x2·순번·중앙 그림·서체·괄호 전부 확인, 27문제 = 7쪽.

### 2026-08-07 — '그림이 안 붙는다' 추적: 진범은 중단원 마무리 미검출 (backend 1.50.1)
- 원본 쪽 그림을 떠서 대조한 결과, 이차곡선 본문 문제들은 **원본에도 곁그림이 거의
  없다** (그래프는 학생이 그리는 것). 창고의 40여 장은 개념·예제 그림 — 정상.
  진범은 따로 있었다: **그림 많은 중단원 마무리(표준문제 7·9·10·11…)가 통째로
  미검출** — x 대역을 본문(x≈360) 하나만 배워 마무리 들여쓰기(x≈510~530)가 탈락.
- **x 대역 복수 학습**(후보 2개 이상 구간 전부)으로 마무리 회수:
  완전일치 기하 66→88, 확통 51→114 (+63). 이차곡선 작업: 문제 27→38, 그림 3→9.
- 새 오검출 봉합 왕복 셋: ① Mathpix 가 같은 줄을 두 번 주는 원본 중복 제거
  ② '준 비 학 습' 코너 번호 배제 — 글자 크기로 갈랐다가 확통 마무리(작은 판)까지
  잘려 -17, **코너 제목 기준으로 교체** ③ 사전배제 확장 (활동 오독 활롱·활몽·활옹,
  보기 시작, '이와 같은' 지시어 시작, 홀낱자 조각 '쥴ㄹ…').
  최종 오검출률 기하 4.0% / 확통 0.8% (목표 5% 안). 오첨부율 ~8%.
- 곁그림: 본문 그림이 있어도 추가 귀속 허용 (태양에너지 문제의 둘째 도식).
- **그림 이슈 시험 자료 추천**: ① 기하 05_공간도형 (30문제 중 22개 그림 — 밀도 최고)
  ② 기하 03_벡터의 연산 ③ 이차곡선 마무리 (24~25쪽 '오른쪽 그림과 같이' 4문제).

### 2026-08-07 — 선분 윗줄 수정 + 그림 검수판 (backend 1.50.2)
- **overline 이 짧던 원인은 글꼴이 아니다**: 윗줄 기호(¯)는 Latin Modern·STIX 모두
  늘여 주지 않는다 (화살표는 늘어남 — 실물 비교). mover 를 버리고 **mrow 테두리
  선(.ovl/.unl CSS)**으로 긋게 바꿈 — 폭이 내용에 정확히 맞는다 (PF′ 프라임까지 덮음).
  OMML(워드)·HWPX(한/글) 경로는 원래 결합 문자/스크립트라 그대로.
- 그림 귀속 검수용 화면을 만들어 시험 통로에 둠 (통로가 .html 도 열게 확장):
  /mathocr/testfile/그림검수_공간도형.html — 원본 쪽과 문제별 그림 수를 나란히.
  검수 결과(그림 있어야 하는데 0인 문제 번호)를 받으면 귀속 규칙을 조인다.
- 잔손질: '문제 \(?\)' 머리 부스러기 청소, CSS 주석 경고 제거.

### 2026-08-08 — 검수 1차 반영 (backend 1.50.3): 미검출 8문제·그림 공유
- 사용자 검수(공간도형 검수판)로 드러난 것들과 원인:
  ① 추론 N·서술형 N·문제 해결 N·의사소통 N — **표기 목록에 아예 없었다** (추가.
  어제까지는 luna 지문 경로로 우연히 잡히던 것). '문제 \(?\)'(번호를 수식 물음표로
  오독)도 표기로 받는다.
  ② 명령형 어미가 줄 끝에서 갈라지면('구하시'+'오.') 어미 검사 실패 → 약한 표기
  탈락. **창을 붙여 읽어 검사** (window_has_order_end). 단서 괄호가 다음 줄로
  넘어가는 경우도 같이 해결.
  ③ 번호 교정 fix_number 가 사슬 근거 없이 '10'을 1로 잘랐다 — 사슬 있을 때만.
  ④ 곁그림 공유: '(1)(2)' 두 문제가 그림 하나를 나누는 배치 — **2단계 추정**
  (1차 임자 없는 그림 400/120, 2차 그림 없는 문제가 60 안 이웃 그림 나눠 받기,
  그림당 최대 2문제). 한 번에 하면 언급 있는 문제가 이웃 그림을 빨아들인다.
- ⚠ 공간도형 12쪽 문제 4의 국기 게양대 그림은 **Mathpix 출력에 없다** (diagram
  줄 자체가 없음). 규칙으로 못 살림 — 그림 0 이 정직한 상태. OCR 한계 사례.
- 채점: 기하 완전일치 95 (오검출률 3.7%) / 확통 117 (0.8%). 오첨부율은 12%/10%로
  올랐다 — 기준 발문이 언급 안 한 곁그림 붙임을 페널티로 세는 지표라, 사용자
  요구(더 붙여라)와 상충. 실물 검수가 우선 잣대.
- 검수판 2 (같은 주소, 그림검수_공간도형.html) 재생성, 이차곡선 작업 재생성
  (문제 42 / 그림 16).

### 2026-08-08 — 검수 2차: 오해 하나, OCR 누락 둘, 번호 복구 (backend 1.50.4)
- "문제들이 잘려 보인다" → **검수판이 80자로 잘라 보여 준 표시 문제** (추론 6 실제
  본문은 고르시오·보기 ㄱㄴㄷ까지 온전). 검수판 3부터 전문 표시.
- '문제 \(?\)' 번호는 같은 파일 '문제' 사슬에서 이어받는다 → 18쪽은 **문제 7**
  (원본 실물 대조로 확정 — 사용자가 2라고 기억했지만 배지는 7).
- **Mathpix 출력 자체에 없는 것 2건** (규칙으로 못 살림, 실물 대조로 확정):
  ① 공간도형 12쪽 문제 4의 국기 게양대 그림 (diagram 줄 없음)
  ② 공간도형 20쪽 기본문제 1 '꼬인 위치' 문제의 글 전체 (오른쪽 열 첫 문제).
  대응 후보: 해당 쪽만 재OCR(과금) / 편집기 수동 추가. 빈도 지켜보고 결정.

### 2026-08-08 — 곁그림 우측상단·클립보드 붙여넣기·편집기 2x2 (backend 1.51.0)
- **곁그림 규칙 (사용자 확정)**: 그림은 문제 오른쪽 위, 그림 위끝 = 문제 첫 글줄.
  문서에 side=true 표시 (build 에서 도형 그림에만 — 수식 대체 그림은 본문 흐름).
  미리보기·PDF 공통 (float, 최대 40mm). 래스터로 정렬 확인.
- **화면 캡처 붙여넣기**: POST /v1/mathocr/jobs/{id}/image (PNG 서명 검사, 5MB,
  crop_9NNNN 대역). 편집기에서 문제 카드 클릭 → Ctrl+V → 곁그림으로 붙음.
  OCR 누락 그림(공간도형 문제4 류)의 수동 보정 통로이기도 하다.
- **편집기**: 미리보기처럼 한 시트에 4문제(2x2) 묶음 + 쪽 번호, 선택 카드 테두리,
  지운 문제는 아래 '지운 문제 N개' 묶음, 그림 창고 붙이기도 곁그림으로.
- **디자인은 챗지피티에 위임 (사용자 결정)** — 인계서 작성:
  저장소 docs/에디터-인계서.md, 내려받기 /mathocr/testfile/편집기인계서.html.
  API 계약·문서 스키마·조판 기준·보안 제약 포함. 기능 코드는 완성 상태라
  돌아온 디자인을 mathocr-edit.html 에 입힐 때 계약(5·7절)만 안 깨면 된다.

### 2026-08-08 — OCR 누락 보완 실전 테스트: 감지 + 부분 재OCR 둘 다 통과
- **감지 (0원)**: 원본 쪽을 그림으로 뜨고 Mathpix 줄 영역을 가린 뒤 남는 큰 잉크
  덩어리를 찾는다 (scripts/gap_probe.py, 컨테이너에서 실행 — fitz 필요).
  ⚠ 빈 껍데기(column·빈 list_item)는 가리개에서 빼야 한다 — 넓게 걸쳐 있어서
  그 안의 누락까지 가려 버린다 (실측). 결과: 공간도형 12쪽 국기 게양대 그림이
  완벽한 조각으로, 20쪽 기본문제 1 글 자리가 정확히 걸렸다. 배너류(기본 문제
  말풍선 등) 오탐이 몇 개 섞인다 — 통합 때 걸러낼 것.
- **부분 재OCR (조각당 ~6원)**: 조각을 Mathpix v3/text 에 넣으면 글이 온전히
  돌아온다 (scripts/reocr_test.py). '1 오른쪽 직육면체의 모서리를 연장한 직선
  중에서 … 구하시오' 신뢰도 1.0. 같은 쪽 전체 재OCR(같은 엔진이 같은 걸 또
  놓칠 위험)보다 확실하고 싸다.
- **통합 설계(미구현, 사용자 결정 대기)**: 추출 뒤 쪽마다 감지 → 그림형 조각은
  그림 창고 + '놓침 의심 N곳' 경고, 글형 조각은 (과금 스위치 안에서) 부분 재OCR
  후 문제 후보로 luna 판정에 합류. 비용 권당 수십 원 수준 예상.

### 2026-08-08 — OCR 누락 보완 통합 (backend 1.51.1)
- app/mathocr/gapscan.py: 원본 쪽 잉크 vs OCR 줄 영역 대조로 놓친 영역 감지.
  service 흐름: 문제 확정 → 감지(공짜, 항상) → 넓은(≥350px)·먹색 조각만
  부분 재OCR(유료 스위치 안, 조각 sha 캐시) → 표기 시작+신뢰 0.8+중복 아님이면
  문제로 합류, 나머지는 그림 창고 'OCR 놓침 의심' (큰 것부터 12개).
  결과 json 에 ocr_gaps {found, recovered}. 처리 시간 +15초(감지)~+60초(복구 첫 회).
- 실측으로 조인 문턱들 (커밋 메시지 상세): 배경 그림 1/3 규칙, 색 장식 생략,
  비대칭 조각 여유(왼쪽 110px — 번호 숫자, 위아래 28px — 이웃 문장 딸림 방지),
  중복 가드(3그램 겹침 0.6 — 영역 좌표가 잉크보다 좁아 헛걸린 자리의 재OCR 이
  기존 문제를 복제하는 실측 사고).
- 검증: 공간도형 기본문제 1('꼬인 위치') 자동 복구 (문제 28로 합류, 신뢰 1.0).
  국기 게양대 그림은 창고 조각으로. 유료 스위치 꺼진 지금은 감지+창고만 돈다 (0원).
- 챗지피티 디자인 참고용으로 GitHub(math-workbook-backend) 최신화 완료.

### 2026-08-08 — 챗지피티 편집기 디자인 커밋 검토·배포
- 커밋 5a1441a "Redesign mathocr editor as A4 direct-edit workspace"
  (mathocr-edit.html 단일 파일, +223/-79). GitHub 에만 있었고 서버 미반영 상태
  → 인계서 계약 검토 후 deploy.sh 로 배포.
- 검토 통과: 바깥 요청 없음(글꼴은 /mathocr/fonts), API 계약 그대로,
  innerHTML 은 서버 html 필드만, 자동저장 1.2초 유지, 그림 이름 전달만,
  side·붙여넣기 흐름 보존. 새로 쓰는 /v1/auth/me 는 기존 창구라 문제없음.
- 헤드리스 로드: 콘솔 오류 0, 미접속 상태 안내 정상.

### 2026-08-08 — 새끼 문항 조판·줄바꿈 규칙 (backend 1.51.2)
- (1)(2) 새끼 문항: 곁그림과 같은 가로줄 회피(clear:both) + 사이 두 줄(margin 3em)
  = 문제풀이 공간 (사용자 확정). 글 단위가 (1)·⑴·① 로 시작하면 sub 클래스.
- 줄바꿈: .line 에 word-break:keep-all (어절 단위만), 수식+바로 뒤 조사 어절은
  render_text_html 이 nowrap 묶음(.nb)으로 감싼다 ('x/를' 갈라짐 방지).
- 주의: 2x2 칸(122mm 고정)에서 새끼 문항 많은 문제는 아래가 잘릴 수 있다 —
  편집으로 줄이거나, 잦으면 칸 높이 자동 조정 검토.

### 2026-08-08 — 문장 중간 그림 줄 + 유료 OCR 스위치 ON (backend 1.51.3)
- Mathpix 가 그림 줄을 이어짐 사슬 한가운데 끼워 주는 경우가 있다 (벡터 18쪽
  서술형 14 '…도착했다고 [그림] 한다.'). merge_lines 가 그림에서 문장을 끊어
  문단이 갈라졌다 — 잇는 대상을 '마지막 글 단위'로 바꿔 그림을 건너뛰고 잇는다.
- **유료 OCR 스위치 켬 (사용자 요청, 임시)** — .env MATHOCR_LIVE_OCR_ENABLED=true.
  처음 보는 파일 과금 + 누락 글 복구(부분 재OCR)도 함께 돈다.
  ⚠ 시험 끝나면 다시 끌 것.

### 2026-08-08 — 동아 미적분 검수 반영 (backend 1.51.4)
- 낯선 출판사(동아) 첫 실전: 유료 OCR 로 24쪽 읽음 (sha 캐시 등록, 재처리 무료).
- ① 빈칸(form_field)의 text_display 는 '\begin{itemize}\item[□]' 마크업 — 표 규칙
  오발로 □ 가 그림 한 줄을 차지했다. merge_lines 에서 '□' 글자로 앞 문장에 잇고
  같은 시각적 줄(y 겹침)의 뒷글도 이어 붙인다.
- ② 새끼 문항 세로 분배: 문제 칸 flex 기둥, subblock 들이 margin-top:auto 로 남는
  공간을 나눠 가짐 — (2)는 위 내용과 칸 바닥의 중간 (사용자 확정 규칙).
- ③ 코너 문구(생각 열기 등) 본문 제거 (CORNER_PHRASE). 단, '생각 열기' 활동 자체가
  문제로 뽑히는 것(동아 문제6·10)은 남아 있음 — 기준 파일 없어 채점 불가 영역.
- 케이스 폭증 대응 방침(사용자 질문에 답함): 출판사 단위로 수렴한다는 전제,
  판매 대상 출판사 우선 + 잔여는 편집기 수동 보정 + 반복 유형은 luna 후처리로
  일반화 검토. 동아 기준 파일(교과서 모음_문제풀이에 있으면)로 채점 확장 가능.

### 2026-08-09 — 기준 자료 전수조사 (refs/ 에 두 폴더 업로드됨)
- **교과서 모음 (5.9GB)** = 원본. 전권 81권 17,445쪽 (교학사·금성·동아강·동아박·
  미래엔·비상·신사고·지학사·천재류·천재이) + '인터넷 교과서 모음' 단원별 211개
  (서비스 업로드에 좋은 단위 — 예: [동아 미적분] 06 여러 가지 적분법.pdf).
  전권 전부 OCR 시 참고 비용 ≈ 13만 원 (17,445쪽 × 7.5원).
- **교과서 모음_문제풀이 (768MB, 124개)** = 기준(수기 편집본).
  · PDF 21개 — 파서 보강 후 17개 즉시 사용 가능 (중2 4종·중3 10종·수학1 교학사·
    기하 신사고/천재이·확통 신사고). 남은 손질: 기하 천재류(9문제뿐)·미적분
    천재이(0)·확통 교학사(144 검증)·중2 동아박(342 검증).
  · **hwp 44개 — 고교 전 과목x전 출판사 편집본. 채점기는 PDF만 읽으므로 한/글에서
    PDF 변환 필요** (동아 미적분 기준도 hwp뿐 — 사용자 변환 대기).
  · 용문아 pdf 59개 — 별도 형식 문제모음 (중1 단원별 등), 미조사.
- 파서 보강 2건 (커밋 메시지 참고): 숫자 사이 공백, 해설 재시작 뒤쪽 40% 제한.
  ⚠ 폴더/파일명에 [완] 같은 대괄호 — glob 은 문자군으로 해석하니 listdir 쓸 것.

### 2026-08-09 — 동아 미적분 기준 확보·첫 채점 (기준점)
- 사용자가 hwp 를 PDF 로 뽑아 refs/[미적분][동아][문제편집].pdf 업로드 (106쪽).
- 파서 보강: 동아식 번호는 괄호가 아니라 **마침표로 닫는다** ('236.2 36.') —
  두 번호(표시.각주)가 다 있을 때만 인정. 5→382문제, 기존 책들 회귀 없음.
- 07 정적분 단원 채점 (GRADE_동아미적분.md, 기준점): 채점 범위 54 중
  완전일치 28 / 잘림 8 / 미검출 16 / 오검출 2 (5.0%) / 정확도 0.52.
  신사고 첫 기준점(0.32)보다 훨씬 높게 시작 — 그동안의 개선이 이식된 효과.
- 잘림 유형 메모: 빈칸(□) 과정 문제(기준330), 종이컵 활동(341), 선택지 ①(375),
  서술형 꼬리(381) 등 — 동아 다듬기 다음 회차 재료.
- 채점 명령: bash scripts/run_grade.sh "refs/[미적분][동아][문제편집].pdf" "동아 미적분" 동아미적분 "메모"

### 2026-08-09 — 기준 편집본 63종 전수 검수 완료 (hwp 일괄 변환 성공)
- hwp 44개는 사용자 PC 한/글 자동화(PowerShell + HAction "FileSaveAsPdf")로 일괄
  PDF 변환. ⚠ COM SaveAs 는 인수 3개 필요, 결과 검사(Test-Path)의 2초 대기는
  짧아 빨강으로 떠도 실제로는 저장됨. 서버측 pyhwp 변환은 문제 번호(각주 필드)가
  로마자 목록 기호로 깨져 탈락 (기록: 번호가 생명인 자료에 pyhwp 쓰지 말 것).
- **과목 x 출판사 63종 중 58종 정상** (문제 167~787개, 표는 GRADE 폴더 참고).
  중3는 10종 전부, 수학1 8종 전부, 미적분·수학2·확통도 사실상 전 출판사 확보.
- 손질 필요 5종: 고등수학 교학사(6)·지학사(5)·천재이(107), 기하 천재류(9),
  확통 교학사(144 — 해설 29쪽 판정 검증 필요). 출판사별 표기 형식 추가 대응 재료.
- 원본 lines.json 은 stage0_out/runs 에 단원별로 넣으면 채점 가능
  (동아 미적분 방식: sha 캐시에서 복사). 채점 명령은 §3 표준 명령과 동일 형식.

### 2026-08-09 — 기준 편집본 63종 전부 정상 (재검증 확정)
- 파서 2건 수정 후 65개 재검증: **63종 전부 정상, 기준 문제 총 26,505개.**
  머리말 학습 수정의 낙수 효과로 여러 권의 문제 수가 추가 회복됨
  (중2 동아박 342→549, 수학1 미래엔 273→414, 고등수학 신사고 270→712 등).
- 과목 x 출판사 커버리지: 중3·고등수학·수학1·수학2·미적분은 사실상 전 출판사,
  확통 9종, 기하 6종, 중2 6종. 전 출판사 채점판 재료 완비.
- 채점을 붙이려면: 해당 단원 원본을 OCR(stage0_out/runs 에 배치) 후 run_grade —
  기준 파일 경로는 refs/교과서 모음_문제풀이/[과목][출판사][문제편집].pdf.

### 2026-08-09 — 채점판 v1 가동 (진행 중)
- 8개 단원 실과금 OCR 완료 (309쪽 ≈ 2,318원, 각 10~15초):
  미적분 01단원 신사고·비상·동아·미래엔(1-1) + 수학1 1단원 비상·미래엔·천재이·지학사.
  결과는 서비스 sha 캐시와 stage0_out/runs/<단원명>/ 양쪽에 배치 (웹 재업로드 0원).
- 채점 배치 진행 중 (태그: 미적분신사고/미적분비상/동아미적분/미적분미래엔/
  수학1비상/수학1미래엔/수학1천재이/수학1지학사, 메모 '채점판 v1 기준점').
  진행 로그: scratchpad grade_batch.log. 완료 후 기준점 표를 이 문서에 적을 것.
- 작업 지침(§0-2) 성문화 — 사용자 지시. 이후 모든 세션은 이 지침을 따른다.

### 2026-08-09 — 채점판 v1 완성 (10개 기준점)
| 태그 | 범위 | 완전 | 잘림 | 미검 | 오검 | 정확도 | 오검출률 |
|---|---|---|---|---|---|---|---|
| 기하(신사고) | 123 | 95 | 4 | 18 | 4 | 0.77 | 3.7% |
| 확통(신사고) | 184 | 117 | 0 | 59 | 1 | 0.64 | 0.8% |
| 미적분신사고 | 35 | 26 | 4 | 5 | 5 | 0.74 | 14.3% |
| 미적분미래엔 | 41 | 25 | 1 | 15 | 3 | 0.61 | 10.3% |
| 미적분비상 | 112 | 66 | 3 | 38 | 14 | 0.59 | 15.9% |
| 동아미적분(01+07) | 92 | 52 | 10 | 26 | 11 | 0.57 | 14.3% |
| 수학1미래엔 | 71 | 44 | 1 | 23 | 5 | 0.62 | 9.4% |
| 수학1비상 | 137 | 75 | 7 | 50 | 15 | 0.55 | 14.7% |
| 수학1천재이 | 170 | 87 | 13 | 63 | 13 | 0.51 | 10.8% |
| 수학1지학사 | 110 | 34 | 19 | 45 | 40 | **0.31** | **38.1%** |
- 읽기: 낯선 출판사도 정확도 0.5~0.74 에서 시작 (신사고 첫 기준점 0.32 대비 —
  이식 효과 확인). 그러나 **오검출률이 10~16%** 로 목표(5%)를 넘는다 — 신사고에
  맞춘 안전장치가 타사 형식엔 부족. 수학1지학사는 별도 정밀 진단 필요 (38%).
- 다음 다듬기 우선순위: ① 오검출 공통 유형 분석 (미적분·수학1 6개 태그의
  false 목록 전수) ② 지학사 수학1 정밀 ③ 잘림 (동아 10, 지학사 19, 천재이 13).

### 2026-08-09 — 오검출 봉합 1차 (backend 1.51.5) — 채점판 v2
- 오검출 106건 전수 분석 → 최다 유형 = 본문에 어미가 없는 것 (개념 성질 나열,
  차례 항목). **약한 표기(번호·번호점·두자리) 사후 검사** 추가: 확정 본문에
  명령형·의문형 어미(시오/하라/보자/…인가?/…여라)가 없으면 버림.
  ⚠ 의문형을 빼면 지학사 진짜 문제가 같이 죽는다 (실측으로 넓힘).
  + 준비학습 변형(준|비|학|습) 사전 배제.
- 채점판 v2 (완전일치 유지, 오검출률만 변화):
  기하 2.8%✓ 확통 0.0%✓ 동아미적분 4.5%✓ 미적분미래엔 3.7%✓ 수학1미래엔 2.0%✓
  미적분비상 7.5% 미적분신사고 11.8% 수학1비상 12.1% 수학1천재이 10.8%(불변)
  수학1지학사 21.8%. 다섯 태그 목표(5%) 진입, 나머지 절반 감소.
- 남은 오검출 유형(다음 회차): 활동·탐구류 luna 통과 (천재이·비상), 지학사
  개념 풀이(어미 있는 예시 '…하라'류 유제 — 기준 미수록과 얽힘) 정밀.
- 챗지피티 편집기 2차 커밋(수식 클릭 편집·그림 위치 조절) 검토 통과·배포 —
  rebase 로 합침 (push 거절 시 fetch → 검토 → rebase 순서).

### 2026-08-09 — 오검출 봉합 2차 (backend 1.51.6) — 채점판 v3
- '…해 보자' 뿐인 약한 표기 → **활동의심** 분류로 luna 판정 라우팅 (룰로는 활동과
  진짜 문제를 못 가름 — 기준에 실린 활동도 있어서). 번호 뒤 '풀이/답/증명' 배제.
- v3: 기하 2.8✓ 확통 0.0✓ 동아 3.0✓ 미적분미래엔 3.7✓ 수학1미래엔 2.1✓
  미적분신사고 9.1(↓) 미적분비상 7.5 수학1비상 12.1 천재이 10.7 지학사 21.8.
- **중요 발견 — 오검출 지표에 아티팩트가 섞여 있다**: '다음 식을 간단히 하시오'
  류는 기준에 18건 존재하는데 한글 뼈대가 전부 같아 채점기가 짝을 못 찾고
  오검출로 센다 (비상·천재이 잔여의 상당수). 실제 오검출률은 표시보다 낮다.
  → 다음 과제: 채점기 짝짓기 개선 (동일 뼈대 다중 후보 허용, 쪽 번호 힌트 활용).
- 남은 재료: 지학사 정밀(21.8 — 짝짓기+예제 유제 혼재), 채점기 짝짓기 개선.

### 2026-08-09 — 채점기 짝짓기 개선 (grade.py 만, 백엔드 무관) — 채점판 v4
- 아티팩트 원인 실측 2가지: ① 편집본이 문제 순서를 재배열 + 동일 뼈대 다중
  존재 → 순서보존 1:1 짝짓기가 못 가름. ② 경계 넘침 추출은 뒤 본문을 삼켜
  뼈대가 희석 (실측: '다음 식을 간단히 하시오' 전문 1031자 → 닮음 0.07,
  앞머리 110자 → 1.00).
- 구조 변경 (지침 4): 짝 못 지은 추출이라도 기준 어딘가에 닮음 0.6+ 문제가
  있으면 **짝중복** 으로 분리 — 오검출이 아니다. 전문·앞머리 두 뼈대 중 높은
  쪽으로 판단. RESCUE_SIM 0.55→0.45, 순서창 2차 구조(0.38) 추가.
  순서창 단독은 무효였다 (재배열 때문에 이웃 짝 구간이 안 잡힘 — 실패 기록).
- v4 (v3 대비): 기하 1.9✓(↓0.9) 확통 0.0✓ 동아 3.0✓ 미적분미래엔 3.7✓
  수학1미래엔 2.0✓ | 수학1비상 12.1→**7.1** (짝중복 3) 천재이 10.7→**9.2**
  (짝중복 2) 미적분비상 7.5= 미적분신사고 9.1→11.8 (루나 판정 변동 —
  '~해 보자' 활동 4건 통과, 짝짓기와 무관·추출 쪽 문제).
- 남은 오검출의 정체 (수학1비상 7건 전수): 포트폴리오 활동 1, 소리 크기
  실생활 활동 3, 공학 도구 활동 1, 역함수 복습 1, 스트론튬 실생활 1 —
  전부 기준에 없는 활동·복습류 = **진짜 오검출**. 지표가 이제 정직하다.
- 다음 재료: ① 활동류 루나 판정 강화 (판정 변동이 태그당 ±1~4건 흔든다)
  ② 지학사 정밀 (21.8%) ③ 잘림 다듬기 (동아 8건 등).

### 2026-08-09 — 활동의심 경로 복구 + 지학사 정밀 (backend 1.51.7) — 채점판 v5
- **버그 3개가 겹쳐 있었다** (전부 지학사 진단 중 발견, 전 과목 영향):
  ① 사후 검사 strong 판정이 ORDER_END(_ANY) 를 재사용 — 둘 다 '보자' 를 포함해
  '…해 보자.' 활동이 강한 어미로 통과, **활동의심→루나 경로가 사실상 죽어 있었다**.
  ② 그림 마크다운 URL 의 `?height=…` 가 의문형 어미로 오인 — **그림 붙은 약한
  표기는 사후 검사 무사통과**. 어미 검사 전에 URL 을 걷어낸다.
  ③ 경계 질의(refine)가 예제 '풀이' 블록을 다음 문제 앞에 붙임 — 풀이·증명·답
  시작 블록은 이웃 후보에서 제외. + 활동의심도 경계 질의 대상에 포함
  (빼면 기준에 실린 활동 문제가 조각으로 남아 미검출).
- 지학사 전용 봉합: 복습 코너('스스로 준비하는 중단원') 쪽 단위 배제 —
  머리줄 type 이 page_info 라 live 스캔으론 못 본다, rows 에서 쪽을 모아 판정.
  '풀이 (…' 시작 후보·몸통 배제. parse_ref 에 '※ 창의융합…' 배지 꼬리 제거.
- **지학사 21.8% → 5.7%** (오검출 40→4: 활동의심 루나 통과 3 + 확인 문제 1).
- 회귀 배치 v5 (오검출률): 기하 0.0✓ 확통 0.0✓ 미적분신사고 3.3✓ 미적분비상 4.0✓
  동아 3.0✓ 미적분미래엔 3.7✓ 수학1비상 2.2✓ 수학1미래엔 0.0✓ **천재이 6.0✗**.
  **10개 태그 중 9개가 목표(5%) 진입** (지학사 5.7 는 목표 코앞).
- 주의: 수학1비상 미검출 49→51 (+2) — URL 어미 수정으로 어미가 오독된 진짜
  문제가 같이 떨어졌을 가능성. 다음에 미검출 정밀 때 확인.
- 루나 판정 변동은 태그당 ±1~4건 흔든다 (같은 코드 재실행 실측) — 남은
  오검출의 주성분. 판정 다수결·프롬프트 보강은 벤치 재측정과 함께 할 것.
- 배포: backend 1.51.7 (vendor extract/llm_filter 동기화). ⚠ /opt 로컬 브랜치가
  master 라 push 는 `git push origin HEAD:main` 으로 할 것 (master 로 밀면
  원격에 새 가지가 생기고 deploy.sh 는 origin/main 만 본다 — 실측 사고 1회).

### 2026-08-09 — 잘림·미검출 다듬기 (backend 1.51.9) — 채점판 v6 확정
- **오검출률 전 과목(10/10) 목표 진입** — 6과목 0%. (v6 확정 배치, history 참조)
- 채점기 (grade.py, 연구 전용):
  ① 포함율(교집합/작은쪽) 3단 구제 + 짝중복 판정 보강 — 자카드는 조각↔긴 기준
  크기 불일치에 깔린다 (천재이: 기준에 실린 활동 5건이 오검출로 찍혔었다).
  ② **뼈대 어미 통일** (하시오·하여라·푸시오→하라) — 편집본이 '하라'체를
  '하시오'체로 고쳐 쓴다. 뼈대 7자 초단문에선 어미가 담김 0.67 로 떨어뜨려
  가짜 잘림 15건 (지학사: 잘림 16→3, 완전일치 +19).
- parse_ref: '내신대비…' 꼬리를 마침표 조건 없이 제거 (⋯(2) 끝 문제에서
  꼬리 18자 잔류 → 완전일치가 잘림으로 찍혔다, 동아 실측 2건).
- 추출기 (extract.py, 1.51.9 배포):
  '1 6명이…'/'75 명이…' — 번호와 수량 숫자가 붙은 오독 회수 (확통 +3).
  llm_filter: '써넣으시오' 빈칸 문제는 표기가 강해도 경계 질의 (동아 잘림 -1).
- 남은 재료 (미검출이 다음 본선): 확통 미검출 57 분해 = 표기있음 27(연습문제
  코너 등 룰 후보) / 표기없음 22(개념 빈칸 박스·활동 — 기준에 실림) / 흡수 9 /
  원본에없음 2. why_missed.py 가 분류해 준다. 천재이 잘림 18 은 활동 조각
  (포함율 구제가 정직하게 잘림으로 표시) — 경계 확장 여지.

### 2026-08-10 — 미검출 '표기있음' 봉합 1차 (backend 1.51.10)
- why_missed 분해(확통 60건): 표기있음 27 / 표기없음 22 / 흡수 9 / 원본에없음 2.
- 표기있음 중 룰로 잡히는 유형 회수:
  ① 번호+수량 오독 ('1 6명이', '75 명이' — FALSE_FOLLOW 예외 + 수량 판) +3
  ② **번호가 수식 라텍스에 합쳐짐** ('서술형 \(6 \quad 1,2…\)', '\(20,1,2…\)')
  — 되돌린 변형으로 마커 재시도 (DELATEX_QUAD/MERGED). 영단어 2자+('5 college에')
  ·숫자 나열('2 0,1,…') 본문도 번호로 인정 +3
- 회귀 배치: 전 과목 오검출률 목표 유지 (10/10 ✅), 미검출 순 -3.
- 확통 남은 미검출 54 의 성분: 개념 빈칸 박스(원순열⑴… — 편집자가 기준에
  넣음, 추출기는 발문 어미가 없어 문제로 안 봄), 활동 상자(축구 심판, 자동차
  안전도… — 루나가 활동으로 거름/ACTIVITY_INTRO 배제), 흡수 9.
  ⚠ 활동·개념 박스는 출판사·편집자마다 수록 방침이 갈린다 (신사고 확통 편집자는
  넣고, 천재이는 뺌) — 룰을 더 조이면 다른 책 오검출이 되살아난다. 다음 회차에
  사장님과 수록 기준을 정한 뒤 손댈 것.

### 2026-08-10 — 수록 방침 확정 + 방침 필터 (backend 1.51.11) — 채점판 v7
- **수록 방침 (사장님 확정)**: 개념 빈칸 박스·활동 상자('~해 보자'체)는 문제로
  뽑지 않는다. 메모리에도 성문화.
- 채점기: 발문 어미 없는 기준 항목을 개념/활동으로 분류해 채점 제외 (수를 밝힘).
  확통 기준에서 개념 21+활동 40 제외 → 누락률 31%→17%.
- 추출기(1.51.11): 활동의심('보자'뿐)은 루나에 묻지 않고 폐기, 루나가 채택한
  지문도 같은 판(ACTIVITY_STRONG)으로 활동이면 폐기 — 채점기 제외 판과 일치.
  루나 호출 수도 감소.
- **v7 확정 배치: 10/10 오검출률 목표, 5과목 0%, 전체 오검출 합계 5건.**
  완전일치 합계 646. 미검출 합계 258 (방침 반영 후 정직한 수).
- 미검출 남은 성분(다음 본선): 흡수됨(끝 판정), 연습문제 코너 잔여,
  원본에없음(OCR 누락 — gapscan 확장 후보). why_missed.py 로 태그별 분해할 것.

### 2026-08-10 — 유료 OCR OFF + 흡수 회수 (backend 1.51.12) — 채점판 v8
- **유료 OCR 스위치 껐다** (MATHOCR_LIVE_OCR_ENABLED=false, 컨테이너 재시작).
  캐시는 계속 쓴다. 다시 켤 땐 .env 수정 후 api·worker 재시작.
- 흡수됨 회수: 번호 판 본문 조건에 대문자 한 글자('10 A 주머니에는')와
  숫자+조사('16 1이 적힌 카드') 허용 — 다음 문제가 앞 문제에 삼켜지던 것.
- v8 회귀: 10/10 목표 유지, 오검출 0% 7과목, 합계 완전일치 648/오검출 5/미검출 257.
- 남은 미검출 성분: 확통 기준 표기없음 9(코너 밖 지문형)·표기있음 9(활동 상자
  안 번호 등 — 방침상 기준 편집자가 넣은 것과 충돌하는 잔여)·원본에없음 1.
  큰 회수 여지는 소진 — 다음은 실전 테스트(실제 업로드 왕복)가 효율적.

### 2026-08-13 — 실험: OCR 원문을 통째로 루나에게 (지학사 수학1, 같은 채점기)
- 방법: 쪽 텍스트 4쪽 묶음(1쪽 겹침) × 20회 호출, 문제만 JSON 으로 옮겨 적게 함.
  스크립트 scripts/llm_extract_test.py (수식 역슬래시 JSON 수선 포함).
- 결과 (현행 룰+루나 보조 대비 전패):
  완전일치 37 vs **56**, 미검출 64 vs **46** (누락률 58% vs 42%),
  오검출률 9.4% vs **0%**, 정확도 0.33 vs 0.51.
- 비용: 토큰 입력 60.7k + 출력 19.9k / 58쪽 1권. 현행 루나 비용(권당 10~20원)
  대비 **5~10배 추정** (정확한 단가 확인 필요).
- 질적 한계: LLM 경로는 그림 좌표를 못 준다 — 그림 오려붙임·곁그림 배치가
  불가능. 옮겨 적기 과정에서 원문 변형도 생긴다 (뼈대 채점으로도 잘림·넘침 7).
- 결론: 통짜 LLM 추출은 품질·비용·그림 전부에서 열세. **현행 구조(룰이 넓게
  뜨고 루나가 애매한 것만 판정) 유지.** 다시 하지 말 것 (실패 기록).

### 2026-08-14 — HWPX 실전 수리 (backend 1.51.13~16) — 실물 왕복 3회
- 실전 테스트(지학사 58쪽 업로드)에서 HWPX 난장판 신고 → 분해 견본(글만/
  글수식/글그림/전체10)으로 원인 가름 (이 방식이 또 통했다).
- 수리 내역: ① 곁그림(side) 문제 첫 문단 우측상단 띄우기 (треatAsChar=0,
  COLUMN-RIGHT, SQUARE, 폭 45% 제한) ② 수식 줄겹침 — **affectLSpacing=1**
  (줄 간격에 영향 줌)이 정답. ③ 라벨 파랑 #1F6BD6 ④ 만료 청소가 _시험
  견본 폴더를 지우던 것 제외 ⑤ 빈칸 상자 \begin{itemize} → □ 글자.
- **⚠ 영구 금지: linesegarray.** 진품 한/글 파일에는 아예 없다. 넣으면
  '문서 손상·변조 의심' 경고 (실물 사고 2회째 — v15 에서 내가 실패 기록을
  안 보고 재도입했다). hwpx_lint 가 이제 이걸 검사한다.
- 검수기 scripts/hwpx_lint.py: 구조 전수 검사 + 작업 문서 대조. 견본 전달
  통로는 /mathocr/testfile/ (_시험 폴더).
- 지학사 빈칸(□)은 OCR 단계 소실 (몸통에 form_field 0) — 유료 재OCR 영역.
- 남은 확인: v16 실물 (수식 줄겹침·손상 경고 해소 여부) — 사장님 왕복 대기.

### 2026-08-14 — HWPX 수식 종결 (backend 1.51.17~19)
- 빈 수식의 원흉 = n제곱근 표기: `^{n} sqrt` 는 한/글이 못 읽는다 →
  **`root n of {x}`** 로 수정 (수식문법시험 실물: [5][7] 빈 상자 → 정상).
- 수식 크기 계산기 구조 수리 (케이스 고정값 아님 — 자식 키 전파 일반 규칙):
  ① 첨자 내용 초과분 반영 ② **밑동 키 + 첨자 돌출분(위188·아래202·양쪽390)
  + 첨자 초과분** — 큰 밑동(루트·괄호·분수)에 지수 얹힌 조합 전부 커버.
  검산: (2³×⅓)^⅓ 2398→3348, (∛(2/3))^½ 3496. 사장님 "다 맞네" 확정.
- affectLSpacing="1" (줄 간격에 영향 줌) — 키 큰 인라인 개체의 줄 확장.
- 시험 파일 요령: 수식 문법 의심 시 '수식문법시험' 같은 여러 표기 비교판을
  만들어 한 번의 왕복으로 가른다. 시험판 수식엔 반드시 내용을 넣을 것
  (빈 수식은 뭘 넣어도 정상처럼 보인다 — 실측 함정).
- 남은 미세: 수식이 제 위치보다 살짝 내려가 보임(기준선) — 재신고 없음, 보류.

### 2026-08-16 — HWPX 2x2 표 배치 + 그림 뒤틀림 판정 (backend 1.51.20~22)
- **2x2 배치는 표(테이블)가 정답**: 쪽마다 투명 2x2 표(진품 한글시험2 뼈대,
  borderFill 1 = 무테두리), 셀=문제 하나(반쪽 고정, 풀이 공간 자동).
  가로 우선 순서(미리보기와 동일). 구역은 1단. 제목 줄은 뺀다 (표가 제목에
  밀려 통째로 2쪽으로 떨어짐 — 실물).
- 라벨 파랑이 안 먹던 원인: 글자 서식 복제본을 목록 중간에 끼우면 한/글이
  순서 기준으로 읽어 어긋난다 — **charProperties 끝에 붙일 것**.
- **그림 찌그러짐 판정: 소스 스캔 문제** — 인터넷 교과서 스캔들의 쪽 비율이
  1.285~1.415 로 제각각 (실제 판형은 국배판 1.333 하나). 문제의 신사고 기하
  스캔은 1.291 = 세로 ~3% 눌림. 파이프라인은 비율 보존 확인(크롭 행렬 등배·
  HWPX 상자 14/14 일치). **사장님 결정: 보정하지 않는다** (소스 책임,
  출판사 원본 PDF 쓰면 정상). 판형 보정 아이디어(쪽비율→1.333 세로 보정,
  ±10% 한도)는 필요해지면 꺼낼 것.
- 사고 기록: 패치 assert 실패에도 버전만 올라 배포된 적 있음(1.51.21 1차) —
  패치와 커밋을 한 셸 체인에 넣지 말 것.
- 다음: 챗지피티가 에디터 디자인 다듬기 예정 (계약서 docs/에디터-인계서.md,
  커밋 검토 후 배포 절차 유지). v22(제목 제거판) 실물 확인 대기.

### 2026-08-16 (계속) — 마무리 (backend 1.51.23~24)
- 제목은 머리말로 (매 쪽 상단 반복, 본문 안 먹음 — 진품 gyehoek 뼈대).
- 첫 쪽 빈 줄 제거 (첫 표를 구역 설정 문단에 직접), 표 높이 본문 영역에
  맞춤 (칸 32600 — 밀리면 여유 늘릴 것).
- **유료 OCR 스위치 다시 켬** (사장님 지시 "막아놨던 기능 풀어줘").
- 챗지피티 에디터 디자인 라운드 시작 예정 — 커밋 검토 후 배포 절차.

### 2026-08-16 (계속) — 실전 피드백 11건 (미래엔 수학2, backend 1.51.25~27)
- 실전 테스트(사장님 직접 업로드) 피드백 11건 전부 처리. 핵심:
  ① '탐구 문제 N' 접두 표기 인식 ② 이름+번호 수식 합침 복원(DELATEX_NAMED)
  ③ 준비하기·다가서기 **구간**(쪽 아님!) 제외 — 같은 쪽 아래 본문 보호
  ④ 창의-융합 코너 쪽 제외 ⑤ lim 은 from(아래끝) — _{} 는 옆에 붙음(실물)
  ⑥ 키 작은 \left( 는 평괄호 — LEFT( 자동확대가 평괄호와 어긋남(실물)
  ⑦ **조건별 함수 수식화**: \begin{cases}·\left\{\begin{array} 둘 다
  → 한/글 pile / MathML mtable / OMML eqArr. ⚠ to_rich 는 OMML 까지 세 경로
  전부 성공해야 통과 — 한 경로만 고치면 그림으로 회귀한다 (실측 사고 1회)
  ⑧ 라벨 '(원본 N쪽 - 예제1)' ⑨ 되돌림 크롭 번호 폭 제외 ⑩ 새끼문항 (1)
  발문 직하 ⑪ 보기 라운딩 박스(미리보기; 한/글 쪽은 남음)
- 회귀 배치: 순이익 — 완전일치 +13 (천재이 +6), 미검출 -9, 10과목 목표 유지.
- 남은 것: 보기 박스 한/글 표현, cases 조판 계산기 정식 지원(지금은 어림 크기).

### 2026-08-16 (계속) — 조판·편집기 마무리 (backend 1.51.28~32)
- 조건함수 최종: **cases{a & 조건 # b & 조건}** (실물 판정 — LEFT \{ 는 역슬래시
  노출). 상자 크기는 줄별 내용 실측 합산(_cases_layout).
- 본문 왼쪽 정렬(paraPr 20 복제) — 양쪽 정렬이 글자 간격을 벌렸다.
- 라벨 '(원본 N쪽 - 예제1)' 미리보기·HWPX 공통, 보기 박스는 ㄱㄴㄷ 포함,
  lim 은 MathML munder + 한/글 from.
- 편집기: 새끼문항 줄 띄우기, ▲▼ 문제 이동, 다음 문제와 합치기(쪼개진 문제
  수리), 칸 구분선 옵션 제거.
- **배치 설정 관통**: 편집기 단수·쪽당 문제 → doc.layout{columns,page_size}
  저장(검증) → 미리보기 격자·HWPX 표(행×열) 반영. origin 저장 유실 버그 수정.
- 챗지피티 1차 커밋(fd61e56, 편집기 개편+루나 이미지 변환 API) 계약 검토 합격.
  ⚠ push 거절 시: fetch → 검토 → cherry-pick/rebase → push (deploy.sh 가
  origin/main 으로 리셋하므로 순서 어기면 커밋 유실 — 실측 2회).

### 2026-08-16 (계속) — 편집기 편의 기능 (backend 1.51.33~35)
- 추가: 문제 찾기 목차(검색+점프), Ctrl+S, Delete 그림 삭제, 저장 시각,
  이탈 경고, **원본 나란히 보기**(/source-page/{page} API + 서랍, 문제 선택
  따라감), **출제 범위 선택**(picked 체크 — 미리보기·PDF·한글 공통 필터,
  모두 출제/해제, '출제 N/M' 카운터).
- 제거: 문제 복제, 다음 문제와 합치기 (사장님 판단).
- ⚠ 같은 사고 3회째: 패치 파이썬과 git 커밋을 한 셸 뭉치에 넣으면 패치가
  중간에 죽어도 커밋·배포가 나간다. **패치 → 확인 → 커밋을 반드시 나눌 것.**

### 2026-08-17 — 편집기 대개편 + 웹 저장소 (backend 1.51.36~38)
- **웹 저장소 확정 방침**: 문서+참조 그림만(원본 PDF 제외), 사용자당 5개,
  마지막 접속 30일 지나면 정리. app/mathocr/storagebox.py — 저장/목록/열기
  (새 작업으로 복원)/삭제, 프로필 모달에서 사용. 왕복 실측 검증 완료.
- **원본 표시**: 문제 선택 시 원본 쪽에 빨간 상자(문제 위치) — 추출 때
  bbox(OCR 픽셀, bbox_w=쪽 그림 폭) 저장, 편집기 저장에도 보존.
  원본 안 Ctrl+스크롤은 원본만 확대(≤4배). 원본 박스는 Selection 아래,
  세로 화면 최대.
- 편집기: Ctrl+스크롤 문제지 줌(기본 A4 맞춤), 햄버거 좌측 서랍(본문 밀기
  +폭 조절), 우측 500px, 다운로드▼ 드롭다운, 프로필 모달(메일·크레딧·저장소),
  단축키 ⓘ 모달, 휴지통(카운터 클릭·현재 페이지 복원·AI 변환 재시도),
  그림 리사이즈 핸들, Ctrl+Y, 그림 2개 너비 조절 오적용 버그(단위 번호 지정).
- 문구 규칙: 문항 N/M·폰트 사이즈·검색어 입력·AI 변환·우측 상단·원본 표시·
  자동 저장됨(AM/PM)·입력 중… 등. 안내문은 짧게, 긴 설명은 모달로.

### 2026-08-17 (계속) — 저장소 덮어쓰기·로고·홈 (backend 1.51.39)
- 저장소 덮어쓰기: 편집기 해시 #job@slot — 저장소에서 열면 슬롯이 붙고
  헤더 버튼이 '저장소에 덮어쓰기'가 된다. 신규 저장 시 슬롯 자동 부착.
- **출력물 로고**: 내 정보에서 PNG 선택 → 사용자 단위 보관(_보관/{키}/user-logo.png)
  + 작업 폴더에 user-logo.png 복사(생성·저장소 열기·업로드 시). 조판기는
  파일 존재만 보고 그린다(미리보기·PDF 쪽 좌상단 8mm, HWPX 머리말 그림).
- 홈 화면: 내 저장소 목록(열기), 시험 단계 안내문 제거.
- 편집기: 번호 재계산(렌더마다 자동 라벨 순번), 페이지당 문제 수 게이지(1~4),
  프로필 강조·모달 위치(우상단/버튼 옆), 모두 선택.
- 왕복 검증: 덮어쓰기 200, 로고 업로드→미리보기 51쪽 로고·HWPX BinData ✓.

### 2026-08-17 (계속) — 홈 리뉴얼·회원가입·도메인 (backend 1.51.40)
- 홈 전면 리뉴얼: Matharin 로고+배경 사진(사장님 제작) + 실제 UI.
  설계 조건(웹 리서치): 스크림 가독 대비·단일 CTA·44px 버튼·8px 스케일·
  자산 압축(배경 168KB·로고 87KB). 배경은 86% 축소(확대 부담 피드백).
- /mathocr/signup 신설: 필수 약관 3종(이용약관·개인정보·업로드 자료 권한).
  가입 버튼에서 유인 멘트 제거. 푸터에 저작권 이용자 책임·면책 고지.
- 로그인 모달: 로그인 전용, Enter 제출, 바깥 닫기는 클릭 시작 기준.
- 챗지피티 홈 폴리시 레이어(659d497) 검토 후 배포 — _static_page 를 감싸
  CSS 링크만 주입하는 방식(기능 무접촉, 합격). 단 사장님 지시 충돌 2곳
  (배경 cover 강제, 안내문 가운데 정렬) 걷어냄. **검토 요령: 챗지피티
  커밋이 사장님 최신 지시를 되돌리는지 반드시 대조할 것.**
- 도메인 personalpms.cloud: Caddy 대기 설정 완료, DNS 変更은 사장님 몫
  (A @ / www → 158.247.240.59). 감시 모니터 가동 중.

### 2026-08-17 (계속) — 피그마 랜딩 적용 (backend 1.51.41)
- 사장님 피그마 Make 시안(React+Vite)을 서버에서 빌드해 루트(/)에 랜딩으로.
  구조: / = 소개 랜딩 → /mathocr = 업로드 앱 → /mathocr/edit = 편집기.
- 시안 소스: /root/피그마시안/"AI Math Workbook Editor" — 수정 후
  `npx vite build` → dist/ 를 app/static/landing/ 에 복사 → deploy.
  에셋은 main.py /assets/{name} 라우트 (이름 검증).
- 브랜드 MathPick→Matharin, CTA 전부 /mathocr 연결.
- **요금제·후기 섹션 비활성** (사장님 지시 — 가상 후기·미확정 가격).
  다시 켤 땐 App.tsx 렌더에 <Pricing/> <Testimonials/> 복원 + 내비 '요금제'.

### 2026-08-17 (계속) — 랜딩이 기준 화면 (backend ~1.51.41)
- **피그마 랜딩(/)이 기준**: 히어로 업로드 박스가 실제 추출 연결(게스트 세션→
  업로드→진행률→통계→편집기/미리보기). /mathocr 은 그대로 두되 부화면.
- 편집기 실물 스크린샷을 랜딩 미리보기 섹션에 (헤드리스 크로미엄 촬영 —
  _시험 폴더 경유, 촬영 후 스냅 파일 삭제).
- **landing-src/ 를 백엔드 저장소에 포함** — 챗지피티 디자인 검토·수정 경로.
  빌드·배포 절차는 landing-src/빌드방법.md. 지피티 커밋 오면: 검토(허위
  수치·가짜 정보·사장님 지시 위반 대조) → 빌드 → dist 복사 → 배포.
- 정직성 손질: 가짜 통계·30초 과장·가짜 사업자번호 제거, 죽은 링크 정리.

### 2026-08-17 (계속) — 홈 통일·로그인 모달·새 스크린샷·지피티 테마
- /mathocr 링크 전부 홈(/)으로 통일. 랜딩에 자체 로그인 모달+상태 칩 신설
  (로그인 UI 의 /mathocr 의존 제거). 가입은 /mathocr/signup 유지.
- 편집기 스크린샷 갱신: 좌측 패널·원본 표시 ON(빨간 상자)·수식 편집 팝업
  열린 실물 (헤드리스 크로미엄 + 상태 주입 스크립트).
- 지피티 커밋 검토·배포: 랜딩 재설계+Dockerfile 랜딩 자동 빌드+CI 검증,
  편집기·가입 다크 테마(CSS 주입 레이어). 전부 합격.
- 사고 4회차(push 교차→커밋 유실→cherry-pick 복구) + node_modules 64,903개
  실수 커밋 → .gitignore 로 정리. **git add landing-src 전에 node_modules
  존재 여부 확인할 것.**

### 2026-08-28 (2차) — 프로세스 검토: 큰 교재·동시 편집·거짓 안내

**① 300쪽 넘는 교재는 저장이 영영 안 됐다 (라이브 차단급)**
실측: 교재는 쪽당 1.65문제(최대 2.06)를 낸다. 업로드 한도가 500쪽이라 많으면
1,000문제가 넘는데 저장 한도(MAX_PROBLEMS)는 500이었다. 270쪽 교재가 이미
488문제다. 사용자는 포인트를 내고 추출까지 마친 뒤 **편집기 자동 저장이 계속
422 로 실패**하는 상태에 갇힌다. 사장님이 128쪽까지만 시험해서(179문제) 이
구간을 못 밟았다.
- 900문제로 재 봤다: PDF 8.1초 6MB · 한글 0.3초(900문제 모두 들어감, zip 정상)
  · 편집기 렌더 오류 0. 서버는 멀쩡하다. **500 → 1200.**
- 안전망: 추출이 한도를 넘으면 잘라 내고 **몇 문제가 빠졌는지 알린다.**
  말없이 두면 다시 '저장이 안 되는' 상태가 된다.

**② 두 창에서 열면 늦게 저장한 쪽이 앞 편집을 말없이 덮었다**
자동 저장이 계속 도니 실제로 일어난다. 문서에 판 번호(mtime_ns+크기)를 붙이고
편집기가 불러온 판을 저장 때 `?version=` 으로 보낸다. 그 사이 바뀌었으면
409 `stale_document`. 편집기는 **자동 저장을 멈추고** 안내창을 띄운다
(최신 내용 불러오기 / 이 창은 그냥 두기). version 을 안 보내면 예전처럼
저장된다(내부 호출 호환).

**③ 없는 기능을 안내하고 있었다**
기능을 걷어낼 때 화면 문구가 따라오지 않았다. 사용자에게 없는 것을 약속하는
것은 서비스에서 가장 나쁜 종류의 오류다.
- 랜딩 요금제 "✓ 정답지·A/B형 조판 포함" — 둘 다 없앤 기능 → "✓ PPT 출력 포함"
- 문제 선택 화면 "해설지를 올리면 정답이 자동으로 붙어요" 두 곳 → 교체
- 확인: 선택 화면이 안내하는 단축키(Q/E·←→·휠·Ctrl+휠·Ctrl+Z·Ctrl+0·쪽 전체)는
  전부 실제로 동작한다.

**④ 랜딩의 이용 방법이 실제 흐름과 달랐다**
"문제 자동 추출"이라고만 적혀 있어 **문제를 고르는 화면이 있다는 것을 알 수
없었다.** 자동인 줄 알고 온 사용자가 선택 화면에서 이탈한다.
  PDF 업로드 → 필요한 문제만 고르기 → 편집기에서 다듬기 → 시험지 완성
"고른 쪽만 인식해 비용을 아껴요"로 선택 단계의 이유도 밝혔다.
PPT 출력이 랜딩 네 곳에서 빠져 있어 한글·PDF·PPT 로 통일.

**⑤ 앱 화면끼리 오갈 길이 없었다**
문제은행 → 내 시험지로 가려면 홈을 거쳐야 했다. 문제은행·내 시험지·내 정보·
충전 머리에 공통 메뉴를 붙였다. 편집기는 화면을 좁게 쓰므로 👤 창 맨 위에.

**확인했고 문제 없던 것**
- 다운로드 결과물을 **열어서** 확인: PDF 6문항 전부 수식까지 정상, 한글 zip
  정상·수식 10개·문항 6개, PPT 슬라이드 6장. (처음에 "문제 4·5가 빠졌다"고
  나온 것은 수식을 걸러 비교한 내 검사의 오탐이었다.)
- 포인트 표시가 서버 계산과 일치한다(인식 쪽×10P, 다운로드 쪽×1P,
  PPT 는 슬라이드 수×1P). 다운로드 전 예상 포인트가 화면에 나온다.

### 2026-08-28 — 문제은행에서 바로 편집 (고친 내용이 제자리로)

**왜 시험지 경로로는 안 되는가**
은행의 파일 이름이 곧 **문제 글의 지문**(sha1)이다. 시험지로 만들어 글을
고친 뒤 다시 저장하면 이름이 달라져 **다른 문제로 쌓인다.** 그래서 어디서
왔는지를 기억해 두고 그 자리에 되돌려 쓰는 길이 따로 필요했다.

- `POST /v1/mathocr/bank/edit` — 고른 문제로 편집용 작업을 만들고, 편집기
  문제 id → 은행 파일 이름을 `bank-refs.json` 에 적는다. 한 번에 60문제.
  단원·라벨도 함께 불러온다.
- `POST /v1/mathocr/jobs/{id}/bank-sync` — 제자리에 되돌려 쓴다.
  · 글을 고쳐 지문이 달라지면 새 이름으로 쓰고 **옛 파일과 그림을 지운다**
  · 휴지통으로 보낸 문제는 은행에서도 지운다(반영 전까지 되살릴 수 있다)
  · 새로 추가한 문제는 같은 교재에 새로 담는다
  · 글이 없어진 문제는 건드리지 않고 건너뛴다(개수를 알려 준다)
- 편집기: 은행에서 열면 헤더에 「문제은행 편집 · 과목 · 교재」 칩이 뜨고
  버튼이 「📚 문제은행에 반영」으로 바뀐다. 결과를 고침/새로담음/지움/건너뜀
  으로 나눠 알려 준다. 이 작업은 은행 것이라 임시 저장소에 또 담지 않는다.
- 문제은행 화면: 문제를 고르면 「고른 문제 편집하기」. **한 번에 한 교재씩**
  (섞이면 어디로 되돌릴지 알 수 없다).
- 이름 규칙을 `bank_digest()` 하나로 모았다 — 저장(collect_document)과
  반영(bank-sync)이 어긋나면 중복이 쌓인다.

**겸사겸사 잡은 버그 — 편집기를 여는 순간 단원이 사라지고 있었다**
워커가 AI 로 붙인 `problem["chapter"]` 를 `clean_incoming_document` 가 지키지
않아서, 편집기 **첫 자동 저장에 통째로 날아갔다.** 그래서 문제은행에 담을 때
단원이 비는 일이 있었다(그동안 `estimate_chapter` 폴백으로 가려져 있었다).
저장 항목에 `chapter` 를 넣고 편집기도 함께 보내게 했다.

**실전 시험**
글 수정·단원 변경·삭제 → 2건 고침·1건 지움 정확, 옛 파일 정리됨.
그림 있는 문제 → 편집기에 4개 다 보이고, 반영 뒤 참조·파일 4개 일치,
옛 이름 잔여 0개. 전체 문제 수 1,114 불변.

### 2026-08-27 (7차) — 문제은행 교재명·단원 고치기, 안 붙은 그림 폐지

사장님 지시 두 건.

**① 교재명·단원을 고칠 수 있어야 한다**
교재명은 파일 이름과 표지 글자로 AI 가 추측한다. 틀리면 '출처 미상 수학'
같은 이름으로 굳어 버리는데, 지금까지는 **지우고 원본을 다시 올리는 것**
말고 고칠 방법이 없었다 — 그러면 포인트를 또 낸다.
- `POST /v1/mathocr/bank/rename-book` — 과목·교재 이름 바꾸기.
  같은 이름이 이미 있으면 **합친다.** 파일이나 그림 이름이 겹치면 새 이름을
  주고 문서 안의 참조도 함께 고친다. 옮긴 뒤 빈 껍데기는 정리한다.
- `POST /v1/mathocr/bank/set-chapter` — 두 가지 방식.
  `ids` 를 주면 고른 문제만, `from_chapter` 를 주면 그 단원을 통째로.
- 화면: 교재 줄에 「이름 고치기」, 단원 줄에 「단원 고치기」, 문제를 고르면
  위에 「고른 문제의 단원 바꾸기」 줄이 나타난다.
- 실전 시험: 교재 이동 3문제·단원 통째 3문제·고른 것만 2문제 모두 정확,
  경로 탈출(`..`, `../../etc`)은 422/404 로 막힘.

**② 안 붙은 그림은 다 버리고 사용자가 캡처해 넣는다**
2026-08-22 편집기 개편으로 '그림 창고'를 여는 화면이 없어졌는데도 **서버는
계속 고아 그림과 'OCR 놓침 의심' 조각을 오려서 쌓고 있었다.** 쓸 수 없는
그림을 만드느라 시간과 디스크만 썼다.
- `add_figure_pool` 호출과 `gap_pool` 조각 저장을 없앴다. gap_pool 은 글을
  되살리는 데에만(recovered) 쓰고 남은 조각은 버린다.
- `document.drop_unused_crops()` 신설. **추출 직후 한 번만 돌린다** —
  사용자 저장 때 돌리면 그림을 지웠다가 되돌리기(Ctrl+Z)로 살릴 때 파일이
  없어 깨진다. 이 함수는 휴지통(deleted) 문제의 그림은 남긴다.
- 이미 쌓인 것 정리: 그림 21개 1,509KB 회수, 창고 0건.
- 편집기 문서 응답에서도 figure_pool 을 내보내지 않는다.
- **편집기 헤더에 「🖼 그림 넣기」 신설.** 파일 고르기와 붙여넣기(Ctrl+V)가
  같은 길(`attachImage`)을 쓴다. JPG 로 캡처해도 브라우저에서 PNG 로 바꿔
  올린다(서버는 PNG 만 받는다). 문제를 안 고르고 누르면 안내가 나오고,
  5MB 를 넘으면 올리기 전에 막는다.

### 2026-08-27 (6차) — 프로세스 전수 검토: 돌아갈 길이 끊기던 자리

코드가 아니라 **사용자가 실제로 밟는 길**을 처음부터 끝까지 훑었다.
돈이나 작업이 조용히 사라지는 자리를 찾는 데 집중했다.

**고친 것 9건**
1. **분석 중인 작업이 「내 작업」에 안 보였다.** succeeded/picking 만 보여 줘서
   128쪽 교재를 돌리는 몇 분 사이에 창을 닫으면 목록에서 사라졌다. 사용자는
   날아간 줄 알고 같은 파일을 다시 올린다 — **또 과금된다.**
   queued/processing/failed 도 보여 주고, 상태별 resume_url·label 을 서버가 준다.
2. **홈(랜딩)에 「하던 작업」이 없었다.** 내 정보 화면에만 있었는데 사용자는
   홈에서 시작한다. 업로드 상자 위 띠, 20초마다 진행률 갱신.
3. **실패한 작업이 편집기로 가서 빈 오류만 보였다.** 선택 화면으로 보내고
   실패 사유와 다음 할 일을 한 화면에.
4. **문제은행에서 만든 시험지는 원본 PDF 가 없어 편집기 왼쪽 40%가 빈 회색
   칸으로 남았다.** 원본을 못 가져오면 칸을 접고 자리를 돌려준다.
5. **다운로드에서 포인트가 모자라면 문구만 뜨고 갈 곳이 없었다.** 충전 버튼 추가.
6. **임시 저장소가 가득 차면 나갈 때 자동 보관이 조용히 실패했다.**
   pagehide 의 fetch 는 응답을 볼 수 없어 아무도 모르고 24시간 뒤 작업이
   사라진다. 편집기 진입 시 자리를 확인해 헤더에 빨간 띠로 경고.
   ※ **사장님 계정은 지금 5/5 로 가득 차 있다** — 정리가 필요하다.
7. **버려진 임시 저장본이 영영 안 치워졌다.** `_cleanup` 은 그 사용자가
   저장소를 다시 건드릴 때만 돈다. `sweep_all()` 을 전체 청소에 붙였다.
   적용 즉시 빈 사용자 폴더 4개 정리.
8. **게스트가 임시 저장소에 저장할 수 있었다.** 12시간 뒤 본인도 못 여는
   쓰레기가 되어 디스크만 먹는다. 가입 안내와 함께 막았다.
9. 가입 인증 화면에 **스팸함 확인·코드 유효시간(10분)** 안내. 인증 메일을
   못 받으면 500P 를 못 받고 그대로 이탈한다.
   업로드 상자에 쪽수 한도(500쪽) 표기 — 전에는 200MB 만 적혀 있어 600쪽
   교재를 다 올린 뒤에야 거절당했다.

**확인했고 문제 없던 것**
- 설정값과 화면 표기가 일치한다(비회원 5쪽·가입 500P·200MB·500쪽·보관 24시간).
- 편집기는 beforeunload 경고 + pagehide 자동 보관이 둘 다 있다.
- 결제 화면에 이용약관·환불 규정 링크가 있다(전자상거래법 요건).
- 비회원이 막히는 자리마다 안내 문구가 있다.
- redis 영속성, 원장 명목, 권한 경계 — 모두 정상.

**남은 프로세스 한계 (기능 결정 필요, 만들지 않음)**
- **문제은행에 잘못 붙은 교재명·단원을 고칠 API 가 없다.** 저장할 때는 고를
  수 있지만 저장 뒤에는 지우는 것 말고 방법이 없다. (교재 이름 바꾸기는
  사장님이 전에 미채택했으나, '자동 분류가 틀렸을 때의 복구 경로'라는
  성격이 다르다 — 다시 판단이 필요하다.)
- **문제에 안 붙은 그림(figure_pool)을 쓸 화면이 없다.** 추출기는 지금도
  고아 그림과 '놓침 의심' 조각을 잘라 두는데(작업당 최대 12장, 실측 21장
  1.5MB) 편집기에 그것을 부르는 코드가 없다. 2026-08-22 우측 패널을 좌측으로
  합칠 때 접근 경로가 사라졌다. 그림이 빠진 문제를 사용자가 손으로 채울
  수단이 없다는 뜻이기도 하다.

### 2026-08-27 (5차) — 백업을 서버 밖으로 (사장님 질문에 대한 판단)

**질문**: 서버 백업 기능을 추가 결제할까, 내 PC 로 보내는 스크립트면 될까.

**답: 추가 결제 불필요. 다만 PC 로 보내는 방식도 정답이 아니다 —
서버가 스스로 외부 저장소로 올리게 하는 것이 맞고, 그게 0원이다.**

근거 (실측)
- 자료는 하루 98MB, 14일치 388MB. 아주 작다.
  (_accounts.db 52K · _문제은행 49M · _보관 57M · _내시험지 1.1M · _비용로그 32K)
- 서버는 Vultr VC2 2코어/3GB/75GB.
- **Cloudflare R2 무료 한도 10GB** — 우리 0.4GB 는 요금 0원, 송신도 무료.
- **Vultr 자동 백업은 인스턴스 요금의 20%**(약 월 4,000원). 서버를 통째로
  되살리는 값어치는 있으나 ① 같은 회사 안이라 진짜 오프사이트가 아니고
  ② 하루 1회 전체 롤백만 되어 '문제은행 하나만 되살리기'가 안 된다.
- PC 로 보내는 방식은 **PC 가 켜져 있어야** 한다. 노트북을 며칠 안 켜면
  그동안 사본이 없다. 사람 상태에 백업이 걸리면 안 된다.

만든 것
- `외부백업.sh` — rclone 으로 S3 호환 저장소(R2·Vultr Object Storage)에
  올린다. 매일 04:47 cron. 백업(04:17) → 되살리기 시험(04:32) → 바깥(04:47).
  올린 뒤 **지문(sha256)을 대조**한다 — 올렸다는 말만 믿지 않는다.
  `--test` `--list` `--get <날짜> <경로>`.
  설정 전에는 cron 로그에 한 줄만 남긴다(안내문 도배 방지).
  **함정**: `.env` 를 `source` 하면 안 된다. `API_DOMAIN` 처럼 따옴표 없이
  쉼표·공백이 든 줄을 명령으로 실행하려 든다. 필요한 열쇠만 sed 로 읽는다.
  토큰은 **'읽기·쓰기'만** 주도록 안내한다 — 서버가 털려도 올라간 백업은
  지울 수 없게.
- `백업받기.ps1 -자동등록` — 윈도 작업 스케줄러에 매일 9시 등록.
  사본 하나를 더 두는 용도(3-2-1 규칙의 세 번째 사본).
- rclone v1.75.0 을 /usr/local/bin 에 설치했다.

**사장님이 할 일 (5분)**: Cloudflare 가입 → R2 → 버킷 `matharin-backup`
생성 → API 토큰(Object Read & Write) 발급 → 값 4줄을 `.env` 에 붙여넣고
`bash 외부백업.sh --test`. 그 뒤로는 자동이다.

### 2026-08-27 (4차) — 바깥 CDN 끊기, HEAD 지원, 관리자 감사 기록

**도커 재시작** — dockerd RSS 892MB → 124MB, 여유 메모리 1.8GB → 2.5GB.
백업 먼저 뜨고, 진행 중 작업 0건·큐 0건 확인 후 실행. 20초 안에 전부 복귀.

**백업 내려받기** — `백업.sh` 가 `최신백업.tar.gz` 바로가기와 sha256 지문을
만든다. `백업받기.ps1` 을 사장님 PC PowerShell 에서 실행하면
`내 문서\Matharin백업\날짜\` 로 받고 지문까지 대조한다. **반쯤 받아진 파일을
백업으로 착각하는 것이 가장 위험하다.**

**바깥 CDN 끊기 (보완점 6번)**
- 서체 조각 346개(12.2MB)를 `app/static/fonts/web/` 로 옮기고
  `matharin-fonts.css` 를 만들어 경로를 전부 우리 서버로.
  Pretendard·Noto Sans KR·Noto Serif KR·JetBrains Mono.
- pdf-lib(PDF 도구) 513KB 도 우리 서버로.
- `서체받기.py` — 서체를 갱신할 때 다시 실행. CSS 가 가리키는 조각이 전부
  있는지 스스로 확인한다.
- 랜딩은 vite 가 `@import` 를 빌드 때 붙여 넣으려 해서 CSS 가 아니라
  `landing-src/index.html` 의 `<link>` 로 걸었다.
- **CSP 를 조였다** — 바깥으로 나가는 길은 이제 결제 위젯 하나뿐.
- 실브라우저 확인(헤드리스 크로미엄, 같은 출처 페이지에서): @font-face
  1,722개 로드, 네 서체 모두 `준비=true`, 같은 글자의 폭이 서체마다 다름
  (242.0 / 270.5 / 257.6 vs 기본 서체 280.0) = 실제로 적용됐다는 뜻.
  ※ `file://` 페이지에서는 fetch 가 막혀 확인이 안 된다. 같은 출처로 띄울 것.

**HEAD 요청이 405 이던 것**
GET 라우트에 HEAD 가 붙지 않아 405 와 **JSON 오류 본문**이 돌아갔다.
감시 도구는 서버가 죽은 줄 알고, 응답 Content-Type 을 보는 검사는 엉뚱한
값을 읽는다 — 이번 검수에서 **"서체가 application/json 으로 나온다"고 잘못
진단할 뻔했다**(실제로는 `curl -I` 가 HEAD 를 보낸 탓). 미들웨어로 해결.
**함정**: 길찾기용으로 바꾼 `scope["method"]` 를 되돌리지 않으면 uvicorn 이
본문을 기다리다 "Content-Length 보다 짧다" 예외를 낸다. 첫 판에서 실제로
났고, **새로 만든 오류 일지가 그것을 잡아냈다** (일지의 첫 성과).

**관리자 감사 기록**
키 하나로 포인트 지급·결제 취소·오류 열람이 열리는데 키가 새어도 알 방법이
없었다. `app/adminauth.py` 로 문을 모으고 성공·실패를 모두 남긴다.
틀린 키로 999,999P 지급을 시도한 흔적이 기록되는 것을 확인했다.

**정정** — redis 영속성은 이미 켜져 있었다(appendonly yes + 볼륨).
3차에서 적은 "redis 가 비면 세션 세대가 0으로 돌아간다"는 걱정은 틀렸다.

**남은 보완점 갱신** (3차 목록 기준)
- 6번(CDN) 처리 완료. 7번(HEAD) 처리 완료. 8번(redis) 해당 없음.
  9번(관리자 키) 감사 기록으로 절반 처리 — 키 자체를 나누는 것은 그대로 숙제.
- 남은 것: 1번 백업 외부 이관, 5번 PDF 렌더를 워커로, 10번 오류 알림,
  11번 워커 증설, 그리고 라이브 직전 항목들.

### 2026-08-27 (3차) — 보안 헤더·비용 추적·백업 내려받기, 그리고 남은 보완점

**도커 재시작** — 34일 가동에 dockerd RSS 892MB. 재시작 뒤 124MB, 여유
메모리 1.8GB → 2.5GB. 재시작 전에 백업을 먼저 떴고, 진행 중 작업 0건·큐 0건을
확인한 뒤 했다. 서비스는 20초 안에 전부 정상 복귀.

**백업을 내 PC 로 가져가는 길**
- `백업.sh` 가 `최신백업.tar.gz` 바로가기와 `sha256` 지문을 만든다. 날짜를
  몰라도 늘 같은 경로로 받을 수 있고, 반쯤 받아진 파일을 백업으로 착각하는
  사고를 지문이 막는다.
- `백업받기.ps1` — 사장님 PC PowerShell 에서 한 번 실행하면
  `내 문서\Matharin백업\날짜\` 로 받고 지문까지 대조한다.

**보안 헤더 (Caddyfile)**
- **클릭재킹 방어가 없었다.** 결제 화면이 있는 사이트인데 남의 페이지가
  우리 화면을 투명한 틀에 넣고 클릭을 훔칠 수 있었다.
  X-Frame-Options SAMEORIGIN + frame-ancestors 'self'.
- CSP 신설. **처음 쓴 정책이 서체(구글·jsdelivr)와 PDF 도구(pdf-lib)를
  막고 있었다** — 화면 전수 확인으로 잡았다. CSP 는 잘못 쓰면 조용히 화면을
  깬다. 그래서 `CSP검사.js` 를 만들었다: 화면·CSS 가 부르는 바깥 주소를
  뽑아 지금 헤더로 허용되는지 **브라우저 규칙대로** 대조한다
  (preconnect 는 CSP 대상 아님 / CSS 의 @import 는 style-src /
  url(...font) 는 font-src). **헤더를 고치면 이걸 먼저 돌릴 것.**
- Permissions-Policy 로 카메라·마이크·위치·결제 API 를 잠갔다.

**Mathpix — 낸 돈을 버리지 않게**
상태 확인이나 결과 내려받기가 한 번 실패하면 그대로 포기했다. 제출은 이미
과금됐으므로 낸 돈을 버리고 처음부터 다시 내는 구조였다. 이 두 곳만 5회까지
재시도한다. **제출 자체는 절대 자동 재시도하지 않는다 — 그건 이중 과금이다.**

**실비가 어디에도 안 남던 것**
가장 큰 비용인 Mathpix 사용량이 기록되지 않았다. 캐시 처리분과 실제 과금분을
나눠 남기고 `GET /v1/admin/cost` 로 기간별 실비·받은 포인트를 견준다.
최근 30일 실측: AI 2.68달러(3,721원), 차감 포인트 13,244P.

**권한 경계 공격 시험 (전부 방어됨)**
남의 작업 열람·수정·다운로드·문제은행 저장 전부 403. 남의 시험지 내려받기·
저장소 열기 404(삭제 시도는 200이지만 남의 것은 그대로 남는다 — 자기 폴더에서
찾다 못 찾은 것). 경로 탈출 4종 404. 위조 토큰 3종 401.

---

## 남은 보완점 (2026-08-27 기준, 우선순위 순)

**A. 라이브 전에 반드시**
1. **백업이 서버와 같은 디스크에 있다.** 디스크가 통째로 죽으면 백업도 같이
   죽는다. `백업받기.ps1` 로 사장님 PC 에 정기적으로 받아 두는 것이 지금
   할 수 있는 최선이고, 오브젝트 스토리지 이관은 사장님 결정 사항.
2. 통신판매업 신고번호 (토스 심사 → 구매안전서비스 이용확인증 → 신고).
3. 토스 실키 전환, 테스트 계정 3개 삭제, 베타 잠금 해제.
4. 이용방법 실물 스크린샷.

**B. 다음에 손볼 것 (서비스가 도는 데 지장은 없다)**
5. **PDF 렌더가 api 컨테이너에서 돈다.** 크로미엄 한 번에 약 350MB 를 쓰고
   요청 처리와 CPU 를 다툰다. 워커로 옮기는 것이 정석이나 지금 규모(2코어·
   동시 30명 시험 통과)에서는 급하지 않다.
6. **서체·pdf-lib 를 바깥 CDN 에서 받는다.** jsdelivr·구글이 죽으면 글꼴이
   깨지고 PDF 도구가 멈춘다. 자체 호스팅하면 CSP 도 더 좁힐 수 있다.
7. **HEAD 요청이 405** — 외부 감시 도구가 HEAD 로 살았는지 확인하면 죽은
   것으로 본다. 감시 도구를 붙일 때 GET 을 쓰게 하거나 라우트를 열 것.
8. **redis 가 비면 세션 세대가 0 으로 돌아간다.** 무효화해 둔 옛 토큰이
   되살아날 수 있다(만료 전까지). redis 를 영속화하거나 세대를 sqlite 로
   옮기면 완전해진다. 지금 위협 수준에서는 감수할 만하다.
9. **관리자 API 가 키 하나로 열린다**(충전·환불·오류 일지·비용). 키가 새면
   전부 열린다. IP 제한이나 별도 관리자 계정이 정석.
10. 오류 일지는 쌓이기만 한다. 심각한 것이 생기면 **알려 주는** 장치는 없다
    (메일·알림). 라이브 뒤 하루 한 번 `/v1/admin/errors` 를 보는 것으로 시작.
11. 워커 2대 = 동시 추출 2건. 동시 10명이 몰리면 8명이 줄을 선다(대기열
    표시로 덮고 있다). 8GB 서버로 올리면 replicas 를 늘릴 수 있다.

**C. 기능 (V2 이월 확정)**
12. 정답·해설 매핑 (서버 코드 보존됨), 난이도·배점 (UI 만 걷어냄).
13. 그림이 필요한 문제은행 69건 재추출 (원본 PDF 재업로드 필요).
14. 교재 이름 바꾸기, 모바일 대응 — 미채택.

### 2026-08-27 (이어서) — 권장 사항 처리 + 운영 기반 다지기

**① 비밀번호 찾기 (없던 기능)**
`/mathocr/reset` 화면. `POST /v1/auth/forgot` → 메일 6자리 → `POST
/v1/auth/reset`. 설계 원칙 두 가지:
- **계정이 있는지 없는지 응답으로 알려 주지 않는다.** 그 차이만으로 가입자
  명단을 긁을 수 있다. 없는 주소도 같은 200 을 준다.
- 재설정하면 **기존 세션을 전부 끊는다.** 비밀번호를 찾는 이유가 '남이
  알아냈다' 일 수 있다.
주소당 1분 1통, 코드 10분, 5회 틀리면 소각. 로그인 모달·가입 화면에 링크.

**② 세션 무효화 (로그아웃해도 토큰이 살아 있던 것)**
세션 **세대 번호**를 redis 에 두고 토큰에 박는다. 무효화 = 세대 +1.
처음엔 발급 시각(iat)과 무효화 시각을 견주게 만들었는데, **무효화와
재발급이 같은 1초에 겹치면 어긋난다** — 실제 시험에서 옛 토큰이 살아남는
것을 보고 세대 방식으로 갈아엎었다. 시각 비교는 쓰지 말 것.
`/v1/auth/logout-all`, `/v1/auth/change-password`. 내 정보에 「보안」 칸.

**③ 구 크롭 파이프라인(/v1/jobs 6개) 잠금**
화면은 아무도 안 쓰는데 **포인트 차감도 횟수 제한도 없이** 300DPI 렌더와
OCR 을 돌리는 경로였다. `legacy_crop_api_enabled`(기본 꺼짐). 라우트
의존성으로 걸어야 본문 검증 전에 404 가 난다(함수 안에 두면 422 가 먼저).

**④ 백업 — 빠진 것이 있었다**
`_내시험지` 가 묶음에 없었다. 사용자가 내려받은 시험지 원본이라 잃으면
포인트를 또 받아야 한다. 추가.
`백업.sh --verify` 신설 — 묶음을 **실제로 풀어** 원장 무결성·문제은행
JSON·그림 파일 존재를 확인한다. **열리는 묶음과 되살아나는 묶음은 다르다.**
cron 04:32 매일. 첫 복원 훈련: 1,114문제 전부 복구·깨진 파일 0·그림 빠짐 0·
현재 서비스와 건수 일치.

**⑤ 오류 가시성 — 지금까지 터져도 알 방법이 없었다**
`app/errorlog.py` 신설. 처리되지 않은 예외·5xx·워커 실패를 JSONL 로 남긴다
(이메일은 앞 두 글자만, 4MB 넘으면 앞부분 폐기). 전역 예외 처리기 2개 —
사용자에게는 늘 같은 문구, 자세한 것은 일지에만. 기존 응답 모양(dict
detail 포함)이 그대로인 것을 확인했다. `GET /v1/admin/errors`.

**⑥ 서버 자원 — 남의 것이 메모리를 잡고 있었다**
- `/root/ocr테스트` 의 surya 실험 서버 2개가 **8일째** 떠서 스왑 1.6GB 를
  물고 있었다(제품과 무관, 실행 파일은 이미 지워진 상태). 정리 → 스왑
  3.1GB → 1.6GB.
- 도커 빌드 캐시가 또 10.45GB(전에 15GB). 정리 → 디스크 60% → 54%.
  **deploy.sh 가 7일 지난 캐시를 자동으로 버리게 했다.**
- `redis_client()` 가 호출마다 새 연결 풀을 만들고 있었다. 상태 폴링은
  1.5초마다 돌고, 업로드 중복 검사는 **루프 안에서** 매번 만들었다.
  lru_cache 로 하나만 쓰게 고침.
- 부하 시험: 동시 30명 300요청 전부 성공(중앙 82ms·95% 424ms). API 는
  넉넉하다. 병목은 워커 2대(동시 추출 2건)이고 대기열 표시가 그것을 덮는다.
- PDF·PPT 만들기는 크로미엄 하나만 돌린다(2코어라 옳다). 그런데 **줄 서는
  시간이 5초뿐이라 동시에 두 명만 눌러도 뒷사람이 실패**했다. 실측 렌더
  3초 안팎 → 60초 대기로.

**⑦ 안내 문구**
비회원 한도·다운로드 잠금처럼 '가입하면 풀리는' 막힘에 오류 문구만 던지던
것을 가입 버튼이 있는 안내창으로(signup_required).

**⑧ 실전 전 구간 시험 (캐시 사용, 실비 0원)**
업로드 → 3쪽 선택 → 추출(15.8초, 6문제, 원본 쪽 번호·단원 정확) →
문제은행 저장 → PDF 다운로드(2쪽 2P) → 재다운로드 무료 → 내 시험지 보관
→ **0문제일 때 환불**(10P 반환, 실패로 안내)까지 실제로 돌려 확인.
시험 흔적(작업 폴더·원장 4줄·은행 교재·시험지)은 전부 되돌렸다 — 잔액과
문제은행 1,114건이 시험 전과 같다.

**남은 권고**
1. 백업이 **같은 디스크**에 있다. 디스크가 죽으면 같이 죽는다. 외부 이관은
   라이브 직전 사장님과 정하기로 한 항목.
2. 도커 데몬이 34일 가동에 RSS 1.1GB. 재시작하면 회수되지만 잠깐 끊긴다 —
   라이브 전 조용한 시간에.
3. PDF 렌더가 api 컨테이너에서 돈다. 요청 처리와 CPU 를 다툰다. 워커로
   옮기는 것이 정석이나 지금 규모에서는 급하지 않다.

### 2026-08-27 — 프로그램 전수 검수 (코드 15,167줄 + 화면 5,183줄), 결함 13건 수정

사장님 지시로 프로세스·코드를 전부 읽고 실제로 돌려서 검증했다.

**가장 큰 것 — 가입이 죽어 있었다**
`accounts.send_verification_mail` 이 `MIMEMultipart` 를 import 없이 썼다.
그 줄이 try 밖이라 `NameError` 가 그대로 올라가 `POST /v1/auth/signup` 이
500 으로 떨어진다. **베타 잠금이 signup 을 먼저 503 으로 막고 있어 가려져
있었을 뿐, 잠금을 푸는 순간 아무도 가입할 수 없다.** 인증 메일 → 인증 →
가입 500P → 다운로드·충전이 전부 이 한 줄에 묶여 있었다.
교훈: 예외를 삼키는 try 는 **그 앞줄**을 지켜 주지 않는다. 메일 조립도
try 안으로 옮겼다.

**돈이 새는 자리 — 전부 '받았는데 못 준' 경우**
`accounts.refund_charge(job_id, reason)` 신설(명목 단위, 중복 환불 차단).
- `/extract`: 차감 뒤 큐 등록이 실패하면 아무 일도 안 일어나는데 환불이
  없었다 → 되돌리고 picking 으로 복귀.
- 다운로드 3종: 차감 뒤 렌더 실패 시 환불이 없었다 → 되돌림.
- **추출 결과가 0문제여도 성공 처리**되어 빈 문서에 쪽수×10P 를 받았다 →
  환불하고 실패로 알린다.
- `charge`/`charge_points` 가 잔액을 읽고 쓰는 사이에 잠금이 없어 동시
  요청이 이중 차감될 수 있었다 → `BEGIN IMMEDIATE`.
- 결제: 1차 승인이 튕겨 `failed` 가 된 주문은 재승인돼도
  `WHERE status='pending'` 에 걸리지 않아 **돈만 받고 포인트를 안 줬다**
  → `IN ('pending','failed')`.
- **환불 실행 수단이 아예 없었다.** 약관엔 '미사용 충전분 7일 이내 전액
  환불'을 적어 놓고 코드에 창구가 없다 → `/v1/admin/payments/refund`
  (토스 취소 + 포인트 회수, 미사용분만, 관리자 키).

**기능 결함**
- 선택 화면에서 드래그로 감싼 영역이 그림으로 담기면 이름이
  `pick_XXXXXXXX.png` 인데 `document.IMAGE_NAME` 은 `crop_NNNNN.png` 만
  받는다 → **편집기 자동 저장이 422 로 계속 실패**. 이름 규칙에 추가.
  (아직 실제 발생 이력 0건 — 터지기 전에 잡았다.)
- `/answer-key` 응답의 `len(found)` 가 int 에 len() — TypeError (V2 보존 코드).
- 내려받는 파일 이름이 늘 `workbook.pdf` → 시험지 제목으로.

**안전·비용**
- `/openapi.json` 이 열려 있어 관리자 API 를 포함한 전체 API 지도가 공개
  → `openapi_url=None`.
- **비회원 업로드 무제한** — 게스트 토큰은 인증 없이 무제한 발급되고
  비회원도 앞 5쪽은 실제로 Mathpix 를 태운다. 스크립트 하나로 실비가 샌다
  → IP 하루 5회.
- 로그인 시도 제한 없음(비밀번호 대입) → 계정 10회·IP 60회 / 10분.
- `analysis-cache` 가 청소 대상에서 빠져 한 번도 줄지 않았다(전에 15GB)
  → 14일 자동 정리. mathpix-cache 는 지우면 실비가 다시 나가므로 유지.
- PNG 업로드가 24바이트 미만이면 struct 오류로 500 → 길이 검사.

**문서**
- 개인정보처리방침에 **구글 로그인**이 통째로 빠져 있었다(수집 항목·위탁·
  국외이전). Google LLC 3개 항목 추가.

**검증** — 화면 13종 렌더 오류 0건, 추출 회귀(교재 74종·2,992문제) 변화
없음, 다운로드 3종 실제 생성 정상, 환불·중복환불·잔액초과·로그인제한을
실제 호출로 확인.

**남은 권고 (미구현, 사장님 판단 필요)**
1. **비밀번호 찾기가 없다.** 잊으면 계정을 영영 못 연다. 라이브 전 필요.
2. 세션 토큰을 무효화할 수단이 없다(로그아웃해도 토큰은 만료까지 유효).
3. `_시험` 폴더 95MB — 견본이 쌓였다. 라이브 전 정리 대상.
4. `QUEUE_TEST_DELAY=3` 아직 켜져 있다(모든 작업에 3초 인위 지연).

### 2026-08-27 — 전체 재검증 + 토스페이먼츠 심사 기준 대조

**전체 검증 결과 — 전부 통과**
- 정적 화면 13종 jsdom 렌더 검사: 오류 0건 (홈·문제선택·편집기·문제은행·
  내 시험지·내 정보·충전·가입·약관·PDF도구 3종·랜딩).
- 추출 회귀 검사: 교재 74종 · 2,992문제. 기존 교재 문제 수 **변화 없음**,
  병합 의심 0 · 활동 유입 0 · 진짜 문제를 더 버리는 규칙 없음.
  (새 교재 2종은 사장님 최근 업로드분 +8.) 스크립트는 이미지에 안 들어가
  있으므로 `docker cp 추출품질검사.py worker:/srv/` 후 실행할 것.
- 다운로드 3종 실제 렌더(배점 표시 끈 상태): PDF 1.1MB · HWPX 1.1MB ·
  PPTX 166KB 모두 정상. 배점 제거가 출력에 영향 없음을 확인.
- 인프라: 디스크 59%, 컨테이너 5개 정상, 일일 백업 08-24~26 3일치 존재.

**토스페이먼츠 심사 기준(notice/20057) 대조**
읽은 문서: '전자계약 신청서 작성 완료 후 절차 안내'. 심사 항목별 현황 —
1. 홈페이지 하단 정보 — **당일 충족 완료.** 토스가 요구하는 건 상호·
   사업자등록번호·대표자명·**사업장 주소·유선번호** 5종이며 통신판매업
   신고번호는 **목록에 없다**(중간 보고에서 잘못 적었던 것을 정정).
   사장님이 준 값으로 기입: 서울특별시 서초구 동산로13길 12, 402호 /
   010-8502-1334 (휴대폰번호도 토스가 인정한다).
   랜딩에 `BIZ` 상수 + `BizInfo` 컴포넌트를 두어 **빈 값은 렌더하지 않는다**
   (허위 표기 방지). 랜딩 하단·이용약관·충전 화면 세 곳 동일 반영.
   통신판매업 신고번호만 공란 — 토스 심사 통과 → 구매안전서비스 이용확인증
   발급 → 신고 순서라 아직 나올 수 없다.
2. 상품 검수 — 충족. 요금제 섹션이 비로그인에게 보이고, 빈 카테고리 없음.
3. 신용카드 결제창 연동 — 충족. payments.py 가 토스 위젯 테스트키로 연동됨
   (실키 전환은 신고번호 이후).
4. 계좌이체(퀵계좌결제) 불가 업종/구조 — 해당 없음.
5. 기타 심사 불가 사례 — 충전 서비스 10만원 미만 규정 대비 최고가 54,900원
   OK. '상품 금액과 결제 금액 상이' 대비 서버가 주문 금액을 대조 검증하고
   있고, 요금제·충전 화면에 **'표시 금액은 부가세(VAT) 포함, 결제 금액과
   동일'** 문구를 새로 넣었다.
구비 서류는 토스 홈페이지로 직접 신청하면 우편 발송이 생략된다(가비아·
고도몰·SIR소프트·메이크샵 경유일 때만 우편).

### 2026-08-26 — 편집기를 「문제은행 저장 전용 화면」으로 재구성 (사장님 지시)

확정된 사용자 순서: **PDF 업로드 → 문제 선택 → 문제 추출 → 편집기에서
문제 수정·문제은행 저장 → 시험지 생성 → 다운로드.**
편집기는 이제 '시험지를 조판하는 곳'이 아니라 **'문제를 다듬어 은행에
넣는 곳'** 이다. 조판 옵션은 시험지 생성(미리보기) 화면으로 옮겼다.

- **오른쪽 목록을 1단 연속(.flow)으로.** 쪽 분할(sheetwrap/sheet)·쪽 머리글
  (sheetno) 제거. 문제만 한 줄로 쭉 이어진다. 왼쪽은 원본 패널이 기본 열림.
- **원본 패널 헤더(.src-head) 제거** — 그 높이만큼 원본이 커진다
  (layoutSrcPane 의 viewH 계산에서 헤더 높이 차감을 뺐다).
- **카드에서 덜어낸 것**: 출제 체크박스(.pickbox), '원본 N쪽' 버튼(.pg.pgbtn),
  배점 입력칸(.card-points), 배점 표시(.head .pts). 목차 체크박스도 제거.
- **좌측 패널(rail) 통째 제거.** 그 안에 있던 것들의 새 자리:
  문제 가져오기·순서 섞기·단축키 → **헤더 버튼**,
  단수·쪽당 문항·폰트 크기 → **시험지 만들기(미리보기) 화면**.
  숨은 스텁 `<div hidden>` 에 기존 id 를 남겨 두어 기존 코드가 안 깨진다.
- **헤더에 「📚 문제은행에 저장」 신설** — 기존 교재에 추가할지 새 교재로
  담을지 고를 수 있다(`GET /v1/mathocr/bank` 로 교재 목록을 채운다).
  누르면 `POST /jobs/{id}/bank-save`, 저장하면 나갈 때 임시 저장소 자동
  보관은 건너뛴다(bankSaved).
- **난이도·배점은 V2 로 이월** — 편집기(난이도 5버튼·AI 채점·배점 입력·
  배점 표시)와 문제은행 화면(난이도 칩 필터·난이도별 무작위 뽑기·
  'AI로 난이도 채우기' 배너)에서 UI 를 전부 덜어냈다. **서버 코드
  (`_grade_with_ai`, `/auto-grade`, `/bank/fill-difficulty`, difficulty·
  points 필드)는 그대로 남겨 둔다** — 정답·해설 때와 같은 방식.
  자동 호출은 없으므로 비용도 0원. 무작위 뽑기는 '개수만' 방식으로 바뀜.

**이번에 잡은 사고 — jsdom 스모크 테스트로 발견**
좌측 rail 을 지웠더니 `document.querySelector('.left-rail').appendChild(grip)`
(서랍 손잡이)과 햄버거 버튼 핸들러가 **최초 스크립트 실행에서 죽었다.**
`node --check` 는 문법만 보므로 못 잡는다. 편집기가 통째로 흰 화면이 될
사고였다. 실물 테스트 전에 **jsdom 으로 화면을 띄워 보는 것**을 표준으로 삼는다
(scratchpad/smoke.js — fetch 를 가짜 응답으로 막고 렌더·모달 클릭까지 확인).

### 2026-08-26 — 내 시험지 신설 + 문제은행 저장 선택식 (2차 흐름 점검)
2차 사용자 흐름 점검에서 나온 5건 중 사장님이 4건 채택.
- **문제은행 오염 차단**: 저장 체크를 **기본 꺼짐**으로. 켜면 **기존 교재를
  고르거나 새 이름**을 적는다(`bankTargetBook()` → bank-save 의 book).
  전에는 편집기 제목을 교재명으로 써서, 문제은행에서 만든 시험지를 내려받으면
  '3월 주간테스트' 같은 가짜 교재가 생기고 문제가 복제됐다.
- **내 시험지** (`app/mathocr/sheetbox.py`, `/mathocr/sheets`):
  내려받은 **파일을 그대로** `_내시험지/{사용자키}/` 에 보관(최근 30개).
  다시 받을 때 재생성하지 않으므로 **포인트 0**. PDF·한글·PPT 모두 훅 연결.
  다시 만들지 않는 이유 — 조판·그림이 그때와 달라질 수 있고 사용자가 원하는 건
  '그때 그 파일'이다. MY PAGE 메뉴·내 정보에 링크 추가.
  실측: PDF 1,080,542바이트 저장 → 다시 받기 동일 바이트.
- **비회원 전환**: 다운로드 403 을 `{code:"signup_required"}` 로 바꾸고 편집기가
  **'가입하고 내려받기 →'** 버튼을 띄운다. 안내 문구 **100P → 500P**(3곳 정정).
- **잔액 노출**: 편집기 헤더에 남은 포인트(클릭 시 충전 창), 프로필의
  '잔여 크레딧' → '남은 포인트'.
- 채택 안 한 것: 교재 이름 바꾸기(4번), 모바일 대응(5번) — 5번은 편집기를
  좁은 화면용으로 다시 짜야 해서 라이브 후 실제 요청을 보고 판단.

### 2026-08-25 — 강사 흐름 점검 결과 반영 (사장님 선별 지시)
사장님과 함께 전체 흐름을 훑고 8개 지적 중 5개를 채택·반영했다.
- **① 선택 임시저장**: 고른 표시(점·박스·쪽 전체·현재 쪽)를 localStorage 에
  3일 보관, 재진입 시 복원하고 '이전 선택을 불러왔어요' 표시.
  `my-jobs` 가 **picking 상태도 반환**(status 필드 추가), 내 정보에서
  '문제 고르는 중 · 이어서 고르기' → `/mathocr/pick#job` 으로 연결.
  (그전에는 선택 도중 창을 닫으면 되돌아갈 길이 아예 없었다)
- **② 실시간 잔액 판정**: 고를 때마다 차감 예정 P 를 계산해 잔액을 넘으면
  즉시 충전 팝업(필요 P·잔액 표시, '충전하러 가기'/'선택을 줄일게요').
- **③ 시험지 머리말(이름·점수란)** — 사장님 판단으로 **넣지 않음**
  ("우리가 만드는 게 꼭 시험지는 아니다").
- **④ 정답지** — V1 범위 정리 후 서비스 전에 최대한 진행 예정(V2 이관 유지).
- **⑥ 다운로드 후 갈림길**: '다른 PDF 올리기' / '내 문제은행 보기' 두 버튼.
- **⑦ 문구**: 환불 안내 '크레딧 N쪽' → 'N P 를 돌려드렸습니다'.
- **⑧ 중복**: 문제 가져오기 팝업에서 현재 문서에 이미 있는 문제에
  '이미 담김' 배지 + 흐리게. 판정은 `/bank/preview` 가 새로 내려주는
  `plain`(수식을 #로 바꾼 평문 90자)과 편집기 문서의 같은 방식 키를 비교.
- 실측: picking 작업이 최근 작업에 뜨고, preview 에 plain 필드 확인.

### 2026-08-25 — 문제은행 교재 재추출·갱신 (비파괴, 비용 0원)
- **원본 PDF 는 24시간 만료로 없다.** 하지만 **OCR 캐시(sha256)가 남아 있어**
  5개 교재 모두 재인식 없이(0원) 다시 추출할 수 있었다.
  캐시↔교재 대조는 은행 문제의 대표 문장이 캐시 본문에 있는지로 확인.
  교학사 중등수학=0424cf8f · 확통=98f02b3d · 비상 수학=e065e5f3 ·
  미래엔 수학2=fef582e1 · YBM 미적분=09353743
- 통째 교체를 **하지 않았다**. 사전 계산에서 '없어짐 197건' 중 99건이
  '다음 □ 안에 알맞은 것을 쓰시오' 같은 **진짜 문제**였다. 새 규칙이 버린 게
  아니라 경계가 바뀌어 해시가 달라진 것도 섞여 있어, 지우면 손실이 난다.
- 또 하나의 함정: 텍스트 유사도 0.72 로 '보정(덮어쓰기)' 하려다
  **'유한소수' 문제를 '순환소수' 문제로 덮을 뻔했다.** 유사도로 덮어쓰기는
  금지. → **추가만 하는 방식**으로 전환(0.95 이상 거의 동일한 것만 건너뜀).
- 결과: **+100문제** (교학사 중등수학 +36 · 비상 수학 +47 · 확통 +12 ·
  미래엔 +3 · YBM +2), 삭제 0건. 추가분 단원은 fill-chapters 로 100건 채움.
  **1,014 → 1,105문제.**
- **보류 69건**: 그림이 필요한 문제인데 원본 PDF 가 없어 그림을 복원할 수 없다.
  해당 교재를 다시 업로드해 추출하면 그림까지 들어온다(그때는 쪽수×10P 과금).
- 교훈: 은행 갱신은 **추가만** 하고 삭제·덮어쓰기는 하지 말 것. 해시가 달라도
  같은 문제일 수 있고, 비슷해도 다른 문제일 수 있다.

### 2026-08-24 — 발문 잘림 37건 원인 추적: 결함 2개 + 코퍼스 구멍 1개
- 은행의 잘린 문제 24건은 **전부 08-18~21 적재**(규칙 수정 이전) = 옛 추출물.
  현재 파이프라인을 다시 감사하니 개발기 교재(runs 23종)에서는 978문제 중 2건뿐.
- **그런데 회귀 코퍼스에 구멍이 있었다**: `runs/` 는 개발기 교재고,
  **사장님이 실제 올린 교재는 `sha256/` 에 있다.** 그쪽을 안 봐서 실제 결함을
  놓치고 있었다 → 추출품질검사 CORPUS 를 둘 다 보도록 수정(23종 → **72종**).
- 그러자 결함 두 개가 드러났다:
  ① **OCR 이 어미를 띄어 읽는다** — '구하시 오', '보이 라'. 어미 검사가
     `시오` 를 붙여서만 찾아 통과 못 했다. ORDER_END·ORDER_END_ANY·
     SELF_ORDER·strong 에 공백 한 칸 허용(`시\s?오` 꼴).
  ② **'번호 뒤 단위·조사' 규칙이 너무 넓었다** — '5 **일**차함수'의 일,
     '21 **에**서'의 에를 단위로 오인해 진짜 문제 6건을 버렸다.
     · 헷갈릴 일 없는 말(이상·이하·시간·자리·마리…)은 그대로
     · 한 글자라 다른 낱말 첫 글자일 수 있는 말(일·시·점·배·에·의…)은
       **뒤에 조사·구두점이 올 때만** 인정
     · 그 줄이 발문으로 끝나면(SELF_ORDER) 단위 판정보다 우선
       ('2 점 G 의 좌표를 구하시오' 가 '2점'으로 죽던 것)
     자체 시험 10/10 통과. 이어지는 줄은 앞 가드(CONTINUE_SUBTYPES)가 먼저 잡아
     '6 일 때, …구하시오' 같은 연속 줄은 여전히 제외된다.
- 결과: **72종 2,957 → 2,984문제**, 모든 규칙에서 진짜 문제 유실 **0건**,
  병합 0, 활동 유입 0. 새 기준선 저장.
- 은행의 24건은 옛 추출물이라 그대로 둔다(내용은 진짜 문제). 그 교재를 다시
  올려 추출하면 고쳐진 규칙으로 들어온다.
- **교훈**: 회귀 코퍼스는 '개발기 자료'가 아니라 **사용자가 실제 올린 자료**를
  포함해야 한다. 아니면 실제 결함을 못 본다.

### 2026-08-24 — 문제은행 전수 점검 (1,023문제 다 읽음) + 원인 수정
- 표기: `classify` 의 출판사 미상 폴백을 **'미상' → '출처 미상'** 으로.
- 'calc_91-180_원본.pdf' 에서 온 '수학/미상 수학' 151문제 제거(사장님 지시).
- **전수 점검 결과 이상 46건**(나머지는 정상):
  | 유형 | 건수 | 조치 |
  |---|---|---|
  | 발문 어미 잘림(진짜 문제) | 33 | 보존 — 경계 판정 한계 |
  | 정답지 페이지 조각 | 5 | **원인 수정** + 은행에서 제거 |
  | 개념 설명 유입 | 4 | 은행에서 제거 |
  | 발문 머리 잘림(진짜 문제) | 4 | 보존 |
- **원인 수정**: 교과서 뒤쪽 **정답지 페이지**가 문제로 들어오고 있었다
  (교학사 중등수학). `service._GUIDE_WORDS` 에 '정답과 해설/정답 및 해설/
  빠른 정답/해설편/답과 풀이/체크하기/스스로 준비하기' 추가 — 안내 페이지와
  같은 방식(그 낱말이 있고 실제 발문이 1개 이하인 쪽은 통째 제외).
  회귀 검사 통과: 교재 23종 978문제 변화 없음.
- 문제은행 현재: **5개 교재 1,014문제** (YBM 미적분 10 / 비상 수학 462 /
  미래엔 수학2 92 / 교학사 중등수학 256 / 교학사 확률과 통계 194).
- 남은 한계(수정 안 함): 발문의 머리·꼬리가 잘리는 37건. 문제 경계를 AI가
  다듬는 단계의 한계라 규칙으로 밀면 오탐이 는다. V2 에서 경계 판정을
  손볼 때 함께 볼 것.

### 2026-08-24 — 클릭 정확도·디스크·문제은행 정리
- **클릭 선택 3단 판정** (_apply_picks): ① 인식 영역 안(허용오차를 고정 10px→
  쪽 높이 1%로, 해상도 무관) ② 세로로 겹치는 문제(2단 조판의 옆 단) ③ 가장
  가까운 것, 단 쪽 높이 12% 이내일 때만. 멀리 빗나간 클릭은 억지로 붙이지 않고
  기존 쪽 단위 안전장치가 받는다. 검증: 문제 중심 클릭 11/11 정확.
- **디스크 74% → 54%** (15.8GB 확보):
  · docker builder prune (24h 초과) 15.1GB — **디스크가 차면 여기부터 볼 것**
  · `/data/jobs/analysis-cache` 719MB 삭제 — 구 크롭 파이프라인(/v1/jobs) 캐시로
    마지막 사용 2026-08-13. 현재 제품(/v1/mathocr)은 mathpix-cache 를 쓴다.
  · ⚠ `/data/mathpix-cache` (30MB) 는 **절대 지우지 말 것** — 재인식 비용이 든다.
- **문제은행 점검 결과: 쓰레기 거의 없음**. 1,175문제 전부 사장님이 올린 실제
  교재였고, 글 15자 미만 껍데기 1건도 정상 문제였다. 정리한 것은
  · 중복 폴더 '확률과 통계/확률과통계교학사'(1문제) 삭제 — 내가 2026-08-22
    bank-save 테스트로 만든 것. 본 폴더에 같은 해시 문제가 있음을 확인 후 삭제.
  · 단원 미지정 19건 → /bank/fill-chapters 로 채움 (확통 7단원 정리 완료)
  현재: 6개 교재 1,174문제. 남은 개선점은 '수학/미상 수학'(151문제) 교재명이
  파일명에 정보가 없어 '미상'인 것 — 이름 바꾸기는 사장님 판단 사항.

### 2026-08-24 — 화면 없이 진행한 작업: 회귀 검사 자동화 + 일일 백업
- **추출 품질 회귀 검사** `/opt/math-workbook-backend/추출품질검사.py`
  (컨테이너 경로 /srv/app/추출품질검사.py). 교재 23종 코퍼스로 무료 실행.
  교재별 문제 수·버려진 후보 사유별 수·**그중 진짜 발문 수**·병합 의심·활동
  유입을 한 번에 계량하고, 기준선(/data/jobs/_시험/추출기준선.json) 대비 diff.
  진짜 발문을 더 버리면 종료코드 1. 규칙 손댈 때마다 먼저 돌릴 것.
  `--save` 는 결과가 옳다고 확인했을 때만.
  기준선(2026-08-24): 23종 978문제, 병합 0, 활동 유입 0, 버려진 진짜 발문 0.
- **일일 백업** `/opt/math-workbook-backend/백업.sh` — 되살릴 수 없는 것만
  (_accounts.db 원장, _문제은행, _보관, _비용로그). sqlite backup API 로
  안전 사본 → tar.gz. 매일 04:17 cron. 14일 보관. /var/backups/matharin/.
  `--list`, `--restore <날짜>`(안내 출력) 지원.
  **되살리기 실측 검증**: 사용자 6명·원장 62건·잔액 988,197P 실서버와 일치.
  ⚠ 사고 하나 잡음: `docker exec` 에 **-i 가 없어 0바이트 사본**이 만들어졌다.
  검증 안 했으면 '백업이 있다'고 착각했을 것. 이제 1만 바이트 미만이면 실패.
  ⚠ 같은 디스크에 둔다 — 디스크 자체 고장은 못 막는다. **외부 저장소 이관은
  라이브 전 사장님과 논의**(pre-launch-checklist 1번 항목).
- 재추출 과금 '쪽 단위 무료 판정'은 폐기 — 2026-08-22 사장님 확정이
  "같은 PDF 재추출도 매번 차감" 이므로 손댈 것이 없다.

### 2026-08-24 — 추출 규칙 전수 재정립 (교재 23종 감사)
- **감사 도구**: `extract.DEBUG_DROPS/DROP_LOG` 추가(기본 꺼짐). 켜면 후보가
  어느 가드에서 떨어졌는지 (사유, 글, 교재, 쪽) 로 쌓인다. 규칙을 손댈 때마다
  이걸로 계량할 것.
- **판정 기준**: 버려진 후보 중 '명령형으로 끝나는 것'이 있으면 그 규칙은
  진짜 문제를 죽이고 있는 것이다.
- 감사 결과(탈락 233건):
  · 건강 — 명령형 어미 없음 69 / 이어지는 줄 55 / 번호 뒤 단위·조사 41 /
    개념 꼭지 제목 40 → 진짜 발문 0건 (이어지는 줄 8건은 줄바꿈 오인,
    본문 중간 숫자라 버리는 게 맞다)
  · **불량 — '복습 코너 쪽' 21건 중 15건, '도입 코너 구간' 4건 중 2건이
    진짜 발문**. 쪽·구간 단위로 통째 버린 탓.
- **수정**: `SELF_ORDER`(그 줄이 '…라./…시오' 로 끝나는가) 예외를 달아
  쪽 단위 배제를 발문 단위로 정밀화. '보자'는 SELF_ORDER 에 없으므로
  활동 제외 방침은 그대로.
- 결과: 23종 **968 → 978문제**(지학사 +5, 비상 고등수학1 +3, 등차수열 +1,
  비상 미적분 +1), 활동 유입 0건, 잔여 탈락에 명령형 0건.
- **병합 감사**도 함께: 문제 본문 안에 다른 문제 표기가 남은 건 7건뿐이고
  전부 줄바꿈 오인(예: '…10 / 2 개의 공을…'). 실제 병합은 지학사 건으로 끝.
- 문제 길이 분포(참고): 중앙값 138자, 90%지점 267자, 최대 1037자.
  500자 이상 18건은 확인 결과 다문항 문제로 정상.

### 2026-08-24 — 명령형 어미 규칙 일반화 (교재 23종 전수조사)
- 증상(사장님): 지학사수학1 7쪽에서 **예제2와 문제3이 한 문제(629자)로 병합**.
- **원인**: 문제3은 '3.' 숫자만으로 시작하는 **약한 표기**라 본문에 명령형
  어미가 있어야 인정되는데, 발문이 "…성립함을 **보이라**." 였고 규칙이
  `시오|하라|여아으라|구해라|말해라` **열거식**이라 '보이라'가 빠졌다.
  시작점이 죽으니 그 줄부터 앞 예제 본문으로 흡수됐다.
  ⚠ 규칙이 **두 곳**에 있다: 후보 단계 `ORDER_END`/`ORDER_END_ANY`
  (window_has_order_end), 사후 검사 `strong`. **둘 다 고쳐야 한다** —
  사후 검사만 고쳤을 때는 증상이 그대로였다.
- **1번(규칙 보강) vs 2번(AI 판정) 판단**: 1번 채택. 근거는 한국어 명령형이
  닫힌 형태소 집합이라 **꼴로 일반화하면 열거가 필요 없다**는 점. 비용·지연 0.
- **전수조사 근거**: /data/mathpix-cache/runs 23종(5개 출판사) 976개 약한 표기
  후보 분석 → '…라'로 끝나는 46건 **전부 진짜 발문, 오탐 0건**. 열거식은
  나타내라(8)·보이라(4)·풀라(4)·그리라(2) 등 18건(13%) 누락.
  단, `[가-힣]라`를 무앵커로 쓰면 "규칙에 **따라** 수열을…" 이 걸린다(실측
  오탐 3건) → **문장 끝(마침표 앞 또는 끝)** 으로 못 박아 해결.
- 결과: 지학사 1단원 13→17문제, 7쪽 예제2(92자)/문제3(138자) 분리 확인.
  23종 합계 968문제. '…해 보자' 활동 제외 정책은 그대로 유지.
- 재사용 가능한 검증 자산: `/data/mathpix-cache/runs` 23종은 **무료 회귀
  테스트 코퍼스**다. 추출 규칙을 손대면 여기서 전/후 문제 수를 비교할 것.

### 2026-08-23 — '원본 N쪽' 버튼이 안 보이던 진짜 원인 (테마 CSS !important)
- 사장님이 **네 번** 지적. 편집기 CSS(`.pg.pgbtn`)를 고치고 인라인 스타일까지
  넣었는데도 그대로였다.
- **진단 방법(추측 금지)**: 헤드리스 크로미엄으로 편집기를 iframe 에 띄우고
  `getComputedStyle` 을 찍어 봤더니 `bg: rgba(243,240,234,.03)`,
  `color: rgb(243,240,234)` — 흰 종이 위 크림색 글자라 사실상 안 보였다.
- **원인**: 테마 주입 레이어 `matharin-studio-polish.css` 의
  `.matharin-editor button { background: rgba(243,240,234,.032) !important;
  color: … !important }` 가 모든 버튼을 덮고 있었다. **!important 라 인라인
  스타일도 밀린다.** 편집기 파일만 고쳐서는 절대 안 바뀐다.
- **수정**: 같은 파일에 `.matharin-editor .pg.pgbtn{...!important}` 예외 추가
  (진한 회색 #4b5261 채움 + 흰 글씨), `POLISH_HREF` 버전을 20260823a 로 올려
  캐시 무효화. 캡처로 확인 완료.
- **교훈**: 편집기·가입 화면은 `app_theme.install_app_theme` 이 3개 CSS 를
  주입한다(app-theme / studio-polish / typography-polish). 스타일이 안 먹으면
  **여기부터 확인**할 것. 화면 문제는 캡처+computed style 로 검증할 것.
- 같은 라운드: 좌측 패널 기본 폭 313px(+20%), '문제 가져오기' 문구,
  원본 박스를 position:fixed 로 완전 고정(헤더 아래 10px·좌 10px·하단 10px,
  본문은 패널 폭만큼 밀어냄 — 스크롤해도 움직이지 않음).

### 2026-08-23 — 정답·해설 기능 V2 로 이관 (현재 버전에서 제외)
- 사장님 결정: **매핑 정확도가 부족해 이번 버전에서는 정답·해설을 전부 뺀다.**
- 제거된 것:
  · 편집기 카드 하단 '정답:/해설:' 두 줄 입력, 해설지 PDF 연결 버튼·핸들러
  · 미리보기 '정답 및 해설 표시' 옵션 (preview 에 answers 파라미터 안 보냄)
  · 랜딩 업로드 팝업의 해설지 첨부 + stash 호출 + 완료 화면 연결 결과 안내
  · worker 의 해설지 자동 연결 단계, service 의 인쇄된 풀이·답 수집 호출
- **남겨 둔 것 (V2 재사용)**: `/jobs/{id}/answer-key`, `/answer-key-stash` 라우트,
  `answer_key_apply`·`answer_key_from_source`·`_collect_printed_solutions` 함수,
  문제은행 항목의 answer/explanation 필드, 미리보기 answers 렌더링 코드.
  → V2 에서는 UI 만 되살리면 된다.
- 실측: 제거 후 추출 정상(YBM 10~12쪽 6문제, 정답 채워진 문제 0).
- 같은 라운드에서 함께: 학습 코너 표기를 발문 머리에서 제거('스스로 하기'가
  본문에 남던 것), 원본 패널 폭을 쪽 비율로 계산, 문제 수가 적을 때 정답 영역이
  쪽 바닥으로 밀리던 그리드 stretch 수정(align-content:start).

### 2026-08-23 — 안내 페이지 오탐 차단 (구성과 특징 쪽)
- 증상: YBM 4·5쪽('이 교과서의 구성과 특징')이 문제로 추출됨. 그 쪽은
  '스스로 하기' 같은 코너를 **소개만** 하는 페이지.
- **진단**: `_drop_weak_learned` 의 발문 판정이 통과시켰다. 원인은
  Mathpix 가 그림을 `![](https://...jpg?height=..)` 로 넣는데 **그 주소의
  물음표**를 '발문의 물음표'로 오판한 것. (텍스트만 보고 판단한 탓)
- 수정 2단:
  ① `_clean_for_check()` — 그림 주소·URL·수식을 걷어낸 뒤 판정하고,
     물음표는 **한글 뒤 물음표**일 때만 발문으로 인정.
  ② `_guide_pages()` — '구성과 특징/이 책의 구성/머리말/일러두기/차례/집필진'
     이 있고 실제 발문이 1개 이하인 쪽은 **쪽 단위로 통째 제외**.
- 실측: 4·5쪽 오탐 2건 → 0건. 10~12쪽 정상 6문제 유지(회귀 없음).
  섞어서(4,5,10,11,12) 추출해도 안내 쪽만 빠지고 6문제 정상.
- 교훈: 텍스트 판정 규칙은 OCR 부산물(이미지 URL·LaTeX)을 먼저 제거하고 볼 것.

### 2026-08-23 — 코너 학습 과잉 인식 교정 (사장님: "인식이 심하게 이상해졌어")
- 원인: 전체 PDF 학습이 '열기·준비하기·생각 넓히기·프로젝트' 같은 **활동 코너**
  까지 문제 표기로 등록해 개념·활동 블록이 문제로 쏟아졌다.
- 교정 2단:
  ① LLM 지시 강화 — 탐구/활동/프로젝트/포트폴리오, 도입·마무리 코너를 명시 제외,
     "확실하지 않으면 넣지 마라".
  ② **구조 게이트** `_drop_weak_learned()` — 학습 표기로 잡힌 후보는
     발문 맺음(하시오/구하시오/그리시오/증명하시오/? 등)이 본문에 있어야만 인정.
     기존 고정 표기(문제·예제·유제…)는 건드리지 않는다.
- 실측(YBM 10~15쪽): 문제1·2·3 / 예제1 / **스스로 하기** / 문제1 / 문제3 = 7개,
  활동 코너 오탐 없음.
- 함께: 편집기 원본 패널이 쪽 비율을 넘겨 넓어지던 것 → width max-content +
  max-width 52vw, 이미지 object-fit contain.

### 2026-08-23 — 인쇄된 풀이·답 수집 + 코너 학습을 업로드 PDF 전체로
- **풀이·답 수집** (`service._collect_printed_solutions`): 문제 본문 뒤에
  인쇄돼 있는 '풀이/해설'·'답/정답'을 최대 40줄까지 훑어 담는다. 다른 문제의
  본문이 시작되거나, 풀이/답 표시 없는 서술이 나오면 즉시 멈춘다.
  **본문(units)에는 넣지 않고** `explanation`·`answer` 로만 담아
  다운로드 옵션(미리보기 '정답 및 해설 표시')으로 켜고 끈다.
  document.build_document 가 printed_solution/printed_answer 를 실어 보낸다.
- **코너 이름 학습 표본을 업로드 PDF 전체로** (`_full_pdf_lines`): 원본
  source.pdf 글자층을 통째로 읽어(무료, 스캔본이면 빈 목록) OCR 줄과 합쳐
  후보를 만든다. 선택 추출로 3쪽만 OCR 해도 책 전체 코너를 익힌다.
  실측(YBM 미적분1): 14,433줄 → **스스로 하기 / 스스로 확인하기 /
  수학으로 생각 넓히기 / 생각 넓히기 / 역량 키우기 / 프로젝트 / 열기 /
  준비하기** 8개 학습.
- 실전 검증(10~12쪽): 6문제 추출, '스스로 하기' 인식 + 그 인쇄 풀이가
  해설 필드에 담김. 이전 동일 조건 5문제·해설 없음.
- 주의: 학습된 코너에 '열기·준비하기' 같은 활동성 이름도 포함될 수 있다.
  2026-08-10 '개념·활동 제외' 방침과 충돌 소지 — 오탐이 늘면 LLM 프롬프트에
  제외 목록을 강화할 것.

### 2026-08-23 — 교재별 문제 코너 이름 자동 학습 (예외 표기 대응)
- 문제: 추출기의 시작 표기가 고정 목록(문제/예제/유제/탐구/유형/서술형…)이라
  YBM '스스로 하기' 처럼 **목록에 없고 번호도 없는** 코너는 통째로 누락.
  (표기가 안 잡히면 오른쪽 단 전체가 앞 문제에 딸려가거나 버려진다)
- **해결**: 교재마다 1회 학습.
  ① `service._learn_corner_marks()` — 문서에서 2~12자 한글 짧은 줄을 모아
     (이미 아는 표기 제외) LLM 에게 "문제가 시작되는 코너 이름"만 고르게 함.
     표본이 적으면(줄 400 미만) 1회 등장도 후보 — 선택 추출 대응.
  ② `extract.set_extra_marks()` — 고른 이름을 그 작업에서만 시작 표기로 등록.
     번호 없는 형태(`(\d{0,3})`)도 받는다.
  비용 교재당 1회 호출(costlog kind=marks), 실패해도 기존 규칙으로 동작.
- 실측(YBM 미적분1 3쪽): 학습 전 5문제 → 학습 후 6문제, '스스로 하기' 인식.
- 함께: 편집기 원본 패널을 세로 100% 로 채움(쪽 전체가 한눈에).
- 남은 판단: '스스로 하기'는 인쇄된 풀이·답이 함께 있는 따라풀기형 —
  풀이까지 담을지 방침 미정 (사장님 결정 대기).

### 2026-08-23 — 선택 누락 2차 수정: 폴백을 쪽 단위 → 박스 단위
- 증상: 박스 4개(14·15쪽)를 골랐는데 편집기에 3개, 그중 2개는 글 없는 그림.
- **원인**: 전날 넣은 폴백이 **쪽 단위**였다. 같은 쪽에 인식된 문제가 하나라도
  있으면 그 쪽의 나머지 박스는 폴백을 못 타서 통째로 사라졌다
  (15쪽: '문제 3' 하나만 인식 → 같은 쪽 '비주얼 개념' 박스 무시).
  14쪽은 인식 0이라 폴백이 돌았지만 그림이라 글이 비어 있었다.
- **수정**: 박스마다 개별 판정. 안 걸린 박스는
  ① 박스 안 **OCR 글줄로 편집 가능한 글 문제 생성**(무료, `_units_from_lines`,
  `_ocr_lines_by_page` 가 선택 PDF sha 캐시에서 줄을 읽음)
  ② 글이 없을 때만 200dpi 그림 오림.
  점(클릭) 선택은 종전대로 쪽 단위 안전장치 유지.
- 실측 재현: 같은 4박스 → **4문제 전부 글로** (206·239·138·267자).
- 함께: 원본 패널 헤더 40% 축소·바깥 여백 10px, 편집기 최초 진입 세로 맞춤.
- 교훈: '없으면 통째로 살린다'식 안전장치는 사용자 조작 단위(박스)와 맞춰야 한다.

### 2026-08-22 — 선택 추출 "3문제만 나오는" 버그 원인·수정 (실측 확정)
- **진단 경로(추측 금지 원칙대로)**: picks.json 과 결과 문서를 대조 →
  4~5쪽을 골랐는데 항상 2쪽에서만, 정확히 3문제. OCR 캐시를 열어 보니
  모든 쪽이 정상 인식(줄 38~61개)이라 OCR 문제 아님 → EX.build 재현 →
  `bands: {None: ([175.0], 90)}` — **x 대역이 왼쪽 단 하나만 학습됨**.
- **근본 원인**: `extract.build` 의 대역 학습이 "같은 x버킷에 후보가 2개 이상"
  일 때만 대역으로 인정한다. 128쪽 통째 분석에선 오른쪽 단(x≈1025)이 수십 번
  나와 통과하지만, **몇 쪽만 고르면 표본이 부족해 문턱을 못 넘어 2단 교재의
  오른쪽 단이 통째로 버려졌다.** (선택 추출 도입으로 처음 드러난 잠복 버그)
- **수정**: 후보 x좌표 표본이 12개 미만이면 문턱을 1회로 낮춘다.
  실측 9afcd120: 3문제 → 6문제, bands 에 1025 복구. 서비스 실측(15·16쪽):
  15쪽 4문제 + 16쪽 1문제 = 5문제 (이전엔 15쪽 0문제).
- **부수 확인**: 사장님이 찍었던 14·16쪽 일부는 '개념/탐구 활동' 페이지 —
  2026-08-10 확정 수록 방침(개념 박스·활동 제외)에 따라 원래 제외 대상이다.
  다만 **직접 드래그한 영역은 뜻이 분명하므로** 그 쪽에서 매칭이 하나도 없으면
  박스 영역을 200dpi 로 오려 그림 문제로 담는 폴백을 `_apply_picks` 에 추가.
- 교훈: 전체 분석에서 잘 되던 로직이 "부분 선택"에선 표본 부족으로 무너진다.
  통계로 문턱을 두는 코드는 소표본 경로를 따로 점검할 것.

### 2026-08-22 — 선택 화면 재설계 + 문제은행 저장 시점 이동
- **저장 시점 변경 (중요)**: 추출 직후 자동 적재 폐지. 흐름은
  추출(단원 분류만 미리) → 편집 → **다운로드 시 '문제은행에 저장' 체크(기본 켜짐)**
  → bank-save. 저장 없이 편집기를 떠나면 **임시 저장소**에 자동 보관
  (pagehide + keepalive fetch, sendBeacon 은 인증 헤더가 없어 못 씀).
  명칭 '내 교재' → **'임시 저장소'** 통일(편집기·내 정보).
  bank_save 는 문제별 chapter 가 이미 있으면 그대로 쓰고, 없으면 assign_chapters.
  실측: 추출 후 은행 1174 그대로 → bank-save 후 1175.
- **선택 화면**: Ctrl+휠 = 포인터 기준 확대(스크롤 보정 계산), 그냥 휠 = 한 쪽씩
  이동(220ms 잠금, 확대 중엔 일반 스크롤), **Ctrl+Z 선택 취소**(스냅샷 60),
  Ctrl+0 배율 복귀, 좌/우 사이드 패널(사용법·단축키), 점+박스 **통합 번호**,
  헤더에 선택 문제 개수, 진행 화면 가로·세로 중앙.
- **드래그 박스 간헐 실패 수정**: 브라우저 기본 이미지 끌기가 포인터 이벤트를
  가로챈 것 → stage 의 dragstart/selectstart preventDefault, img.draggable=false,
  setPointerCapture try/catch, pointercancel 처리.
- 1100px 이하에서는 사이드 패널 자동 숨김(--side-w:0).

### 2026-08-22 — 선택 화면 스프레드·드래그 박스, 전체/선택 옵션, 카드 배점
- **버그 정정**: "업로드 후 '추출할 문제를 선택해 주세요'만 뜨고 진행 안 됨"의
  진짜 원인은 **App.tsx 의 pick 리다이렉트·해설지 stash 코드가 유실**된 것.
  (앞서 '예전 탭 캐시'로 진단한 것은 오진 — poll 안전망이 증상만 가렸다.)
  복구 완료. 교훈: 여러 rep 을 한 스크립트에 묶으면 중간 실패 시 전부 미반영 —
  파일 쓰기 전에 assert 가 터지면 앞 rep 도 날아간다. 적용 후 grep 로 확인할 것.
- **pick 화면 전면 개편**: 2쪽 스프레드, ←→ 키 이동, 세로 100% 맞춤,
  Ctrl+스크롤 확대(1~4배), 다음 스프레드 선반입, 쪽별 '쪽 전체' 토글.
- **드래그 박스 선택** 추가 (짧은 클릭=점, 드래그=영역). 서버 boxes 파라미터,
  worker 가 bbox 겹침으로 매칭. 실측: 문제1만 덮는 박스 → 1개 남고 3개 휴지통.
  **누락 방지 안전장치**: 선택 표시한 쪽에서 하나도 못 맞추면 그 쪽 문제 전부 유지
  (실측: 문제 없는 상단 절반 박스 → 4개 전부 유지). 사장님 '5개 중 2개 누락'
  신고의 대응책.
- 랜딩 팝업: **문제 선택 변환 / 전체 페이지 변환** 2택 (전체는 all_pages 로 즉시
  extract 후 기존 진행률 화면). 팝업에서 차감 포인트 표기 제거(선택 화면이 실금액).
- 편집기: **배점 인풋을 카드 우상단**(옛 X 자리, 호버 시 진해짐), 우측 패널 배점칸
  제거, **카드 우클릭 메뉴**(원본에서 보기·출제 포함/제외·제거)로 삭제 이동,
  원본 N쪽 버튼·선택 강조 네이비(#12335e), 섞기 확인 문구 변경+드래그 안내.

### 2026-08-21 — 편집기 대개편 1단계: 선택 추출 플로우 (실측 검증 완료)
- **새 플로우**: 업로드(과금·OCR 없음, status=picking) → `/mathocr/pick#job`
  선택 화면(원본 쪽 이미지 지연 로드, 문제 클릭=파란 점, 재클릭 해제,
  '쪽 전체' 토글) → '문제 추출(N쪽·N×10P)' → 선택 쪽만 잘라
  source-selected.pdf 로 OCR → 편집기.
- **과금**: extract 시점에 선택 쪽수 × 10P. 잘라낸 PDF sha 로
  already_paid_for — 같은 쪽 묶음 재추출 무료. 실패 시 refund_job 그대로 작동
  (charge 가 job_id·sha 기록). 다른 쪽 묶음이면 겹친 쪽도 재과금(알려진 한계).
- **worker(_apply_picks)**: 쪽 번호를 원본 번호로 복원(잘린 i→pages[i-1]),
  클릭 지점(0~1 정규화)을 bbox 와 매칭(안이면 확정, 아니면 같은 쪽 최근접),
  '쪽 전체' 쪽은 모두 유지, 나머지 deleted(휴지통 복원 가능), 매칭 전무 시
  전체 유지 안전장치. 실측: 15쪽 문제4개 중 클릭 2지점 → 정확히 2개만 남음,
  1쪽 10P 차감, 쪽 번호 '원본 15쪽' 정상.
- 해설지는 업로드 때 answer-key-stash 로 맡겨 추출 후 자동 연결(글자층만).
- 문제은행 자동 적재는 이제 **선택된 문제만** 들어간다 (전체 적재 폐지).
- 랜딩 팝업: '문제 선택하러 가기', 선택 기반 과금 안내.
- 중복 업로드 dedup 에 picking 상태 포함.
- 남은 논의: 책 전체를 은행에 넣고 싶을 때 '모든 쪽 전체 선택' 단축 버튼
  (128쪽을 일일이 누르기 힘듦) — 사장님 의견 대기.

### 2026-08-21 — 해설지 자동 연결·난이도 5단계·편집기 개편 (실측 검증 완료)
- **해설지 자동 연결 2경로**: ① 교재에 동봉된 정답 부분 —
  routes.answer_key_apply 코어 분리, answer_key_from_source 가 '정답'류
  **첫** 표제부터 파싱(마지막 표제 기준이던 초판 버그 수정 — 단원마다 표제가
  반복되는 교재에서 0건 나옴), 근거 빈약(found<5·expl<3)이면 미적용.
  실측: 확통 128쪽 → "정답 17개 자동 연결" 완료 메시지·문서 반영 확인.
  ② 업로드 팝업 '해설지 PDF(정답/해설 자동연결)' 별도 첨부 — 완료 시
  answer-key 호출, 스캔본은 건너뛰고 편집기 안내 (자동 과금 안 함).
- **난이도 5단계** 하/중하/중/중상/상 — 편집기 버튼·AI 판정(_grade_with_ai)·
  은행 필터·무작위 뽑기 전부.
- 편집기: 우측 패널 20% 축소(216px)·'N번째 문제' 제거·배점 인라인·
  'AI로 난이도·배점' 문구, 좌측 기본 290px, 원본 모달 전체 페이지 맞춤 +
  Ctrl+스크롤 줌(인식 영역 중심), 이미지 핸들 세로 커서·세로 드래그,
  이미지 우클릭 메뉴 안 닫히던 버그(인라인 display:flex가 hidden 덮음) 수정,
  원본 버튼 강조·라벨 자동 폭, 본문 -webkit-text-stroke .22px.
- 구글 로그인 라이브: 키 .env 주입 완료, 사장님 실클릭 테스트 대기.

### 2026-08-21 — 구글 로그인 + 대기열 시연 + UI 다듬기
- **구글 로그인 (OAuth 인가 코드)**: /v1/auth/google/login → 동의 →
  /v1/auth/google/callback → 이메일 확인, 없으면 계정 생성(랜덤 비번)+
  mark_verified(가입 크레딧 최초 1회) → `/#gtoken=` 조각으로 세션 전달
  (랜딩 App useEffect 가 수신·저장). state 는 HMAC 서명 10분.
  베타 잠금 존중, 같은 이메일이면 기존 계정에 로그인됨.
  **키 대기**: .env GOOGLE_CLIENT_ID/SECRET 비어 있음 — 사장님이 구글
  클라우드 콘솔에서 발급해야 활성화 (지금은 버튼 누르면 '준비 중' 안내).
  승인된 리디렉션 URI: https://www.personalpms.cloud/v1/auth/google/callback
- 로그인 모달·가입 페이지에 'Google로 계속하기/3초만에 가입' 버튼.
- 업로드 박스 호버: 떠오름 빼고 **뒤가 밝아지는 글로우** (사장님 정정).
- **대기열 시연**: .env QUEUE_TEST_DELAY=3 (워커가 집기 전 3초 대기 —
  tasks.py, 시연 후 제거 예정), 순번 계산에서 자기 자신 제외 보정.
- 라이브 직전 메모리 체크리스트에 테스트 계정 3개 삭제·시연 지연 제거 추가.
- 드라이브 저장(drive.file 권한)은 라이브 후 후보로 논의됨 — 구글 로그인
  이 먼저 들어갔으니 scope 추가만 하면 됨.

### 2026-08-21 — 라이브 준비 라운드: 보너스 500P·문제별 단원·워커 2대
- 사장님 확정: 환불 공제 10%(건별 재량), **가입 보너스 500P**(config
  signup_grant_pages), 라이브 직전 신호는 사장님이 직접 줌 (백업·스크린샷
  그때 재논의 — 메모리 pre-launch-checklist.md).
- **문제별 단원 분류 (assign_chapters)**: ①표본으로 단원 목록(2~8개) 추출
  ②25문제씩 목록에 배정. 분석 완료 시 자동 적용(실패 시 문서 공통 단원 폴백).
  `/bank/fill-chapters` 로 기존 미지정 백필 — 확통 194문제가 6단원으로 분리됨
  (2회 실행, 잔여 19→0). 비용: 책 한 권 ~15k 토큰 = 몇 원.
- **동시 변환 실측**: 90쪽 분석 중 워커 피크 **550MB**, api 170MB.
  RAM 3.8GB → **worker replicas 2** (docker-compose deploy.replicas).
  동시 10건 필요 시 8GB+ 증설. '동시 사용자 10명' ≠ 동시 변환 10건 —
  대기열+순번 표시가 있어 2대로 초기 트래픽 감당.
- 원가 확정: Mathpix **$0.0033/쪽 (콘솔 확인)**, OpenAI ~1.4원/쪽,
  신규 인식 쪽당 ~6원, 마진 ~87%. 옛 36원 추정 폐기 (메모리 갱신됨).
- 랜딩 네비 잔액 표기 '쪽'→'P' 수정.

### 2026-08-21 — 문제은행 자동화 대개편 (사장님 지시: PDF→내 문제은행 자동 구축)
- **분석 완료 시 자동 적재**: worker 가 교재명(classify)·단원(estimate_chapter, AI)
  붙여 collect_document. 실측: calc 90쪽 → "수학>미상 수학>여러 가지 미분법(149)".
  업서트 보존 병합(빈 값이 기존 단원·난이도·정답 안 덮음).
- 편집기 '문제은행에 저장' 버튼·모달·JS 전부 제거 (bank-save API 는 남김).
- **명칭 통일: 저장소 → '내 교재'** (편집기 헤더·프로필·확장 팝업·내 정보 페이지).
  '교과서' 표기도 '교재'로.
- 문제 추가 '저장소 문서' → **'내 문제은행' 팝업**: 신설 GET /bank/preview 가
  수식 HTML + 그림 데이터 URI 로 편집기와 같은 모양으로 내려줌(60개 한도),
  bank-add 한도 20→60. 예전 저장소 문서 합치기(merge slot_id) UI 는 제거됨
  (API /jobs/{id}/merge 는 남아 있음).
- 모두 선택/해제 제거, 'A/B형' 문구 제거, 순서 섞기에 확인 팝업(askConfirm),
  좌측 패널 제목 eyebrow 16px·option-label 15.5px.
- **남은 한계**: ① 단원 추정이 문서당 1개 — 여러 단원짜리 통짜 교재는 한 단원으로
  뭉침 (문제별 분류로 개선 여지) ② 기존 확통 194문제는 '단원 미지정' 그대로
  ③ 편집기에서 고친 정답·수식은 은행에 자동 반영 안 됨 — 텍스트 해시 키라
  다운로드 시 업서트하면 중복 생겨서 보류. 문제별 고정 id 도입이 선결.

### 2026-08-21 — 처리 속도 실측 + 진행률 개선 + 배포 사고·재발 방지
- **페이지당 처리 시간 실측** (128쪽 확통 교재, 신규 파일):
  - Mathpix OCR: 128쪽 ≈ 24초 (쪽당 0.2초 — 서버측 병렬)
  - AI 후보 가리기 ≈ 80초, 경계 다듬기(92문) ≈ 55초, 놓친 영역 ≈ 100초
  - 합계 ≈ 4분 20초 → **쪽당 약 2초**. 90쪽 캐시히트 재분석은 149초(1.7초/쪽).
  - 500쪽 예상 ≈ 17분. 시간의 주범은 OCR 이 아니라 AI 단계.
- **진행률 멈춤 오인 수정** (사장님 신고: 64%에서 3분 정지로 보임):
  service.py 경계 다듬기 단계가 64% 고정 + 분모 없는 (i) 표시였고, 다음
  단계가 63%로 뒤로 갔다 → 64→68 이동·(i/n) 표시·놓친영역 69로 수정.
  랜딩 분석 화면에 'N초 경과 · 작업 진행 중' 실시간 표시 추가.
  남은 정적 구간: '놓친 영역 확인 중'(69%)이 ~100초 무갱신 — 다음에 쪼갤 것.
- **사고: MAX_PAGES 적용하려던 `docker compose up -d` 가 처리 중이던 128쪽
  작업을 죽였다.** RQ FailedJobRegistry.requeue 로 재큐(재과금 없음, sha 캐시로
  OCR 무료 재사용) → 성공(문제 197개). **deploy.sh 에 가드 추가**: 처리 중/대기
  작업 있으면 배포 중단, FORCE=1 로만 강행.
- 같은 파일 재업로드는 같은 sha 라 인식 요금이 다시 안 나가는 것 실측 확인
  (1280P 1회만 차감, 재업로드 2건은 0P).

### 2026-08-21 — PDF 최대 쪽수 200→500 (.env MAX_PAGES)
- 사장님 질문으로 점검: 파이프라인이 쪽 단위 순차 처리라 RAM 은 쪽수와 거의
  무관, 작업 타임아웃은 max_pages×30초로 자동 연동, 인식비는 사용자 부담(10P/쪽).
- 유일한 걸림돌이 디스크 85%였는데 원인이 **도커 빌드 캐시 30GB** —
  `docker builder prune -f --filter until=48h` 로 17.7GB 정리(→62%).
  디스크 차면 이 명령 먼저 쓸 것.
- `.env` MAX_PAGES=500 (gitignore 라 저장소엔 없음), `docker compose up -d` 로
  재적용. 게스트 5쪽 제한은 그대로. 실측: api·worker 모두 500 확인.

### 2026-08-21 — 문제은행 개편 (타사 조사 기반 선수정)
- 매쓰플랫·족보닷컴 조사 결과 핵심은 **난이도 지정 출제** — 그런데 우리 은행은
  93문제 중 92개가 난이도 빈값이었다. 보완:
  - `POST /v1/mathocr/bank/fill-difficulty` — 난이도 빈 문제만 AI 일괄 판정
    (상/중/하 + 권장 배점 2~6, 25문제씩 청크, costlog kind=grade).
    실전: 93문제 2회 호출로 전부 채움(상15/중49/하29), 토큰 8.1k in/2.9k out — 몇 원 수준.
  - **버그: 편집기 'AI 난이도' 버튼의 `/jobs/{id}/auto-grade` 라우트가 서버에
    없어서 404였다** → bank.py 에 복구 (빈 난이도·배점만 채움, 같은 헬퍼 사용).
  - `POST /v1/mathocr/bank/delete` — 문제 삭제(그림 파일 포함, 빈 폴더 정리).
  - bank/full 응답에 text(전문, 수식 껍데기 제거)·points·source 추가.
- 은행 페이지(/mathocr/bank) UI: 텍스트 검색, 난이도 칩 필터(전체/상/중/하/미지정),
  **무작위 뽑기(상·중·하 개수 배합 + 난이도 무관)**, 문제 클릭 시 전문 펼침,
  호버 ✕ 삭제, 교과서별 문제 수, '난이도 미지정 N개 AI로 채우기' 배너(무료).
- 못 한 것(콘텐츠가 없어 불가): 유사·쌍둥이 문제, 학교 기출 — 자체 문제은행이
  본인 업로드 문제뿐이라 구조적으로 타사와 다름. 오답 관리는 채점 기능이 생기면.

### 2026-08-21 — 이용약관·개인정보처리방침 정비 (`/mathocr/terms`)
- 웹조사 기반으로 4개 문서를 한 페이지에 정비: **이용약관 / 포인트·환불 규정 /
  저작권 정책 / 개인정보처리방침** (`app/static/mathocr-terms.html`, 다크 테마,
  상단 고정 탭 앵커 #terms #refund #copyright #privacy).
- 법적 근거 반영:
  - 전자상거래법 17조 — 충전 7일 이내 미사용 전액 환불, **포인트 사용분은
    디지털콘텐츠 제공 개시라 청약철회 제외**(결제 화면에 고지 — 이 고지가
    있어야 제외가 유효), 표시·광고와 다르면 3개월/30일, 환급 3영업일.
  - 콘텐츠이용자보호지침 — 잔여 유상 포인트 환불 시 10% 공제(무상 지급분 제외).
  - 개인정보보호법 30조 + **28조의8 3호(국외 처리위탁은 처리방침 공개로 동의
    갈음)** — Mathpix(미국)·OpenAI/Anthropic(미국) 국외이전 표 명시,
    법정 보존기간(결제 5년·분쟁 3년·접속기록 3개월), 보호책임자 양하린.
  - 저작권법 103조 — 권리자 신고 채널(이메일)→복제·전송 중단→양측 통보 절차.
- 연결: 랜딩 푸터 `이용약관 · 개인정보` → /mathocr/terms (재빌드),
  가입 약관 3종에 국외 처리위탁 문구+전문 링크, 충전 페이지에 청약철회 제외
  고지+환불 규정 링크(+잔액 표기 '쪽'→'P' 수정).
- 통신판매업 신고번호는 "신고 절차 진행 중"으로 표기 — 발급되면 terms 페이지
  사업자 정보와 랜딩 푸터에 기입할 것.
- 미확정: 잔여 포인트 환불 공제율 10%는 업계 관행 기준 — 사장님 확정 대기.

### 2026-08-18~21 — 사업화 라운드: 도메인·결제·가격·베타 잠금
- **도메인 개통**: personalpms.cloud + www 둘 다 HTTPS (Caddy 자동 발급,
  HSTS+preload). nginx/certbot 지시가 와도 **깔지 말 것** — Caddy 가 그 역할.
  크롬 '주의 요함'은 대부분 사용자 캐시/HSTS 미학습 문제였다.
- **상호 하린 · 대표 양하린 · 사업자등록번호 303-20-57695** 푸터 기입.
  통신판매업 신고는 구매안전서비스 확인증 문제로 진행 중 (스마트스토어
  확인증 반려 — 개인도메인몰은 국민·기업은행 or PG 확인증 필요. 토스
  심사 통과 후 토스 확인증으로 재신청이 최단 경로).
- **토스페이먼츠 연동 완료(테스트 키)**: app/payments.py (주문→위젯 v2→
  서버 승인·금액 대조·중복 방지→ledger 지급), /mathocr/charge 충전 페이지.
  위젯은 반드시 gck/gsk 계열 키. 실키 적용: `bash 토스실키적용.sh <ck> <sk>`.
- **과금 확정 (2026-08-20)**: 포인트 이원화 — 인식 원본 1쪽=10P, 다운로드
  시험지 1쪽=1P(작업·형식별 최초 1회만, 재다운로드 무료). 충전 상품
  1,000/3,000/10,000P = 6,900/17,900/54,900원. 가입 보너스 100P.
  원장(ledger)은 ×10 이관 완료. 원가 실측: 쪽당 ~36원(Mathpix 35 + AI 1미만).
- **베타 잠금 (2~3주)**: BETA_ALLOWED_EMAILS(.env) 계정만 업로드·가입 가능,
  그 외는 '서비스 준비 중' 안내(클릭 단계부터). 오픈 = 설정 비우고 재배포.
- 경쟁 조사(네이버 실사용 리뷰 포함): 문제당 환산 우리 33~42원 = 전 시장
  최저권. 수작업 외주 500~800원. 상세는 memory/pricing 노트.

### 2026-08-19~21 — 편집기 플로우 대개편 (backend 1.51.41)
새 흐름: 인식 → 다듬기 → **문제은행에 저장**(교과서·단원 입력, AI 단원
자동 추정) → '시험지 만들까요?' → 계단식 선택(교과서>단원>문제, 기본 접힘)
→ 새 시험지 편집 → **실시간 미리보기 모달**(단수·쪽당·배점·폰트·형식) →
다운로드(차감).
- 문제은행: 사용자별(_문제은행/{sha256(email)[:20]}/과목/교과서/) — 공용
  개방은 저작권 검토 뒤 별도 결정. 게스트 적재 금지. bank-save 는 upsert.
  과목은 교과서 이름·기존 폴더 우선으로 판정 (갈라짐 방지).
- 출력 3종: PDF·HWPX·**PPTX**(문제당 1쪽 PDF→슬라이드 그림, 조판 동일).
- 해설지 연결: 글자층 무료, 스캔본은 확인 후 Mathpix(쪽당 10P, sha 캐시
  재과금 없음). 정답표(①-⑤)+번호별 해설 블록 → 원본 번호/순서 중 더 잘
  맞는 매핑 자동 선택. explanation 필드 — 정답지가 '정답 및 해설'로 확장.
- 편집기 UX: 목차 체크박스, 원본 N쪽 버튼→인식 영역 확대 모달(우측 원본
  패널 제거), 이미지 우클릭 메뉴(복사·위치·자르기·AI), 하단 리사이즈 핸들,
  배점 우측정렬·총점, AI 난이도·배점 추천(책 한 권 수십 원 미만), A/B 섞기.
- 랜딩: 글래스모피즘 히어로, 핵심 기능 = 통합 실물 캡처 1장+강조 이동,
  가격 섹션, PDF 도구(분할·병합·이미지→PDF — 브라우저 내 처리 무료),
  MY PAGE(내 정보 /mathocr/me · 문제은행 /mathocr/bank).
- **주의**: 비회원은 캐시 있어도 앞 5쪽만. 게스트 이메일은 @guest.local.
  _문제은행/_비용로그 등 밑줄 폴더는 만료 청소 제외, 소유자는 uid 10001
  (호스트 root 로 만들면 PermissionError 남).
