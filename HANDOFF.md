# 프로젝트 인수인계 — 문제집 추출기

새 대화를 시작할 때 이 문서를 붙여넣어 맥락을 이어간다.

---

## 0. 응답 규칙

- **명령마다 실행 위치를 먼저 표시할 것.** `【내 PC — PowerShell】` 또는 `【서버 — SSH】`
- 결론 먼저. 예고·과정 서술 없이 바로 답할 것.
- 개발 용어는 **괄호로 역할 설명을 덧붙일 것.** 예: `column` (단 번호 — 이 줄이 왼쪽 칸인지 오른쪽 칸인지)
- 사용자 작업을 최소화할 것. 사용자가 하지 않으면 안 되는 것만 시킬 것.
- 사용자는 최종 결과물에 대한 피드백을 하는 사람이다. 이해시키려 하지 말 것.
- 명령은 한 줄씩 제시할 것.
- 추측한 수치·필드명은 "확인 필요"로 표시할 것.
- "정답지"는 해설지를 뜻할 때만. 채점 기준 파일은 **"테스트 기준 파일"**.
- 코드는 생략 없이 전문 출력. 변경 요약은 표로.
- 압박성 격려, 반복 사과 금지.

---

## 1. 만들려는 것

PDF 문제집·교과서를 업로드하면 **문제만 골라내 좌우 2단으로 조판한 편집 가능 문서**를 돌려주는 웹 서비스.

| 항목 | 결정 |
|---|---|
| OCR | Mathpix `v3/pdf` + SSE 스트리밍 |
| 산출물 | DOCX (한/글에서 편집 가능) |
| 조판 | 좌우 2단 |
| 서버 | Vultr 2vCPU / 4GB / 80GB, Ubuntu 24.04, 서울 |
| 처리 방식 | 비동기 (큐 + 워커) |
| 대상 | 수학. 이후 국어·영어 확장 |
| 판매가 | 페이지당 30원 (미검증 가설), 크레딧 묶음 판매 |
| 원가 | Mathpix $0.005/p = 7.5원 (환율 1,500원) |
| PG 수수료 | 3% |

**무료 체험 (확정안)**

| 단계 | 대상 | 제공 | 다운로드 |
|---|---|---|---|
| 1. 미리보기 | 비가입자 | 앞 5페이지 처리, 화면에서 결과 전체 확인 | ✕ |
| 2. 가입 크레딧 | 이메일 인증 | 1회성 10페이지 | ○ |
| 3. 결제 | 유료 | 페이지당 | ○ |

**남용 방어** — 1~3순위만 적용.

1. 파일 SHA-256 해시 중복 차단 (차단 용도로만. 결과 재활용 안 함)
2. 다운로드 게이트 (서버에서 막을 것)
3. Cloudflare Turnstile 봇 차단
4. ~~브라우저 지문~~ (보류)
5. ~~IP 평판 조회~~ (보류)

**판정 목표**

| 지표 | 목표 |
|---|---|
| 미탐지 누락 | 0% — 누락 시 반드시 경고 표시 |
| 오검출 | 5% 미만 |
| 그림 오첨부 | 0% — 확신 없으면 붙이지 않고 "확인 필요" |

---

## 2. 확정된 사실

**텍스트레이어는 쓸 수 없다.** 글자가 있어도 수식이 깨지고 읽는 순서가 붕괴된다. 전량 OCR 대상.

**`lines.json` 실측 완료.** 14개 파일 263페이지, 9,775줄. 비용 $1.03.

| 필드 | 뜻 |
|---|---|
| `type` | 줄의 종류 (본문·수식·도형·머리말) |
| `column` | **본문 흐름 단위** (곁글·박스 구분. 좌우 2단이 아님) |
| `region` | 위치와 크기 |
| `font_size` | 글자 크기 |
| `parent_id` / `children_ids` | 상하위 줄 연결 |
| `conversion_output` | 최종 문서 포함 여부 (False면 중복) |
| `confidence` | 인식 확신도 0~1 |

type 분포: `text` 5,278 / `list_item` 884 / `page_info` 806 / `math` 522 / `diagram` 421 / `chart` 103 / `multiple_choice_block` 142

**결정적 관측:** 발문이 x좌표·글자크기에 극단적으로 몰림.

```
문제 N  202건  x중앙 361  글자크기 32
예제 N   53건  x중앙 363  글자크기 32
탐구 N   45건  x중앙 384  글자크기 27
```

**기준 파일도 수식을 잃는다.** 한/글에서 PDF로 저장한 편집본은 글자는 남지만 **수식·숫자·영문 변수가 통째로 사라진다.** 예: `초점이 점 F(0,2)이고 준선의 방정식이 y=-2인 포물선` → `초점이 점 F 이고 준선의 방정식이  인 포물선`.
그래서 채점은 **한글 뼈대만 비교**한다. 숫자·수식 정확도는 이 방식으로 잴 수 없다.

**미해결:** SSE 스트리밍 이벤트 0건 (진행률용, 후순위). 샘플 115 DPI로 낮음.

---

## 3. 작업 환경

**서버 작업 폴더:** `~/mathocr/`

```
.env                  API 키 (커밋 안 됨)
pull.sh               코드 동기화 (codeload 압축본 방식)
scripts/
  stage0.py           Mathpix 측정 + 리포트
  extract.py          문제 추출 (v3)
  preview.py          2단 조판 HTML 미리보기
  analyze.py          lines.json 구조 분석
  parse_ref.py        테스트 기준 파일 파서 (v2)
  grade.py            자동 채점기
  inventory.py        자료 폴더 형식 판별
  run*.sh
samples/테스트자료_스캔본/        14개
samples/테스트자료_텍스트레이어/   4개
stage0_out/runs/<파일명>/result.lines.json
grade_out/            채점 결과·점수 이력
```

**표준 명령**

```
cd ~/mathocr && bash pull.sh && bash scripts/run_preview.sh
cd ~/mathocr && bash pull.sh && bash scripts/run_extract.sh
cd ~/mathocr && bash pull.sh && bash scripts/run_grade.sh --list
cd ~/mathocr && bash pull.sh && bash scripts/run.sh          # 과금
```

**로컬:** Windows / PowerShell / Python 3.14 (`python`, `python3` 아님) / pillow·pypdf 설치됨 / SSH 키 인증 완료

**서버 파이썬 패키지:** requests / pypdf / pillow. 기준 파서는 pypdf만 쓴다 (pdfplumber 안 씀).

`raw.githubusercontent.com`은 CDN 캐시 때문에 옛 파일을 준다. codeload 압축본을 쓸 것.

pull.sh 는 v2부터 `scripts/` 폴더를 통째로 받는다. 새 스크립트를 넣어도 목록을 고칠 필요가 없다.

---

## 4. 추출기 현황 (v3)

| 지표 | v1 | v2 |
|---|---|---|
| 본문 길이 중앙값 | 20줄 | 3줄 |
| 최대 본문 | 917줄 | 43줄 |
| 추출 문제 | 156개 | 176개 |
| 번호 오독 | 8건 | 0건 |

**판정 로직**

1. `page_info` 제거
2. `conversion_output=False` 제거
3. 페이지 폭 75% 넘는 그림 제거
4. 시작 판정: `문제/예제/유제/탐구/유형/0N` + 최상위 줄 + 글자크기 60 이하
5. 파일별 x 최빈 구간 학습 후 ±90 밖 제외
6. 끝 판정: 명령형 어미(`~시오`, `~보자`) 이후 소문항만 수집
7. 종료: 파일경계 / `section_header` / 페이지 2장 초과 / 60줄 초과
8. 예제·유제는 서술문에서 끊지 않음
9. 번호 보정: `직전 8 + '9100' → 9`
10. 그림 귀속: 본문 안의 것만. 발문이 그림을 가리키는데 없으면 같은 높이대 추정 후 표시

**미리보기 확인 완료** — 2단·수식·체크박스·인쇄 정상. 그림 귀속만 미확인.

---

## 5. 테스트 기준 파일

`교과서 모음_문제풀이` 폴더에 수기 편집본 123개. `[문제편집]` PDF 34개 + hwp 44개, `(용문아)` 단원별 PDF 44개.

**파서 검증 완료** (`scripts/parse_ref.py`)

| 파일 | 결과 |
|---|---|
| 교학사 확통 68쪽 | 문제 224개, 결손 0 |
| 천재(류) 경우의 수 17쪽 | 문제 65개, 파일명 "65제"와 일치 |
| 신사고 기하 77쪽 | 문제 301개, 결손 0, 해설 52쪽부터 (`[정답 및 해설]` 표기) |
| 신사고 확통 75쪽 | 문제 313개, 결손 0, 해설 52쪽부터 (표기 없음 → 쪽 단위 추정) |

신사고 2건은 서버가 아닌 다른 PDF 추출기로 잰 값이다. **서버 pypdf 결과와 다를 수 있어 확인 필요.**

**편집본 공통 패턴**

- 문제 시작이 `N.N)`. **두 번호가 항상 같다** → 오탐 0
- 소단원 `N-N-N. 제목` (번호가 앞/뒤 둘 다 존재)
- 해설은 `[정답 및 해설]` / `[빠른 정답]` 이후
- 머리말·워터마크는 반복 빈도로 자동 식별

**출판사별로 어긋나는 부분** (parse_ref v2에서 처리)

| 어긋남 | 실제 예 | 처리 |
|---|---|---|
| 머리말에 쪽번호가 박혀 쪽마다 문구가 다름 | `Du kannst ... \| 1 \| 용문아 수학방` | 숫자를 `#`로 가린 뒤 반복 빈도 판정 |
| 꼬리말이 `- 12 -` 형태 | 신사고 확통 | 쪽번호 패턴에 추가 |
| 해설 시작 표기가 없음 | 신사고 확통 | 뒤쪽에서 `N.N)`이 사라지고 `N)`만 나오는 첫 쪽을 해설 시작으로 봄 |
| 소단원 번호 `N-N-N.`이 없음 | 신사고 확통 | 쪽 머리 2줄에서 제목 줄을 추정 (숫자 없고 24자 이하이며 서술 어미가 아닌 줄) |

**산출물 사양**

- 개념 설명·풀이·답 제거, 발문과 소문항만
- 교재 전체 통합 일련번호
- 소단원 제목·구획 라벨 유지
- 답 쓸 공백 확보
- 정답·해설은 맨 뒤로 분리
- 본문 길이: 최대 405자 / 227자

---

## 6. 자동 채점기 (`scripts/grade.py`)

**명령**

```
bash scripts/run_grade.sh --list
bash scripts/run_grade.sh <기준.pdf> <run폴더필터> [태그] [메모]
```

`<run폴더필터>`는 `stage0_out/runs` 폴더 이름에 들어 있는 글자. 쉼표로 여러 개.

**동작**

1. 기준 파일을 parse_ref 로 읽어 문제 목록을 만든다
2. `result.lines.json` 을 extract 로 읽어 추출 문제를 만든다 (Mathpix 재호출 없음 = 0원)
3. 양쪽에서 수식·숫자·영문을 걷어내고 한글 뼈대만 남긴다
4. **읽는 순서를 지키며** 정렬 대조한다 (앞뒤가 뒤바뀐 짝은 애초에 만들지 않는다)
5. 판정하고 점수를 `grade_out/history.jsonl` 에 쌓는다 → 다음 실행 때 증감이 표에 찍힌다

**판정 기준**

`담김`(기준 문장이 추출에 얼마나 들어왔나)과 `군더더기없음`(추출 문장이 기준 안에 얼마나 들어가나) 두 값으로 가른다. 기본 경계 0.85.

| 판정 | 뜻 |
|---|---|
| 완전일치 | 두 값 모두 0.85 이상 |
| 잘림 | 기준 문장 일부가 빠짐 (경계를 너무 일찍 끊음) |
| 넘침 | 기준에 없는 문장이 섞임 (경계를 너무 늦게 끊음) |
| 부분일치 | 빠짐과 섞임이 동시에 |
| 판정보류 | 한글 뼈대가 6자 미만 (수식만 있는 문제) |
| 미검출 | 기준에 있는데 추출에 없음 |
| 오검출 | 추출했는데 기준에 없음 |

**채점 범위 자동 축소** — 기준 파일은 교재 전체이고 OCR 한 것은 일부 단원뿐이다. 짝이 맞은 문제 사이 간격이 15개 이하인 구간만 채점 범위로 잡고, 나머지는 "범위 밖"으로 빼서 리포트에 구간을 표시한다.

**바꿀 수 있는 값**

| 옵션 | 기본 | 뜻 |
|---|---|---|
| `--ok` | 0.85 | 완전일치 경계 |
| `--min-sim` | 0.35 | 이 아래는 같은 문제로 보지 않음 |
| `--gap-max` | 15 | 채점 범위를 잇는 최대 간격 |
| `--keep-math` | 꺼짐 | 수식을 남기고 비교 (기준 파일이 수식을 보존할 때만) |

---

## 7. 바로 다음 할 일

1. 서버에 기준 파일 2개 올리고 `run_grade.sh --list` 로 run 폴더 이름 확인
2. 기하·확통 각각 채점 → 첫 점수 확보 (이후 모든 룰 수정의 기준점)
3. 미검출·오검출 목록을 보고 추출 룰 v4 조정
4. 룰 고칠 때마다 같은 명령을 다시 돌려 증감 확인

---

## 8. 확인 필요

- 서버 pypdf 로 신사고 기준 파일 2개를 읽었을 때 문제 수가 301 / 313 로 같은지
- `stage0_out/runs` 폴더 실제 이름 (필터 문자열을 정하려면 필요)
- SSE 스트리밍 미작동 원인
- 샘플 confidence 실측치 (115 DPI 충분성)
- 페이지당 실제 처리 시간 (워커 개수 산정)
- Vultr 월 요금
- LLM 모델·단가 (룰로 안 되는 부분만)
- 저작권 정책·이용약관
- 서버 SSH 보안 설정 (서비스 직전 일괄 점검)
