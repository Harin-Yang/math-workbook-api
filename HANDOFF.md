# 프로젝트 인수인계 — 문제집 추출기

새 대화를 시작할 때 이 문서를 붙여넣어 맥락을 이어간다.

---

## 0. 응답 규칙

- **명령마다 실행 위치를 먼저 표시할 것.** `【내 PC — PowerShell】` 또는 `【서버 — SSH】`
- 결론 먼저. 예고·과정 서술 없이 바로 답할 것.
- 개발 용어는 **괄호로 역할 설명을 덧붙일 것.**
- 사용자 작업을 최소화할 것. 명령은 붙여넣기 한 번으로 끝나게 묶을 것.
- 사용자는 최종 결과물에 대한 피드백을 하는 사람이다. 이해시키려 하지 말 것.
- 구어체로 쓰고, 한 문장이 끝나면 줄바꿈할 것.
- 추측한 수치·필드명은 "확인 필요"로 표시할 것. 임의로 경로나 이름을 지어내지 말 것.
- "정답지"는 해설지를 뜻할 때만. 채점 기준 파일은 **"테스트 기준 파일"**.
- 말해놓고 실제로 안 하지 말 것. 고치겠다고 했으면 그 턴에 고쳐서 올릴 것.
- 압박성 격려, 반복 사과 금지.

---

## 1. 만들려는 것

PDF 문제집·교과서를 업로드하면 **문제만 골라내 좌우 2단으로 조판한 편집 가능 문서**를 돌려주는 웹 서비스.

| 항목 | 결정 |
|---|---|
| OCR | Mathpix `v3/pdf` + SSE 스트리밍 |
| 산출물 | DOCX (한/글에서 편집 가능) |
| 조판 | 좌우 2단 |
| 서버 | Vultr 2vCPU / 4GB / 80GB, Ubuntu 24.04, 서울 (158.247.240.59) |
| 처리 방식 | 비동기 (큐 + 워커) |
| 대상 | 수학. 이후 국어·영어 확장 |
| 판매가 | 페이지당 30원 (미검증 가설), 크레딧 묶음 판매 |
| 원가 | Mathpix $0.005/p = 7.5원 (환율 1,500원) |
| PG 수수료 | 3% |

**무료 체험 (확정안)**

| 단계 | 대상 | 제공 | 다운로드 |
|---|---|---|---|
| 1. 미리보기 | 비가입자 | 앞 5페이지 처리, 화면에서 결과 전체 확인 | ✕ |
| 2. 가입 크레딧 | 이메일 인증 | 1회성 10페이지 | ○ |
| 3. 결제 | 유료 | 페이지당 | ○ |

**남용 방어** — 1~3순위만 적용.

1. 파일 SHA-256 해시 중복 차단 (차단 용도로만)
2. 다운로드 게이트 (서버에서 막을 것)
3. Cloudflare Turnstile 봇 차단

**판정 목표**

| 지표 | 목표 | 현재 (기하 / 확통) |
|---|---|---|
| 미탐지 누락 | 0% | 21.9% / 36.1% |
| 오검출 | 5% 미만 | **5.0% / 0.0%** ✅ |
| 그림 오첨부 | 0% | 미측정 |

---

## 2. 확정된 사실

**텍스트레이어는 쓸 수 없다.** 글자가 있어도 수식이 깨지고 읽는 순서가 붕괴된다. 전량 OCR 대상.

**`lines.json` 실측 완료.** 14개 파일 263페이지, 9,775줄. 비용 $1.03.

| 필드 | 뜻 |
|---|---|
| `type` | 줄의 종류 (본문·수식·도형·머리말) |
| `column` | 본문 흐름 단위 (좌우 2단이 아니다) |
| `region` | 위치와 크기 (`top_left_x/y`, `width`, `height`) |
| `font_size` | 글자 크기 |
| `id` / `parent_id` | 줄 사이 상하위 연결 |
| `conversion_output` | 최종 문서 포함 여부 (False면 중복) |

**기준 파일도 수식을 잃는다.** 한/글 → PDF 편집본은 글자는 남지만 **수식·숫자·영문 변수가 통째로 사라진다.**
그래서 채점은 **한글 뼈대만 비교**한다. 숫자·수식 정확도는 이 방식으로 잴 수 없다.

**서버 pypdf 는 앞 표시번호를 버린다.** 편집본의 `107.107)` 을 `107)` 로만 뽑는다.
그래서 parse_ref 는 뒤 번호만으로도 문제를 찾고, 번호가 1씩 커지는 가장 긴 사슬만 인정한다.

**Mathpix 의 줄 구조에서 배운 것**

- 발문을 `column` / `list_item` 같은 **빈 껍데기로 한 번 감싸고** 글자는 자식 줄에 넣는다.
  껍데기에 딸렸다고 버리면 문제가 통째로 사라진다. 타입 이름은 판마다 달라지므로
  **'글자가 없는 줄'** 인지로 판단해야 한다.
- 교과서는 소단원이 바뀌면 문제 번호가 1로 돌아간다. 번호를 억지로 키우면 안 된다.
- OCR 이 `증명` 을 `정명`, `탐구` 를 `밤구` 로 읽는다. 표기 목록에 오독 형태를 넣어야 한다.

**미해결:** SSE 스트리밍 이벤트 0건 (진행률용, 후순위). 샘플 115 DPI로 낮음.

---

## 3. 작업 환경

**서버 작업 폴더:** `~/mathocr/`

```
.env                  API 키 (커밋 안 됨)
pull.sh               코드 동기화 (scripts/ 폴더 통째로 받음)
scripts/
  stage0.py           Mathpix 측정 + 리포트
  extract.py          문제 추출 (v6)
  parse_ref.py        테스트 기준 파일 파서 (v3)
  grade.py            자동 채점기 (v2)
  probe.py            원본 줄 들여다보기 (원인 추적용)
  preview.py          2단 조판 HTML 미리보기
  analyze.py          lines.json 구조 분석
  inventory.py        자료 폴더 형식 판별
  run*.sh
refs/                 테스트 기준 파일 (신사고 기하·확통 편집본 PDF 2개)
samples/테스트자료_스캔본/        14개
stage0_out/runs/<파일명>/result.lines.json
grade_out/            채점 결과·점수 이력
```

**run 폴더 이름** — 필터는 `기하` / `확률과통계` 두 개면 된다.

```
[신사고 고등 기하] 01_이차곡선 / 03_벡터의 연산 / 05_공간도형
[신사고 고등 확률과통계] 01_순열 / 03_확률의 뜻과 활용 / 04_조건부확률 / 06_통계적 추정
```

**표준 명령**

```
cd ~/mathocr && bash pull.sh
bash scripts/run_grade.sh "refs/[기하][신사고][문제편집].pdf" "기하" 기하 "메모"
bash scripts/run_grade.sh "refs/[확률과통계][신사고][문제편집].pdf" "확률과통계" 확통 "메모"
python3 scripts/probe.py ./stage0_out /tmp/P.md --file 이차곡선 --pages 5-8
bash scripts/run.sh          # Mathpix 측정 (과금)
```

**서버 파이썬 패키지:** requests / pypdf / pillow. pdfplumber 안 쓴다.

`raw.githubusercontent.com` 은 CDN 캐시 때문에 옛 파일을 준다. codeload 압축본을 쓸 것.

---

## 4. 추출기 현황 (v6)

**판정 로직**

1. `page_info` 제거, `conversion_output=False` 제거, 페이지 폭 75% 넘는 그림 제거
2. 시작 판정: `문제/예제/유제/탐구/유형/0N` + 글자크기 60 이하
   - 부모가 있어도 **부모가 빈 껍데기면 통과** (부모를 못 찾아도 통과)
   - `탐구 (1)` 처럼 괄호 붙은 것은 소문항이므로 제외
3. 파일별 x 최빈 구간 학습 후 ±90 밖 제외
4. 끝 판정 — 명령형 어미(`~시오`, `~보자`) 이후 첫 서술문에서 끊는다. 예제도 같다.
5. 종료: 파일경계 / 단원제목 / 풀이시작 / 세로간격(평소의 4배) / 페이지초과 / 60줄
6. 번호는 원본 그대로. 억지로 키우지 않는다.
7. 그림 귀속: 본문 안의 것만. 발문이 그림을 가리키는데 없으면 같은 높이대 추정 후 표시

**점수 이력 (기하 기준)**

| 버전 | 정확도 | 누락률 | 오검출률 | 바뀐 것 |
|---|---|---|---|---|
| v3 원본 | 0.319 | 0.833 | 0.902 | 기준점 (기준 파서 고친 직후) |
| v4 | 0.425 | 0.356 | 0.203 | 빈 껍데기 부모 통과 |
| v4b | 0.507 | 0.329 | 0.076 | 예제 풀이에서 끊기 + 탐구 소문항 제외 |
| v5 | 0.575 | 0.233 | 0.067 | 껍데기 판정을 '글자 없는 줄' 로 |
| **v6 (현재)** | **0.616** | **0.219** | **0.050** | 예제도 발문에서 끊기, 풀이 오독 대응 |

**확통 (v6):** 정확도 0.525 / 누락률 0.361 / **오검출률 0.000**

---

## 5. 실패한 시도 — 다시 하지 말 것

**표기 없이 지문으로 시작하는 문제 찾기** (`UNMARKED`, 현재 꿨짐)

네 조건(x 대역 / 글자 크기 / 위쪽 여백 / 명령형 어미)을 모두 걸었는데도 결과는 이렇다.

| | 기하 | 확통 |
|---|---|---|
| 오검출률 | 0.05 → **0.11** | 0.00 → **0.13** |

**이유:** 개념 설명 문단과 발문은 네 조건이 전부 같다. 둘 다 본문 대역에 있고,
문단이 바뀌고, 개념 설명 안에도 `~해 보자` 같은 어미가 나온다.
**위치·모양 룰로는 구분할 수 없다.** 여기가 LLM 분류기가 필요한 지점이다.

기능 자체는 코드에 남아 있다. `python3 scripts/extract.py ... --unmarked` 로 시험 가능.

**채점기의 rescue 단계** — 순서가 어긋난 짝을 건지는 기능을 넣었으나 실측에서 0건이었다.
미검출과 오검출에 같은 문제가 동시에 있어도 순서 문제가 아니었다. 해롭진 않으니 둘 것.

---

## 6. 테스트 기준 파일

`교과서 모음_문제풀이` 폴더에 수기 편집본 123개. 이 중 2개를 서버 `refs/` 에 올려둄다.

| 파일 | 결과 |
|---|---|
| 신사고 기하 77쪽 | 문제 301개, 결손 0, 해설 52쪽부터 (`[정답 및 해설]` 표기) |
| 신사고 확통 75쪽 | 문제 303개, 해설 52쪽부터 (표기 없음 → 번호재시작으로 판정) |
| 교학사 확통 68쪽 | 문제 224개, 결손 0 |
| 천재(류) 경우의 수 17쪽 | 문제 65개, 파일명 "65제"와 일치 |

**출판사별로 어긋나는 부분** (parse_ref 가 처리)

| 어긋남 | 예 | 처리 |
|---|---|---|
| 머리말에 쪽번호가 박혀 쪽마다 문구가 다름 | `Du kannst ... \| 1 \| 용문아 수학방` | 숫자를 `#`로 가린 뒤 반복 빈도 판정 |
| 꼬리말이 `- 12 -` 형태 | 신사고 확통 | 쪽번호 패턴에 추가 |
| 해설 시작 표기가 없음 | 신사고 확통 | 번호가 다시 1부터 시작하는 쪽을 해설 시작으로 |
| 소단원 번호 `N-N-N.` 이 없음 | 신사고 확통 | 쪽 머리 2줄에서 제목 추정 |
| 앞 표시번호가 사라짐 | 서버 pypdf 전체 | 뒤 번호만으로 찾고 1씩 커지는 가장 긴 사슬만 인정 |

**산출물 사양**

- 개념 설명·풀이·답 제거, 발문과 소문항만
- 원본 번호와 쪽은 작은 라벨로만 남기고, 본문은 발문만
- 소단원 제목·구획 라벨 유지 / 답 쓸 공백 확보 / 정답·해설은 맨 뒤로 분리

---

## 7. 자동 채점기 (`scripts/grade.py`)

```
bash scripts/run_grade.sh --list
bash scripts/run_grade.sh <기준.pdf> <run폴더필터> [태그] [메모]
```

기준 파일은 매번 다시 읽는다. (예전에 캐시를 재활용해서 파서를 고쳐도 점수가 안 바뀌는 사고가 있었다)

**판정 기준** — `담김`(기준이 추출에 얼마나 들어갔나)과 `군더더기없음`(추출이 기준 안에 들어가나), 경계 0.85.

| 판정 | 뜻 |
|---|---|
| 완전일치 | 두 값 모두 0.85 이상 |
| 잘림 | 기준 문장 일부가 빠짐 |
| 넘침 | 기준에 없는 문장이 섞임 |
| 미검출 / 오검출 | 짝이 없음 |

**채점 범위 자동 축소** — 기준은 교재 전체이고 OCR 한 건 일부 단원뿐이다.
짝이 맞은 문제 사이 간격이 15 이하인 구간만 채점 범위로 잡는다.
**그래서 새 문제를 많이 찾으면 분모가 커져서 정확도가 떨어질 수 있다.** 숫자를 그대로 믿지 말 것.

---

## 8. 바로 다음 할 일

1. 남은 미검출은 대부분 **표기 없는 서술형**이다. 룰로는 한계가 드러났으므로
   LLM 분류기(문제/비문제 이진 판정)를 붙일지 결정해야 한다. 모델·단가 확인 필요.
2. 그림 귀속 정확도는 아직 한 번도 측정하지 않았다. 목표가 0% 오첨부이므로 채점 항목을 더해야 한다.
3. 산출물(DOCX 2단 조판) 생성기는 아직 없다. preview.py 가 HTML 미리보기까지만 한다.

---

## 9. 확인 필요

- LLM 모델·단가 (룰로 안 되는 부분에만 쓸 계획)
- 그림 귀속 정확도 측정 방법
- SSE 스트리밍 미작동 원인
- 샘플 confidence 실측치 (115 DPI 충분성)
- 페이지당 실제 처리 시간 (워커 개수 산정)
- Vultr 월 요금
- 저작권 정책·이용약관
- 서버 SSH 보안 설정 (서비스 직전 일괄 점검)
